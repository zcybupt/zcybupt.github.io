<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://zcybupt.github.io</id>
    <title>CODEFANCIER</title>
    <updated>2020-08-14T02:46:10.779Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://zcybupt.github.io"/>
    <link rel="self" href="https://zcybupt.github.io/atom.xml"/>
    <logo>https://zcybupt.github.io/images/avatar.png</logo>
    <icon>https://zcybupt.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, CODEFANCIER</rights>
    <entry>
        <title type="html"><![CDATA[Golang堆的实现]]></title>
        <id>https://zcybupt.github.io/post/GoHeap/</id>
        <link href="https://zcybupt.github.io/post/GoHeap/">
        </link>
        <updated>2020-08-13T13:19:55.000Z</updated>
        <content type="html"><![CDATA[<h2 id="堆的使用">堆的使用</h2>
<p>Golang 中没有给出堆的具体实现，仅提供了标准容器库 <code>container/heap</code> ，需手动实现。</p>
<p>heap 接口定义如下</p>
<pre><code class="language-go">type Interface interface {
	sort.Interface
	Push(x interface{}) // add x as element Len()
	Pop() interface{}   // remove and return element Len() - 1.
}
</code></pre>
<p>该接口同时继承了 <code>sort.Interface</code> ，定义如下</p>
<pre><code class="language-go">type Interface interface {
	// Len is the number of elements in the collection.
	Len() int
	// Less reports whether the element with
	// index i should sort before the element with index j.
	Less(i, j int) bool
	// Swap swaps the elements with indexes i and j.
	Swap(i, j int)
}
</code></pre>
<p>故创建堆时需实现 <code>Len()</code> 、<code>Less(i, j int)</code> 、<code>Swap(i, j int)</code> 、<code>Push(x interface{})</code> 、<code>Pop()</code> 五个方法。</p>
<p>向堆中插入节点时，堆会自动调整结构，Golang 中始终保持最后一个元素为堆顶，故 Pop 时返回<strong>最后一个节点</strong>并删除即可。</p>
<h3 id="实例">实例</h3>
<pre><code class="language-go">package main

import (
	&quot;container/heap&quot;
	&quot;fmt&quot;
	&quot;math/rand&quot;
	&quot;strconv&quot;
	&quot;time&quot;
)

type Person struct {
	Name string
	Age  int
}

type PersonHeap []Person

func (ph PersonHeap) Len() int {
	return len(ph)
}

func (ph PersonHeap) Swap(i, j int) {
	ph[i], ph[j] = ph[j], ph[i]
}

func (ph PersonHeap) Less(i, j int) bool {
	return ph[i].Age &lt; ph[j].Age // 大根堆
}

func (ph *PersonHeap) Push(p interface{}) {
	*ph = append(*ph, p.(Person))
}

func (ph *PersonHeap) Pop() interface{} {
	n := len(*ph)
	result := (*ph)[n-1]
	*ph = (*ph)[:n-1]
	return result
}

func main() {
	rand.Seed(time.Now().Unix())
	hp := &amp;PersonHeap{}
	for i := 0; i &lt; 6; i++ {
		*hp = append(*hp, Person{&quot;p&quot; + strconv.Itoa(i), rand.Intn(50)})
	}
	fmt.Printf(&quot;原始切片: %#v\n&quot;, hp)

	heap.Init(hp)

	fmt.Printf(&quot;堆: %#v\n&quot;, hp)

	fmt.Println(hp.Pop())
	heap.Push(hp, Person{&quot;newP&quot;, 30})

	fmt.Printf(&quot;%#v\n&quot;, hp)
}
</code></pre>
<h2 id="例题leetcode-23">例题(LeetCode #23)</h2>
<p>合并 k 个排序链表，返回合并后的排序链表。请分析和描述算法的复杂度。</p>
<p><strong>示例:</strong></p>
<blockquote>
<p>输入:<br>
[<br>
  1-&gt;4-&gt;5,<br>
  1-&gt;3-&gt;4,<br>
  2-&gt;6<br>
]<br>
输出: 1-&gt;1-&gt;2-&gt;3-&gt;4-&gt;4-&gt;5-&gt;6</p>
</blockquote>
<pre><code class="language-go">package main

import (
	&quot;container/heap&quot;
	&quot;fmt&quot;
)

type ListNode struct {
	Val  int
	Next *ListNode
}

type PriorityQueue []*ListNode

func (pq PriorityQueue) Len() int {
	return len(pq)
}

func (pq PriorityQueue) Less(i, j int) bool {
	return pq[i].Val &lt; pq[j].Val
}

func (pq *PriorityQueue) Swap(i, j int) {
	(*pq)[i].Val, (*pq)[j].Val = (*pq)[j].Val, (*pq)[i].Val
}

func (pq *PriorityQueue) Push(x interface{}) {
	*pq = append(*pq, x.(*ListNode))
}

func (pq *PriorityQueue) Pop() interface{} {
	result := (*pq)[len(*pq)-1]
	*pq = (*pq)[:len(*pq)-1]
	return result
}

func mergeKLists(lists []*ListNode) *ListNode {
	if lists == nil || len(lists) == 0 {
		return nil
	}

	dummyHead := &amp;ListNode{0, nil}
	curNode := dummyHead
	var pq PriorityQueue

	for _, list := range lists {
		tmp := list
		for tmp != nil {
			heap.Push(&amp;pq, tmp)
			tmp = tmp.Next
		}
	}

	for len(pq) != 0 {
		curNode.Next = heap.Pop(&amp;pq).(*ListNode)
		curNode = curNode.Next
	}
	curNode.Next = nil

	return dummyHead.Next
}

func main() {
	l1 := &amp;ListNode{1, nil}
	l1.Next = &amp;ListNode{4, nil}
	l1.Next.Next = &amp;ListNode{5, nil}

	l2 := &amp;ListNode{1, nil}
	l2.Next = &amp;ListNode{3, nil}
	l2.Next.Next = &amp;ListNode{4, nil}

	l3 := &amp;ListNode{2, nil}
	l3.Next = &amp;ListNode{6, nil}

	lists := []*ListNode{l1, l2, l3}

	result := mergeKLists(lists)
	for result != nil {
		fmt.Printf(&quot;%d &quot;, result.Val)
		result = result.Next
	}
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[删除链表的倒数第N个节点]]></title>
        <id>https://zcybupt.github.io/post/RemoveNthNodeFromEndOfList/</id>
        <link href="https://zcybupt.github.io/post/RemoveNthNodeFromEndOfList/">
        </link>
        <updated>2020-08-11T00:44:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-19">题目(<a href="https://leetcode-cn.com/problems/remove-nth-node-from-end-of-list/">LeetCode #19</a>)</h2>
<p>给定一个链表，删除链表的倒数第 n 个节点，并且返回链表的头结点。</p>
<p><strong>示例：</strong></p>
<blockquote>
<p>给定一个链表: <strong>1-&gt;2-&gt;3-&gt;4-&gt;5</strong>, 和 <strong>n = 2</strong>.</p>
<p>当删除了倒数第二个节点后，链表变为 <strong>1-&gt;2-&gt;3-&gt;5</strong>.</p>
</blockquote>
<p><strong>说明：</strong></p>
<p>给定的 n 保证是有效的。</p>
<p><strong>进阶：</strong></p>
<p>你能尝试使用一趟扫描实现吗？</p>
<h2 id="题解">题解</h2>
<h3 id="递归法">递归法</h3>
<p>利用递归的特性，从递归栈底向上累加即为倒数索引。</p>
<p><code>removeNode</code> 最终返回的值为输入链表的长度，若该返回值与 <code>n</code> 相等，则说明去掉的是第一个节点，此时头节点为 <code>head.next</code> 。</p>
<h4 id="java">Java</h4>
<pre><code class="language-java">class ListNode {
    int val;
    ListNode next;
    ListNode(int x) {
        val = x;
    }
}

public class Solution {
    public ListNode removeNthFromEnd(ListNode head, int n) {
    	return removeNode(head, n) == n ? head.next : head; 
    }

    public int removeNode(ListNode node, int n) {
        if (node.next == null) return 1;
        int m = removeNode(node.next, n);
        if (m == n)
            node.next = node.next.next;

        return m + 1;
    }
}
</code></pre>
<h4 id="python">Python</h4>
<pre><code class="language-python">class ListNode:
    def __init__(self, x):
        self.val = x
        self.next = None

class Solution:
    def removeNthFromEnd(self, head: ListNode, n: int) -&gt; ListNode:
        return head.next if self.remove_node(head, n) == n else head

    def remove_node(self, head: ListNode, n: int) -&gt; int:
        if not head.next: return 1
        m = self.remove_node(head.next, n)
        if m == n:
            head.next = head.next.next

        return m + 1
</code></pre>
<h4 id="go">Go</h4>
<pre><code class="language-go">package main

type ListNode struct {
    Val  int
    Next *ListNode
}

func removeNthFromEnd(head *ListNode, n int) *ListNode {
    if removeNode(head, n) == n {
        return head.Next
    } else {
        return head
    }
}

func removeNode(head *ListNode, n int) int {
    if head.Next == nil {
        return 1
    }

    m := removeNode(head.Next, n)
    if m == n {
        head.Next = head.Next.Next
    }

    return m + 1
}
</code></pre>
<h3 id="双指针法">双指针法</h3>
<p>使用快慢指针来查找索引，快指针先移动 <code>n</code> 个位置，再同时移动两指针，当快指针指向链表的尾节点时，慢指针所指即为倒数第 <code>n</code> 个节点。</p>
<h4 id="java-2">Java</h4>
<pre><code class="language-java">class Solution {
    public ListNode removeNthFromEnd(ListNode head, int n) {
        if (head == null || head.next == null) return null;

        ListNode fast = head, slow = head;
        for (int i = 0; i &lt; n; i++)
            fast = fast.next;

        if (fast == null)
            return head.next;

        while (fast.next != null) {
            fast = fast.next;
            slow = slow.next;
        }

        slow.next = slow.next.next;
        return head;
    }
}
</code></pre>
<h4 id="python-2">Python</h4>
<pre><code class="language-python">class Solution:
    def removeNthFromEnd(self, head: ListNode, n: int) -&gt; ListNode:
        fast, slow = head, head
        for i in range(n):
            fast = fast.next

        if not fast: return head.next

        while fast.next:
            fast = fast.next
            slow = slow.next

        slow.next = slow.next.next

        return head
</code></pre>
<h4 id="go-2">Go</h4>
<pre><code class="language-go">func removeNthFromEnd(head *ListNode, n int) *ListNode {
    fast, slow := head, head

    for i := 0; i &lt; n; i++ {
        fast = fast.Next
    }

    if fast == nil {
        return head.Next
    }

    for fast.Next != nil {
        fast = fast.Next
        slow = slow.Next
    }
    slow.Next = slow.Next.Next

    return head
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[最接近的三数之和]]></title>
        <id>https://zcybupt.github.io/post/ThreeSumClosest/</id>
        <link href="https://zcybupt.github.io/post/ThreeSumClosest/">
        </link>
        <updated>2020-08-10T01:22:59.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-16">题目(<a href="https://leetcode-cn.com/problems/3sum-closest/">LeetCode #16</a>)</h2>
<p>给定一个包括 <code>n</code> 个整数的数组 <code>nums</code> 和 一个目标值 <code>target</code> 。找出 <code>nums</code> 中的三个整数，使得它们的和与 <code>target</code> 最接近。返回这三个数的和。假定每组输入只存在唯一答案。</p>
<p><strong>示例：</strong></p>
<blockquote>
<p><strong>输入：</strong> nums = [-1,2,1,-4], target = 1</p>
<p><strong>输出：</strong> 2</p>
<p><strong>解释：</strong> 与 target 最接近的和是 2 (-1 + 2 + 1 = 2) 。</p>
</blockquote>
<p><strong>提示：</strong></p>
<ul>
<li>3 &lt;= nums.length &lt;= 10³</li>
<li>-10³ &lt;= nums[i] &lt;= 10³</li>
<li>-10<sup>4</sup>&gt; &lt;= target &lt;= 10<sup>4</sup></li>
</ul>
<h2 id="题解">题解</h2>
<p>本题与<a href="https://zcybupt.github.io/post/ThreeSum/">三数之和</a>类似 ，可采用双指针法，差别在于本题可以有重复元素。</p>
<p>由于处理过程中数组已经过排序，故在遍历数组元素的过程中，若 <code>nums[i] + nums[L] + nums[R] &gt; target</code> ，需将 <code>R</code> 左移，以减小三数和，进而减小其与 <code>target</code> 的差值；若 <code>nums[i] + nums[L] + nums[R] &lt; target</code> ，需将 <code>L</code> 右移，增大三数和来达到相同目的。</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">import java.util.Arrays;

public class Solution {
    public int threeSumClosest(int[] nums, int target) {
        int arrLen = nums.length;
        if (arrLen &lt; 3) return 0;

        Arrays.sort(nums);
        int L, R;
        int result = 0;
        int minDiff = Integer.MAX_VALUE;
        int curDiff, curSum;

        for (int i = 0; i &lt; arrLen; i++) {
            L = i + 1;
            R = arrLen - 1;
            while (L &lt; R) {
                curSum = nums[i] + nums[L] + nums[R];
                if (curSum == target) return curSum;
                curDiff = Math.abs(curSum - target);
                if (curDiff &lt; minDiff) {
                    result = curSum;
                    minDiff = curDiff;
                }
                if (curSum &gt; target) R--;
                else L++;
            }
        }

        return result;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class Solution:
    def threeSumClosest(self, nums: list, target: int) -&gt; int:
        list_len = len(nums)
        if not nums or list_len &lt; 3: return 0

        nums.sort()
        min_diff = float('inf')
        result = 0

        for i in range(list_len):
            l = i + 1
            r = list_len - 1
            while l &lt; r:
                cur_sum = nums[i] + nums[l] + nums[r]
                if cur_sum == target: return cur_sum
                cur_diff = abs(cur_sum - target)
                if cur_diff &lt; min_diff:
                    result = cur_sum
                    min_diff = cur_diff

                if cur_sum &gt; target: r -= 1
                else: l += 1

        return result
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">package main

import (
    &quot;math&quot;
    &quot;sort&quot;
)

func threeSumClosest(nums []int, target int) int {
    arrLen := len(nums)
    if nums == nil || arrLen &lt; 3 {
        return 0
    }

    sort.Ints(nums)
    result, curSum, curDiff := 0, 0, 0
    minDiff := math.MaxInt32

    for i, v := range nums {
        L, R := i+1, arrLen-1
        for L &lt; R {
            curSum = v + nums[L] + nums[R]
            if curSum == target {
                return curSum
            }
            curDiff = int(math.Abs(float64(curSum - target)))
            if curDiff &lt; minDiff {
                result = curSum
                minDiff = curDiff
            }

            if curSum &gt; target {
                R--
            } else {
                L++
            }
        }
    }

    return result
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[三数之和]]></title>
        <id>https://zcybupt.github.io/post/ThreeSum/</id>
        <link href="https://zcybupt.github.io/post/ThreeSum/">
        </link>
        <updated>2020-08-09T11:09:30.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-15">题目(<a href="https://leetcode-cn.com/problems/3sum/">LeetCode #15</a>)</h2>
<p>给你一个包含 n 个整数的数组 nums，判断 nums 中是否存在三个元素 a，b，c ，使得 a + b + c = 0 ？请你找出所有满足条件且不重复的三元组。</p>
<p>注意：答案中不可以包含重复的三元组。</p>
<p><strong>示例：</strong></p>
<blockquote>
<p>给定数组 nums = [-1, 0, 1, 2, -1, -4]，</p>
<p>满足要求的三元组集合为：<br>
[<br>
 [-1, 0, 1],<br>
 [-1, -1, 2]<br>
]</p>
</blockquote>
<h2 id="题解">题解</h2>
<h3 id="双指针法">双指针法</h3>
<p>若要找出所有满足条件的解，需三个变量 <code>a</code> , <code>b</code> , <code>c</code> 的所有情况且不能重复，若直接遍历时间复杂度为 <code>O(N³)</code> ，本题的关键在于如何降低时间复杂度和去除重复解。</p>
<p>可采用双指针法来实现遍历的不重不漏，首先将数组进行排序，再使<code>L</code> 指向 <code>i+1</code> ，<code>R</code> 指向末尾元素，两指针向中间移动直至相遇为止。在遍历过程中</p>
<ul>
<li>若 <code>nums[i]</code> 与 <code>nums[i-1]</code> 、<code>nums[L]</code> 与 <code>nums[L+1]</code> 、<code>nums[R]</code> 与 <code>nums[R-1]</code> 相等即说明遇到了重复元素，跳过即可；</li>
<li>若 <code>nums[i] + nums[L] + nums[R] == target</code> ，即为一组解，<code>L</code> 和 <code>R</code> 同时移动；</li>
<li>若 <code>nums[i] + nums[L] + nums[R] &gt; target</code> ，<code>R</code> 应向左移动以减小三数之和；</li>
<li>若 <code>nums[i] + nums[L] + nums[R] &lt; target</code> ，<code>L</code> 应向右移动以增大三数之和。</li>
</ul>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200809203120.png" alt="" loading="lazy"></figure>
<h4 id="时间复杂度">时间复杂度</h4>
<ul>
<li>时间复杂度：<code>O(N²)</code> ，数组排序 <code>O(NlogN)</code> ，遍历数组 <code>O(N)</code> ，双指针遍历 <code>O(N)</code> ，总体为 <code>O(NlogN) + O(N) * O(N)</code> ，<code>O(N²)</code></li>
<li>空间复杂度：<code>O(1)</code></li>
</ul>
<p>参考题解：<a href="https://leetcode-cn.com/problems/3sum/solution/pai-xu-shuang-zhi-zhen-zhu-xing-jie-shi-python3-by/">LeetCode</a></p>
<h4 id="java">Java</h4>
<pre><code class="language-java">import java.util.List;
import java.util.ArrayList;
import java.util.Arrays;

public class Solution {
    public List&lt;List&lt;Integer&gt;&gt; threeSum(int[] nums) {
        int listLen = nums.length;
        List&lt;List&lt;Integer&gt;&gt; results = new ArrayList&lt;&gt;();
        if (listLen &lt; 3) return results;

        Arrays.sort(nums);
        int L, R;

        for (int i = 0; i &lt; listLen; i++) {
            if (nums[i] &gt; 0) return results;
            if (i &gt; 0 &amp;&amp; nums[i] == nums[i - 1]) continue;
            L = i + 1;
            R = listLen - 1;
            while (L &lt; R) {
                if (nums[i] + nums[L] + nums[R] == 0) {
                    results.add(new ArrayList&lt;&gt;(Arrays.asList(nums[i], nums[L], nums[R])));
                    while (L &lt; R &amp;&amp; nums[L] == nums[L + 1]) L++;
                    while (L &lt; R &amp;&amp; nums[R] == nums[R - 1]) R--;
                    L++; R--;
                } else if (nums[i] + nums[L] + nums[R] &gt; 0) {
                    R--;
                } else {
                    L++;
                }
            }
        }

        return results;
    }
}
</code></pre>
<h4 id="python">Python</h4>
<pre><code class="language-python">class Solution:
    def threeSum(self, nums: list) -&gt; list:
        list_len = len(nums)
        results = []
        if not nums or list_len &lt; 3: return results

        nums.sort()
        for i in range(list_len):
            if nums[i] &gt; 0: return results
            if (i &gt; 0 and nums[i] == nums[i - 1]): continue
            L = i + 1
            R = list_len - 1
            while L &lt; R:
                if nums[i] + nums[L] + nums[R] == 0:
                    results.append([nums[i], nums[L], nums[R]])
                    while L &lt; R and nums[L] == nums[L + 1]: L += 1
                    while L &lt; R and nums[R] == nums[R - 1]: R -= 1
                    L += 1
                    R -= 1
                elif nums[i] + nums[L] + nums[R] &gt; 0:
                    R -= 1
                else:
                    L += 1
        return results
</code></pre>
<h4 id="go">Go</h4>
<pre><code class="language-go">package main

import (
    &quot;sort&quot;
)

func threeSum(nums []int) [][]int {
    arrLen := len(nums)
	var results [][]int
	if nums == nil || arrLen &lt; 3 {
		return results
	}

	sort.Ints(nums)

	for i := 0; i &lt; arrLen; i++ {
		if nums[i] &gt; 0 {
			return results
		}

		if i &gt; 0 &amp;&amp; nums[i] == nums[i-1] {
			continue
		}

		L, R := i+1, arrLen-1
		for L &lt; R {
			if nums[i]+nums[L]+nums[R] == 0 {
				results = append(results, []int{nums[i], nums[L], nums[R]})
				for L &lt; R &amp;&amp; nums[L] == nums[L+1] {
					L++
				}
				for L &lt; R &amp;&amp; nums[R] == nums[R-1] {
					R--
				}
				L++
				R--
			} else if nums[i]+nums[L]+nums[R] &gt; 0 {
				R--
			} else {
				L++
			}
		}
	}

	return results
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[字符串转换整数（atoi）]]></title>
        <id>https://zcybupt.github.io/post/StringToIntegerAtoi/</id>
        <link href="https://zcybupt.github.io/post/StringToIntegerAtoi/">
        </link>
        <updated>2020-08-04T07:59:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-8">题目(<a href="https://leetcode-cn.com/problems/string-to-integer-atoi/">LeetCode #8</a>)</h2>
<p>请你来实现一个 <code>atoi</code> 函数，使其能将字符串转换成整数。</p>
<p>首先，该函数会根据需要丢弃无用的开头空格字符，直到寻找到第一个非空格的字符为止。接下来的转化规则如下：</p>
<ul>
<li>
<p>如果第一个非空字符为正或者负号时，则将该符号与之后面尽可能多的连续数字字符组合起来，形成一个有符号整数。</p>
</li>
<li>
<p>假如第一个非空字符是数字，则直接将其与之后连续的数字字符组合起来，形成一个整数。</p>
</li>
<li>
<p>该字符串在有效的整数部分之后也可能会存在多余的字符，那么这些字符可以被忽略，它们对函数不应该造成影响。</p>
</li>
</ul>
<p>注意：假如该字符串中的第一个非空格字符不是一个有效整数字符、字符串为空或字符串仅包含空白字符时，则你的函数不需要进行转换，即无法进行有效转换。</p>
<p>在任何情况下，若函数不能进行有效的转换时，请返回 0 。</p>
<p><strong>提示：</strong></p>
<ul>
<li>本题中的空白字符只包括空格字符 <code>' '</code> 。</li>
<li>假设我们的环境只能存储 32 位大小的有符号整数，那么其数值范围为 [−2<sup>31</sup>,  2<sup>31</sup> − 1]。如果数值超过这个范围，请返回  INT_MAX (2<sup>31</sup> − 1) 或 INT_MIN (−2<sup>31</sup>) 。</li>
</ul>
<p><strong>示例 1:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;42&quot;<br>
<strong>输出:</strong> 42</p>
</blockquote>
<p><strong>示例 2:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;   -42&quot;<br>
<strong>输出:</strong> -42<br>
<strong>解释:</strong> 第一个非空白字符为 '-', 它是一个负号。<br>
我们尽可能将负号与后面所有连续出现的数字组合起来，最后得到 -42 。</p>
</blockquote>
<p><strong>示例 3:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;4193 with words&quot;<br>
<strong>输出:</strong> 4193<br>
<strong>解释:</strong> 转换截止于数字 '3' ，因为它的下一个字符不为数字。</p>
</blockquote>
<p><strong>示例 4:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;words and 987&quot;<br>
<strong>输出:</strong> 0<br>
<strong>解释:</strong> 第一个非空字符是 'w', 但它不是数字或正、负号。<br>
因此无法执行有效的转换。</p>
</blockquote>
<p><strong>示例 5:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;-91283472332&quot;<br>
<strong>输出:</strong> -2147483648<br>
<strong>解释:</strong> 数字 &quot;-91283472332&quot; 超过 32 位有符号整数范围。<br>
因此返回 INT_MIN (−2<sup>31</sup>) 。</p>
</blockquote>
<h2 id="题解">题解</h2>
<p>逐位处理字符串，根据每位字符确定一个覆盖所有情况的有限状态机，如下图所示（<a href="https://leetcode-cn.com/problems/string-to-integer-atoi/solution/zi-fu-chuan-zhuan-huan-zheng-shu-atoi-by-leetcode-/">图源</a>）：</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200804155943.png" alt="" loading="lazy"></figure>
<p>得到以下的状态转移表：</p>
<table>
<thead>
<tr>
<th style="text-align:center"></th>
<th style="text-align:center">space</th>
<th style="text-align:center">+ / -</th>
<th style="text-align:center">number</th>
<th style="text-align:center">other</th>
</tr>
</thead>
<tbody>
<tr>
<td style="text-align:center">start</td>
<td style="text-align:center">start</td>
<td style="text-align:center">signed</td>
<td style="text-align:center">in_number</td>
<td style="text-align:center">end</td>
</tr>
<tr>
<td style="text-align:center">signed</td>
<td style="text-align:center">end</td>
<td style="text-align:center">end</td>
<td style="text-align:center">in_number</td>
<td style="text-align:center">end</td>
</tr>
<tr>
<td style="text-align:center">in_number</td>
<td style="text-align:center">end</td>
<td style="text-align:center">end</td>
<td style="text-align:center">in_number</td>
<td style="text-align:center">end</td>
</tr>
<tr>
<td style="text-align:center">end</td>
<td style="text-align:center">end</td>
<td style="text-align:center">end</td>
<td style="text-align:center">end</td>
<td style="text-align:center">end</td>
</tr>
</tbody>
</table>
<p>根据上表即可编写代码即可</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">class FSM {
    public String status = &quot;start&quot;;
    public int sign = 1;
    public long ans = 0;
    public HashMap&lt;String, String[]&gt; table = new HashMap&lt;&gt;();

    public FSM() {
        table.put(&quot;start&quot;, new String[]{&quot;start&quot;, &quot;signed&quot;, &quot;in_number&quot;, &quot;end&quot;});
        table.put(&quot;signed&quot;, new String[]{&quot;end&quot;, &quot;end&quot;, &quot;in_number&quot;, &quot;end&quot;});
        table.put(&quot;in_number&quot;, new String[]{&quot;end&quot;, &quot;end&quot;, &quot;in_number&quot;, &quot;end&quot;});
        table.put(&quot;end&quot;, new String[]{&quot;end&quot;, &quot;end&quot;, &quot;end&quot;, &quot;end&quot;});
    }

    public int getColumn(char ch) {
        if (ch == ' ') return 0;
        if (ch == '+' || ch == '-') return 1;
        if (ch &gt;= '0' &amp;&amp; ch &lt;= '9') return 2;
        return 3;
    }

    public void get(char ch) {
        status = table.get(status)[getColumn(ch)];
        if (status.equals(&quot;in_number&quot;)) {
            int curInt = Character.getNumericValue(ch);
            ans = ans * 10 + curInt;
            if ((int) ans != ans) {
                if (sign == 1) ans = Integer.MAX_VALUE;
                if (sign == -1) ans = Integer.MIN_VALUE;
            }
        } else if (status.equals(&quot;signed&quot;)) {
            sign = ch == '+' ? 1 : -1;
        }
    }
}

class Solution {
    public int myAtoi(String str) {
        FSM fsm = new FSM();
        for (char ch : str.toCharArray()) {
            fsm.get(ch);
        }
        return fsm.sign * (int) fsm.ans;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class FSM:
    def __init__(self):
        self.state = 'start'
        self.sign = 1
        self.ans = 0
        self.table = {
            'start': ['start', 'signed', 'in_number', 'end'],
            'signed': ['end', 'end', 'in_number', 'end'],
            'in_number': ['end', 'end', 'in_number', 'end'],
            'end': ['end', 'end', 'end', 'end'],
        }

    def get_column(self, c: str) -&gt; int:
        if c.isspace():
            return 0
        if c in ['+', '-']:
            return 1
        if c.isdigit():
            return 2
        return 3

    def get(self, c: str):
        self.state = self.table[self.state][self.get_column(c)]
        if self.state == 'in_number':
            self.ans = self.ans * 10 + int(c)
            self.ans = min(self.ans, INT_MAX) if self.sign == 1 else min(self.ans, -INT_MIN)
        elif self.state == 'signed':
            self.sign = 1 if c == '+' else -1


class Solution:
    def myAtoi(self, str: str) -&gt; int:
        fsm = FSM()
        for c in str:
            fsm.get(c)
        return fsm.sign * fsm.ans
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[无重复字符的最长子串]]></title>
        <id>https://zcybupt.github.io/post/LengthOfLongestSubstring/</id>
        <link href="https://zcybupt.github.io/post/LengthOfLongestSubstring/">
        </link>
        <updated>2020-08-02T09:09:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-3">题目(<a href="https://leetcode-cn.com/problems/longest-substring-without-repeating-characters/">LeetCode #3</a>)</h2>
<p>给定一个字符串，请你找出其中不含有重复字符的<strong>最长子串</strong>的长度。</p>
<p><strong>示例 1:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;abcabcbb&quot;<br>
<strong>输出:</strong> 3<br>
<strong>解释:</strong> 因为无重复字符的最长子串是 &quot;abc&quot;，所以其长度为 3。</p>
</blockquote>
<p><strong>示例 2:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;bbbbb&quot;<br>
<strong>输出:</strong> 1<br>
<strong>解释:</strong> 因为无重复字符的最长子串是 &quot;b&quot;，所以其长度为 1。</p>
</blockquote>
<p><strong>示例 3:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;pwwkew&quot;<br>
<strong>输出:</strong> 3<br>
<strong>解释:</strong> 因为无重复字符的最长子串是 &quot;wke&quot;，所以其长度为 3。答案必须是<strong>子串</strong>的长度，&quot;pwke&quot; 是一个子序列，不是子串。</p>
</blockquote>
<h2 id="题解">题解</h2>
<p>可采用滑动窗口法，将窗口内的字符保存到集合中，窗口的左边界向右移动时，弹出已不在窗口内的元素；窗口的右边界向右移动时判断字符是否已在集合中，若不在则加入，若已有则终止循环，记录当前最长长度。</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">import java.util.HashSet;

public class Solution {
    public int lengthOfLongestSubstring(String s) {
        HashSet&lt;Character&gt; charSet = new HashSet&lt;&gt;();
        int length = s.length();
        int rk = -1, ans = 0;

        for (int i = 0; i &lt; length; i++) {
            if (i != 0) {
                charSet.remove(s.charAt(i - 1));
            }
            while (rk + 1 &lt; length &amp;&amp; !charSet.contains(s.charAt(rk + 1))) {
                charSet.add(s.charAt(rk + 1));
                rk++;
            }
            ans = Math.max(ans, rk - i + 1);
        }

        return ans;
    }

    public static void main(String[] args) {
        String s = &quot;abcabcbb&quot;;
        Solution solution = new Solution();
        System.out.println(solution.lengthOfLongestSubstring(s));
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class Solution:
    def lengthOfLongestSubstring(self, s: str) -&gt; int:
        char_set = set()
        rk, ans = -1, 0
        length = len(s)

        for i in range(length):
            if i != 0: char_set.remove(s[i - 1])

            while rk + 1 &lt; length and s[rk + 1] not in char_set:
                char_set.add(s[rk + 1])
                rk += 1
            ans = max(ans, rk - i + 1)

        return ans
</code></pre>
<h3 id="go">Go</h3>
<p>Golang 中没有集合，使用 map 实现过于繁琐，可采用 int 数组来模拟集合操作。</p>
<p>将每个字符的 <code>ASCII</code> 值作为数组的索引，数组内存储的为该字符的出现与否（1 或 0）。</p>
<pre><code class="language-go">package main

import (
	&quot;fmt&quot;
	&quot;math&quot;
)

func lengthOfLongestSubstring(s string) int {
	var window [128]int         // ASCII 总共定义了 128 个字符
	length := len(s)
	rk, ans := -1, 0

	for i, _ := range s {
		if i != 0 {
			window[s[i-1]] = 0  // 左边界移出窗口
		}
		for rk+1 &lt; length &amp;&amp; window[s[rk+1]] == 0 {
			window[s[rk+1]] = 1 // 右边界移入窗口
			rk++
		}
		ans = int(math.Max(float64(ans), float64(rk-i+1)))
	}
	return ans
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[二叉查找树]]></title>
        <id>https://zcybupt.github.io/post/BinarySearchTree/</id>
        <link href="https://zcybupt.github.io/post/BinarySearchTree/">
        </link>
        <updated>2020-07-28T07:38:11.000Z</updated>
        <content type="html"><![CDATA[<h2 id="定义">定义</h2>
<p><strong><a href="https://zh.wikipedia.org/wiki/%E4%BA%8C%E5%85%83%E6%90%9C%E5%B0%8B%E6%A8%B9">二叉查找树</a></strong>，也称为<strong>二叉搜索树</strong>、<strong>有序二叉树</strong>（ordered binary tree）或<strong>排序二叉树</strong>（sorted binary tree），是指一棵空树或者具有下列性质的二叉树：</p>
<ol>
<li>若任意节点的左子树不空，则左子树上所有节点的值均小于它的根节点的值；</li>
<li>若任意节点的右子树不空，则右子树上所有节点的值均大于或等于它的根节点的值；</li>
<li>任意节点的左、右子树也分别为二叉查找树；</li>
</ol>
<p>其优势在于查找、插入的时间复杂度较低，理想情况下为 <code>O(logn)</code> 。</p>
<h2 id="实现">实现</h2>
<h3 id="节点定义">节点定义</h3>
<pre><code class="language-java">class Node {
    int value;
    Node parent;
    Node left;
    Node right;

    public Node(int value, Node parent, Node left, Node right) {
        this.value = value;
        this.parent = parent;
        this.left = left;
        this.right = right;
    }
}
</code></pre>
<h3 id="查找节点">查找节点</h3>
<pre><code class="language-java">public static Node search(int ele) {
    Node tmp = root;

    while (tmp != null &amp;&amp; tmp.value != ele) {
        if (ele &lt; tmp.value) {
            tmp = tmp.left;
        } else {
            tmp = tmp.right;
        }
    }

    return tmp;
}
</code></pre>
<h3 id="插入节点">插入节点</h3>
<p>先找到待插入节点的父节点，再插入新节点并返回。</p>
<pre><code class="language-java">public static Node insert(int ele) {
    if (root == null) {
        root = new Node(ele, null, null, null);
        return root;
    }

    // 找到即将插入元素的父节点
    Node parent = null;
    Node tmp = root;
    while (tmp != null) {
        parent = tmp;
        if (ele &lt; tmp.value) {
            tmp = tmp.left;
        } else {
            tmp = tmp.right;
        }
    }

    Node newNode = new Node(ele, parent, null, null);
    if (parent.value &gt; ele) {
        parent.left = newNode;
    } else {
        parent.right = newNode;
    }

    return newNode;
}
</code></pre>
<h3 id="删除节点">删除节点</h3>
<h4 id="替换-移植节点">替换 / 移植节点</h4>
<p>使用 <code>newNode</code> 替换 <code>nodeToReplace</code> ，并返回被替换的节点。</p>
<pre><code class="language-java">public static Node transplant(Node nodeToReplace, Node newNode) {
    if (nodeToReplace.parent == null) {
        root = newNode;
    } else if (nodeToReplace == nodeToReplace.parent.left) {
        nodeToReplace.parent.left = newNode;
    } else {
        nodeToReplace.parent.right = newNode;
    }

    if (newNode != null) {
        newNode.parent = nodeToReplace.parent;
    }

    return newNode;
}
</code></pre>
<h4 id="找到后继节点">找到后继节点</h4>
<p>找到节点的中序后继，即该节点右子树上的最小值。</p>
<pre><code class="language-java">public static Node getMinimum(Node node) {
    while (node.left != null) {
        node = node.left;
    }

    return node;
}
</code></pre>
<h4 id="删除">删除</h4>
<p>将待删除节点的中序后继移动到待删除节点位置，并调整树的结构</p>
<ul>
<li>
<p>若待删除节点的父节点无左子树或右子树，直接将该节点的右子树或左子树上移即可；</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730102431.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>若待删除节点的左右子树均不为空，以下图所示树为例，其中 9 为待删除节点，此时其中序后继 10 与 9 不直接相连</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730153412.png" alt="" loading="lazy"></figure>
<ul>
<li>先找到待删除节点的中序后继，在此为 10，即为待删除节点的替换节点，此时该后继不与待删除节点直接相连，先将其子树 11 移至 10 位置。①使 <code>successorNode.right</code> 指向待删除节点 9 的 <code>right</code> ，②使 <code>successorNode.right.parent</code> 指向 10，得到：</li>
</ul>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730154437.png" alt="" loading="lazy"></figure>
<ul>
<li>再将 <code>successorNode</code> 移到 <code>deleteNode</code> 的位置，并修改其左子树的指向。</li>
</ul>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730154935.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>若中序后继与待删除节点直接相连，如下图所示：</p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730160504.png" alt="" loading="lazy"></figure>
<ul>
<li>9 与 12 直接相连，则直接将 12 上移至 9 的位置，并修改 12 的左子树指向即可。</li>
</ul>
<figure data-type="image" tabindex="6"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730160537.png" alt="" loading="lazy"></figure>
</li>
</ul>
<pre><code class="language-java">public static Node delete(Node deleteNode) {
    if (deleteNode != null) {
        Node nodeToReturn = null;
        if (deleteNode.left == null) {
            nodeToReturn = transplant(deleteNode, deleteNode.right);
        } else if (deleteNode.right == null) {
            nodeToReturn = transplant(deleteNode, deleteNode.left);
        } else {
            Node successorNode = getMinimum(deleteNode.right);
            if (successorNode.parent != deleteNode) {
                transplant(successorNode, successorNode.right);
                successorNode.right = deleteNode.right;
                successorNode.right.parent = successorNode;
            }
            transplant(deleteNode, successorNode);
            successorNode.left = deleteNode.left;
            successorNode.left.parent = successorNode;
            nodeToReturn = successorNode;
        }
        return nodeToReturn;
    }
    return null;
}
</code></pre>
<h2 id="缺点">缺点</h2>
<p>插入或删除节点过程中如果不及时调整，将使查找效率降低，如将数组 <code>[2, 3, 4, 5, 6]</code> 插入树的过程中，不加调整将得到如下结构，此时查找和插入效率退化至 <code>O(n)</code> 。</p>
<figure data-type="image" tabindex="7"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200729204357.png" alt="" loading="lazy"></figure>
<p>为解决查找效率退化的问题，又提出了 AVL 树和红黑树等，可将最坏效率降至 <code>O(logn)</code>。</p>
<p><a href="https://github.com/zcybupt/rawfiles/blob/master/codes/Java/Code_50_BinarySearchTree.java">完整代码</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Morris遍历]]></title>
        <id>https://zcybupt.github.io/post/MorrisTraversal/</id>
        <link href="https://zcybupt.github.io/post/MorrisTraversal/">
        </link>
        <updated>2020-07-26T10:20:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="原理">原理</h2>
<p>无论是手动创建栈遍历树还是递归（即调用系统栈）遍历树，其空间复杂度均为 <code>O(N)</code> ，而 Morris 实现了 <code>O(1)</code> 常数空间复杂度的树的遍历。</p>
<p>递归遍历树的过程中，每个节点均被遍历了三次，前中后序取决于打印的位置。</p>
<pre><code class="language-java">public class RecursiveTraversal {
    public static void recursiveTraverse(TreeNode head) {
        if (head == null) return;

        // 先序遍历
        // System.out.println(head.val);

        recursiveTraverse(head.left);

        // 中序遍历
        // System.out.println(head.val);
        
        recursiveTraverse(head.right);
        
        // 后序遍历
        // System.out.println(head.val);
    }
}
</code></pre>
<p>Morris 遍历实质上是人为地追踪递归过程中遍历位置的变化。其过程如下：</p>
<p>设 <code>cur</code> 为当前节点。</p>
<ol>
<li>若 <code>cur</code> 无左子树，<code>cur</code> 向右移动</li>
<li>若 <code>cur</code> 有左子树，找到左子树上最右节点，记为 <code>rightmost</code>
<ol>
<li>若 <code>rightmost</code> 的右孩子为 <code>null</code> ，<code>rightmost.right = cur</code> ，<code>cur</code> 向左移动</li>
<li>若 <code>rightmost</code> 的右孩子为 <code>cur</code> ，<code>rightmost.right = null</code> ，<code>cur</code> 向右移动</li>
</ol>
</li>
</ol>
<ul>
<li>
<p>若某节点有左子树，则该节点将被遍历两次（即 <code>2.2</code> ），若没有左子树则将被遍历一次；</p>
</li>
<li>
<p>在第二遍历某节点时，当前节点总是该节点左子树的 <code>rightmost</code> 的后继（节点的后继指中序遍历输出结果的后续节点）</p>
</li>
<li>
<p>通过 <code>rightmost</code> 的右孩子的指向来判断是第一次还是第二次遍历到当前节点。</p>
</li>
</ul>
<p>下图为每一步迭代的结果（从左至右，从上到下），cur代表当前节点，深色节点表示该节点已输出。（图源：<a href="https://www.cnblogs.com/anniekim/archive/2013/06/15/morristraversal.html">AnnieKim</a>）</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200727191413.png" alt="" loading="lazy"></figure>
<h2 id="实现">实现</h2>
<h3 id="先序遍历">先序遍历</h3>
<p>第一次到达一个节点时就输出。</p>
<h4 id="java">Java</h4>
<pre><code class="language-java">class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;

    TreeNode(int val) {
        this.val = val;
    }
}

public class MorrisTraversal {
    public static void morrisPreorderTraverse(TreeNode head) {
        if (head == null) return;

        TreeNode cur1 = head;
        TreeNode cur2 = null;

        while (cur1 != null) {
            cur2 = cur1.left;
            if (cur2 != null) {                       // cur1 有左子树
                while (cur2.right != null &amp;&amp; cur2.right != cur1) {
                    cur2 = cur2.right;                // 左子树上最右节点, 此时 cur2 即为 rightmost
                }
                if (cur2.right == null) {             // rightmost 的右孩子为空, 此时为第一次遍历当前节点
                    cur2.right = cur1;
                    System.out.print(cur1.val + &quot; &quot;); // 第一次遍历一个节点, 打印该节点
                    cur1 = cur1.left;
                    continue;
                } else {
                    cur2.right = null;                // rightmost 的右孩子指向 cur1, 此时为第二次遍历当前节点
                }
            } else {
                System.out.print(cur1.val + &quot; &quot;);     // 节点无左子树, 只会遍历一遍, 故直接输出
            }
            cur1 = cur1.right;                        // cur1 无左子树或 rightmost 的右孩子指向 cur1 时
        }
    }
}
</code></pre>
<h4 id="python">Python</h4>
<pre><code class="language-python">class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


def morris_preorder_traverse(head: TreeNode) -&gt; None:
    if not head: return

    cur1 = head
    while cur1:
        cur2 = cur1.left
        if cur2:
            while cur2.right and cur2.right != cur1:
                cur2 = cur2.right
            if cur2.right is None:
                cur2.right = cur1
                print(cur1.val, end=&quot; &quot;)
                cur1 = cur1.left
                continue
            else:
                cur2.right = None
        else:
            print(cur1.val, end=&quot; &quot;)
        cur1 = cur1.right
</code></pre>
<h4 id="go">Go</h4>
<pre><code class="language-go">type TreeNode struct {
	val   int
	left  *TreeNode
	right *TreeNode
}

func morrisPreorderTraverse(head *TreeNode) {
	if head == nil {
		return
	}

	cur1 := head
	var cur2 *TreeNode

	for cur1 != nil {
		cur2 = cur1.left
		if cur2 != nil {
			for cur2.right != nil &amp;&amp; cur2.right != cur1 {
				cur2 = cur2.right
			}
			if cur2.right == nil {
				cur2.right = cur1
				fmt.Printf(&quot;%d &quot;, cur1.val)
				cur1 = cur1.left
				continue
			} else {
				cur2.right = nil
			}
		} else {
			fmt.Printf(&quot;%d &quot;, cur1.val)
		}
		cur1 = cur1.right
	}
}
</code></pre>
<h3 id="中序遍历">中序遍历</h3>
<p>有左子树的节点第二次回到该节点时再打印；无左子树的节点只会遍历一次，故直接输出。如下图输出顺序为 <code>2 1 3</code> 。</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200727220729.png" alt="" loading="lazy"></figure>
<p>当前节点无左子树或第二次遍历当前节点（即 <code>rightmost</code> 的右孩子指向 <code>cur1</code>）时均需输出当前节点值，而只有这两种情况需将指针右移，故可理解为只要向右移动就输出。</p>
<h4 id="java-2">Java</h4>
<pre><code class="language-java">public class MorrisTraversal {
    public static void morrisInOrderTraverse(TreeNode head) {
        if (head == null) return;

        TreeNode cur1 = head;
        TreeNode cur2 = null;

        while (cur1 != null) {
            cur2 = cur1.left;
            if (cur2 != null) {               // cur1 有左子树
                while (cur2.right != null &amp;&amp; cur2.right != cur1) {
                    cur2 = cur2.right;        // 左子树上最右节点, 此时 cur2 即为 rightmost
                }
                if (cur2.right == null) {     // rightmost 的右孩子为空, 此时为第一次遍历当前节点
                    cur2.right = cur1;
                    cur1 = cur1.left;
                    continue;
                } else {
                    cur2.right = null;        // rightmost 的右孩子指向 cur1, 此时为第二次遍历当前节点
                }
            }
            System.out.print(cur1.val + &quot; &quot;); // 当前节点无左子树或第二次遍历当前节点
            cur1 = cur1.right;                // cur1 无左子树或 rightmost 的右孩子指向 cur1 时
        }
    }
}
</code></pre>
<h4 id="python-2">Python</h4>
<pre><code class="language-python">def morris_in_order_traverse(head: TreeNode) -&gt; None:
    if not head: return

    cur1 = head
    while cur1:
        cur2 = cur1.left
        if cur2:
            while cur2.right and cur2.right != cur1:
                cur2 = cur2.right
            if cur2.right is None:
                cur2.right = cur1
                cur1 = cur1.left
                continue
            else:
                cur2.right = None
        print(cur1.val, end=&quot; &quot;)
        cur1 = cur1.right
</code></pre>
<h4 id="go-2">Go</h4>
<pre><code class="language-go">func morrisInOrderTraverse(head *TreeNode) {
	if head == nil {
		return
	}

	cur1 := head
	var cur2 *TreeNode

	for cur1 != nil {
		cur2 = cur1.left
		if cur2 != nil {
			for cur2.right != nil &amp;&amp; cur2.right != cur1 {
				cur2 = cur2.right
			}
			if cur2.right == nil {
				cur2.right = cur1
				cur1 = cur1.left
				continue
			} else {
				cur2.right = nil
			}
		}
		fmt.Printf(&quot;%d &quot;, cur1.val)
		cur1 = cur1.right
	}
}
</code></pre>
<h3 id="后序遍历">后序遍历</h3>
<p>第二次遍历到某节点时，逆序打印其左子树的右边界。遍历完后再逆序打印整棵树的右边界即为后序遍历。</p>
<h4 id="java-3">Java</h4>
<pre><code class="language-java">public class MorrisTraversal {
    public static void morrisPostOrderTraverse(TreeNode head) {
        if (head == null) return;

        TreeNode cur1 = head;
        TreeNode cur2 = null;

        while (cur1 != null) {
            cur2 = cur1.left;
            if (cur2 != null) {             // cur1 有左子树
                while (cur2.right != null &amp;&amp; cur2.right != cur1) {
                    cur2 = cur2.right;      // 左子树上最右节点, 此时 cur2 即为 rightmost
                }
                if (cur2.right == null) {   // rightmost 的右孩子为空, 此时为第一次遍历当前节点
                    cur2.right = cur1;
                    cur1 = cur1.left;
                    continue;
                } else {
                    cur2.right = null;      // rightmost 的右孩子指向 cur1, 此时为第二次遍历当前节点
                    printEdge(cur1.left);   // 第二次遍历到该节点, 打印左子树的右边界
                }
            }
            cur1 = cur1.right;              // cur1 无左子树
        }
        printEdge(head);                    // 打印整棵树的右边界
    }

    public static void printEdge(TreeNode head) {
        TreeNode tail = reverseEdge(head);
        TreeNode cur = tail;

        while (cur != null) {
            System.out.print(cur.val + &quot; &quot;);
            cur = cur.right;
        }
        recursiveTraverse(tail);
    }

    public static TreeNode reverseEdge(TreeNode from) {
        TreeNode pre = null;
        TreeNode next = null;

        while (from != null) {
            next = from.right;
            from.right = pre;
            pre = from;
            from = next;
        }
        return pre;
    }
}
</code></pre>
<h4 id="python-3">Python</h4>
<pre><code class="language-python">def morris_post_order_traverse(head: TreeNode) -&gt; None:
    if not head: return

    cur1 = head
    while cur1:
        cur2 = cur1.left
        if cur2:
            while cur2.right and cur2.right != cur1:
                cur2 = cur2.right
            if cur2.right is None:
                cur2.right = cur1
                cur1 = cur1.left
                continue
            else:
                cur2.right = None
                print_edge(cur1.left)
        cur1 = cur1.right
    print_edge(head)


def print_edge(head: TreeNode) -&gt; None:
    tail = reverse_edge(head)
    cur = tail

    while cur:
        print(cur.val, end=&quot; &quot;)
        cur = cur.right

    reverse_edge(tail)


def reverse_edge(head: TreeNode) -&gt; TreeNode:
    pre = None
    while head:
        rear = head.right
        head.right = pre
        pre = head
        head = rear
    return pre
</code></pre>
<h4 id="go-3">Go</h4>
<pre><code class="language-go">func morrisPostOrderTraverse(head *TreeNode) {
	if head == nil {
		return
	}

	cur1 := head
	var cur2 *TreeNode

	for cur1 != nil {
		cur2 = cur1.left
		if cur2 != nil {
			for cur2.right != nil &amp;&amp; cur2.right != cur1 {
				cur2 = cur2.right
			}
			if cur2.right == nil {
				cur2.right = cur1
				cur1 = cur1.left
				continue
			} else {
				cur2.right = nil
				printEdge(cur1.left)
			}
		}
		cur1 = cur1.right
	}
	printEdge(head)
}

func printEdge(head *TreeNode) {
	tail := reverseEdge(head)
	cur := tail

	for cur != nil {
		fmt.Printf(&quot;%d &quot;, cur.val)
		cur = cur.right
	}

	reverseEdge(tail)
}

func reverseEdge(head *TreeNode) *TreeNode {
	var pre *TreeNode
	for head != nil {
		next := head.right
		head.right = pre
		pre = head
		head = next
	}

	return pre
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[最短回文串]]></title>
        <id>https://zcybupt.github.io/post/ShortestPalindrome/</id>
        <link href="https://zcybupt.github.io/post/ShortestPalindrome/">
        </link>
        <updated>2020-07-22T10:28:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-214">题目(<a href="https://leetcode-cn.com/problems/shortest-palindrome">LeetCode #214</a>)</h2>
<p>给定一个字符串 s，你可以通过在字符串前面添加字符将其转换为回文串。找到并返回可以用这种方式转换的最短回文串。</p>
<p><strong>示例 1:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;aacecaaa&quot;<br>
<strong>输出:</strong> &quot;aaacecaaa&quot;</p>
</blockquote>
<p><strong>示例 2:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;abcd&quot;<br>
<strong>输出:</strong> &quot;dcbabcd&quot;</p>
</blockquote>
<h2 id="题解">题解</h2>
<p>可利用<a href="https://zcybupt.github.io/post/Manacher/">Manacher 算法</a>找到原字符串包含第一个字符的最长回文子串，将该回文子串后的字符逆序加到开头即可。</p>
<p>示例 1 中，对 <code>aacecaaa</code> 来说，包含第一个字符的最长回文子串为 <code>aacecaa</code> ，需把剩余的 <code>a</code> 逆序加到开头，即得到 <code>aaacecaaa</code> ；示例 2 中，<code>abcd</code> 包含第一个字符的最长回文子串为 <code>a</code> ，<code>bcd</code> 逆序得到 <code>dcb</code> ，放到开头即得到 <code>dcbabcd</code> 。</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">public class ShortestPalindrome {
    public static String preprocess(String s) {
        String result = &quot;^&quot;;
        for (int i = 0; i &lt; s.length(); i++) {
            result += &quot;#&quot; + s.charAt(i);
        }

        return result + &quot;#$&quot;;
    }

    public static String shortestPalindrome(String s) {
        if (s == null || s.length() == 0) return &quot;&quot;;

        String tmpStr = preprocess(s);
        int strLen = tmpStr.length();
        int C = 0, R = 0;
        int[] p = new int[strLen];
        int maxLen = 0;

        for (int i = 1; i &lt; strLen - 1; i++) {
            p[i] = R &gt; i ? Math.min(p[2 * C - i], R - i) : 1;

            while (tmpStr.charAt(i - p[i]) == tmpStr.charAt(i + p[i])) {
                p[i]++;
            }

            if (R &lt; p[i] + i) {
                R = p[i] + i;
                C = i;
            }

            // 仅保存以第一个字符开头的回文子串的最大长度
            // (C - p[C]) / 2 即为当前回文串的起始索引
            if (maxLen &lt; p[i] - 1 &amp;&amp; (C - p[C]) / 2 == 0) {
                maxLen = p[i] - 1;
            }
        }

        // 逆序剩余部分字符串
        String result = &quot;&quot;;
        for (int i = s.length() - 1; i &gt;= maxLen; i--) {
            result += s.charAt(i);
        }

        return result + s;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class ShortestPalindrome:
    def shortest_palindrome(self, s: str) -&gt; str:
        tmp_str = &quot;^#&quot; + &quot;&quot;.join([x + &quot;#&quot; for x in s]) + &quot;$&quot;
        str_len = len(tmp_str)
        p = [0] * str_len
        c, r = 0, 0
        max_len = 0

        for i in range(1, str_len - 1):
            p[i] = min(p[2 * c - i], r - i) if r &gt; i else 1
            while tmp_str[i - p[i]] == tmp_str[i + p[i]]:
                p[i] += 1

            if r &lt; p[i] + i:
                r = p[i] + i
                c = i

            if max_len &lt; p[i] - 1 and (c - p[c]) // 2 == 0:
                max_len = p[i] - 1

        return &quot;&quot;.join([x for x in s[:max_len - 1:-1]]) + s
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">func preprocess(s string) string {
	result := &quot;^#&quot;

	for _, ch := range s {
		result += string(ch) + &quot;#&quot;
	}

	return result + &quot;$&quot;
}

func shortestPalindrome(s string) string {
	if len(s) == 0 {
		return &quot;&quot;
	}

	tmpStr := preprocess(s)
	strLen := len(tmpStr)
	p := make([]int, strLen)
	C, R := 0, 0
	maxLen := 0

	for i := 1; i &lt; strLen-1; i++ {
		if R &gt; i {
			p[i] = min(p[2*C-i], R-i)
		} else {
			p[i] = 1
		}

		for tmpStr[i-p[i]] == tmpStr[i+p[i]] {
			p[i]++
		}

		if R &lt; p[i]+i {
			R = p[i] + i
			C = i
		}

		if maxLen &lt; p[i]-1 &amp;&amp; (C-p[C])/2 == 0 {
			maxLen = p[i] - 1
		}
	}

	result := &quot;&quot;
	for i := len(s) - 1; i &gt;= maxLen; i-- {
		result += string(s[i])
	}

	return result + s
}

func min(a, b int) int {
	if a &lt; b {
		return a
	}
	return b
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Manacher算法]]></title>
        <id>https://zcybupt.github.io/post/Manacher/</id>
        <link href="https://zcybupt.github.io/post/Manacher/">
        </link>
        <updated>2020-07-20T02:58:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-5">题目(<a href="https://leetcode-cn.com/problems/longest-palindromic-substring/">LeetCode #5</a>)</h2>
<p>给定一个字符串 <code>s</code>，找到 <code>s</code> 中最长的回文子串。你可以假设 <code>s</code> 的最大长度为 1000。</p>
<p><strong>示例 1：</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;babad&quot;<br>
<strong>输出:</strong> &quot;bab&quot;<br>
<strong>注意:</strong> &quot;aba&quot; 也是一个有效答案。</p>
</blockquote>
<p><strong>示例 2：</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;cbbd&quot;<br>
<strong>输出:</strong> &quot;bb&quot;</p>
</blockquote>
<h2 id="题解">题解</h2>
<h3 id="扩展中心法">扩展中心法</h3>
<p>遍历字符串，由每个字符串向两侧扩展逐一比对。</p>
<p>时间复杂度为 <code>O(n²)</code> 。</p>
<pre><code class="language-java">public class Solution {
    public static String longestPalindrome(String s) {
        if (s == null || s.length() == 0) return &quot;&quot;;
        int start = 0, end = 0;

        for (int i = 0; i &lt; s.length(); i++) {
            int len1 = expandAroundCenter(s, i, i);
            int len2 = expandAroundCenter(s, i, i + 1);
            int len = Math.max(len1, len2);

            if (len &gt; end - start) {
                start = i - (len - 1) / 2;
                end = i + len / 2;
            }
        }

        return s.substring(start, end + 1);
    }

    public static int expandAroundCenter(String s, int left, int right) {
        while (left &gt;= 0 &amp;&amp; right &lt; s.length() &amp;&amp; s.charAt(left) == s.charAt(right)) {
            left--; right++;
        }

        return right - left - 1;
    }
}
</code></pre>
<h3 id="manacher算法">Manacher算法</h3>
<h4 id="预处理">预处理</h4>
<p>在原始字符串的每个字符的左右两边都加上特殊字符，使处理后的字符串长度均为奇数，以此来解决偶回文时用扩展法不能找到回文中心而导致的找不到回文子串的问题。</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721172042.png" alt="处理前" loading="lazy"></figure>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721172128.png" alt="处理后" loading="lazy"></figure>
<h4 id="最长回文子串长度">最长回文子串长度</h4>
<p>下表中 <code>p</code> 数组存储的是以各字符为中心的回文子串的最大长度。观察可知，预处理后的字符串的最长回文子串的回文半径减 <code>1</code> 即为原字符串的最长回文子串长度。以字符串 <code>cabbaf</code> 为例，处理后的最长回文子串的回文半径为 <code>p[6] = 5</code> ，<code>p[6] - 1 = 4</code> 即为原字符串的最长回文子串 <code>abba</code> 的长度。</p>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721172934.png" alt="" loading="lazy"></figure>
<p>所以原字符串的最长回文子串长度 <code>maxLen = p[i] - 1</code></p>
<h4 id="最长回文子串的起始索引">最长回文子串的起始索引</h4>
<p>在 <code>#c#a#b#b#a#f#</code> 中，<code>p[6] = 5</code> ，用索引 <code>i</code> 减去最长回文半径 <code>p[i]</code> 即可得到原字符串的最长回文子串的起始索引，即 <code>i - p[i] = 6 - 5 = 1</code> ；但对奇回文 <code>aba</code> 来说，用这种方法得到的结果为 <code>3 - p[3] = 3 - 4 = -1</code> ，下标越界。</p>
<p>为解决下标越界的问题，可在预处理后的字符串前再添加一个特殊字符，而为了使字符串长度保持为奇数，其后也需添加另一个字符。而原字符串的最长回文子串的起始索引变为 <code>(i - p[i]) / 2</code> 。</p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721180408.png" alt="" loading="lazy"></figure>
<p>对 <code>cabbaf</code> 来说，原字符串的最长回文子串的起始索引为 <code>(7 - p[7]) / 2 = (7 - 5) / 2 = 1</code></p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721180435.png" alt="" loading="lazy"></figure>
<p>对 <code>aba</code> 来说，原字符串的最长回文子串的起始索引为 <code>(3 - p[3]) / 2 = (3 - 3) / 2 = 0</code></p>
<p>故原字符串的最长回文子串的起始索引可表示为 <code>index = (i - p[i]) / 2</code></p>
<h4 id="计算-p-数组">计算 p 数组</h4>
<p>定义两个变量 <code>C</code> 和 <code>R</code> ，分别为某一回文子串的中心字符的索引和右边界。如对 <code>^#c#a#b#b#a#f#$</code> 中的 <code>#a#b#b#a#</code> 来说，<code>C = 7</code> ，<code>p[C] = 5</code> ，以 <code>7</code> 为中心的回文子串的右边界即为 <code>R = C + p[C] = 12</code> ；当 <code>C = 12</code> 时，以 <code>12</code> 为中心的回文子串的右边界即为 <code>R = C + p[C] = 12 + 2 = 14</code> 。</p>
<p>故可得回文子串右边界与其半径之间的关系为：<code>R = C + p[C]</code></p>
<figure data-type="image" tabindex="6"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721192607.png" alt="" loading="lazy"></figure>
<p>由于回文子串关于 <code>C</code> 对称，由该子串上某位置 <code>i</code> 可得其对称位置 <code>j</code> ，由 <code>i + j = 2 * C</code> 知 <code>j = 2 * C - i</code> 。</p>
<ul>
<li>若以 <code>j</code> 为中心的回文子串的右边界在 <code>R</code> 以内，即 <code>j + p[j] &lt; R</code> 时，由对称性可知，<code>p[j] = p[i]</code> ；</li>
<li>若以 <code>j</code> 为中心的回文子串的右边界大于 <code>R</code> ，即 <code>j + p[j] &gt; R</code> 时，图中红色部分为关于 <code>j</code> 对称的回文子串，故此时有 <code>p[j] = R - j</code> ；</li>
<li>若 <code>j + p[j] &gt;= R</code> ，则令 <code>p[j] = 1</code> ，此时也需更新 <code>C</code> 和 <code>R</code> ，继续向后用扩展中心法计算 <code>p</code> 各位置的值。</li>
</ul>
<h5 id="java">Java</h5>
<pre><code class="language-java">public class Manacher {
    public static String preprocess(String s) {
        String result = &quot;^&quot;;
        for (int i = 0; i &lt; s.length(); i++) {
            result += &quot;#&quot; + s.charAt(i);
        }

        return result + &quot;#$&quot;;
    }

    public static String longestPalindrome(String s) {
        if (s.length() == 0) return &quot;&quot;;

        String tmpStr = preprocess(s);
        int len = tmpStr.length();
        int[] p = new int[len];
        int C = 0, R = 0;
        int maxLen = -1;    // 最长回文子串长度
        int index = 0;      // 最长回文子串中心索引

        for (int i = 1; i &lt; len - 1; i++) {
            p[i] = R &gt; i ? Math.min(p[2 * C - i], R - i) : 1;

            while (tmpStr.charAt(i + p[i]) == tmpStr.charAt(i - p[i]))
                p[i]++;

            if (R &lt; p[i] + i) {
                R = p[i] + i;
                C = i;
            }

            if (maxLen &lt; p[i] - 1) {
                maxLen = p[i] - 1;
                index = i;
            }
        }

        int start = (index - maxLen) / 2;   // (i - p[i]) / 2
        return s.substring(start, start + maxLen);
    }
}
</code></pre>
<h5 id="python">Python</h5>
<pre><code class="language-python">class Manacher:
    def longest_palindrome(self, s: str) -&gt; str:
        if s is None or len(s) == 0: return &quot;&quot;

        tmp_str = '^#' + ''.join([x + '#' for x in s]) + &quot;$&quot;
        length = len(tmp_str)
        p = [0] * length
        c, r = 0, 0
        max_len = -1
        index = 0

        for i in range(1, length - 1):
            p[i] = min(p[2 * c - i], r - i) if r &gt; i else 1

            while tmp_str[i + p[i]] == tmp_str[i - p[i]]:
                p[i] += 1

            if r &lt; i + p[i]:
                r = i + p[i]
                c = i

            if max_len &lt; p[i] - 1:
                max_len = p[i] - 1
                index = i

        start = (index - max_len) // 2
        return s[start: start + max_len]
</code></pre>
<h5 id="go">Go</h5>
<pre><code class="language-go">func preprocess(s string) string {
	result := &quot;^#&quot;

	for _, ch := range s {
		result += string(ch) + &quot;#&quot;
	}

	return result + &quot;$&quot;
}

func longestPalindrome(s string) string {
	if len(s) == 0 {
		return &quot;&quot;
	}

	tmpStr := preprocess(s)
	length := len(tmpStr)
	p := make([]int, length)
	C, R := 0, 0
	maxLen := 0
	index := 0

	for i := 1; i &lt; length-1; i++ {
		if R &gt; i {
			p[i] = int(math.Min(float64(p[2*C-i]), float64(R-i)))
		} else {
			p[i] = 1
		}

		for tmpStr[i+p[i]] == tmpStr[i-p[i]] {
			p[i]++
		}

		if R &lt; i+p[i] {
			R = i + p[i]
			C = i
		}

		if maxLen &lt; p[i]-1 {
			maxLen = p[i] - 1
			index = i
		}
	}

	start := (index - maxLen) / 2
	return s[start : start+maxLen]
}
</code></pre>
<h3 id="参考资料">参考资料</h3>
<p><a href="https://www.jianshu.com/p/392172762e55">马拉车算法</a></p>
<p><a href="https://leetcode.wang/leetCode-5-Longest-Palindromic-Substring.html">leetcode.wang</a></p>
]]></content>
    </entry>
</feed>