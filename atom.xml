<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://zcybupt.github.io</id>
    <title>CODEFANCIER</title>
    <updated>2020-07-06T01:13:09.717Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://zcybupt.github.io"/>
    <link rel="self" href="https://zcybupt.github.io/atom.xml"/>
    <logo>https://zcybupt.github.io/images/avatar.png</logo>
    <icon>https://zcybupt.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, CODEFANCIER</rights>
    <entry>
        <title type="html"><![CDATA[母牛数量问题]]></title>
        <id>https://zcybupt.github.io/post/CowNumber/</id>
        <link href="https://zcybupt.github.io/post/CowNumber/">
        </link>
        <updated>2020-07-04T12:15:51.000Z</updated>
        <content type="html"><![CDATA[<h3 id="题目">题目</h3>
<p>母牛每年生一头母牛, 新出生的母牛成长三年后也能每年生一头母牛, 假设牛不会死. 求 <code>N</code> 年后母牛的数量</p>
<h3 id="题解">题解</h3>
<h4 id="递归">递归</h4>
<p>归纳得 <code>F(N) = F(N-1) + F(N-3)</code></p>
<p>该式实际上可解释为每年牛的总数等于前一年牛的总数 <code>F(N-1)</code> 加上三年前牛的总数 <code>F(N-3)</code> , 其中 <code>F(N-3)</code> 即为每年新增的牛的数量.</p>
<p>若规定牛的寿命为 10 年, 减去 <code>F(N-10)</code> 即可</p>
<h5 id="java">Java</h5>
<pre><code class="language-java">public class CowNumber {
    public static int getCowNum(int N) {
        if (N &lt;= 2) return N + 1;
        return getCowNum(N - 1) + getCowNum(N - 3);
    }

    public static void main(String[] args) {
        System.out.println(getCowNum(8));
    }
}
</code></pre>
<pre><code class="language-java">28
</code></pre>
<h5 id="python">Python</h5>
<pre><code class="language-python">def get_cow_number(n):
    if n &lt;= 2:
        return n + 1

    return get_cow_number(n - 1) + get_cow_number(n - 3)
</code></pre>
<h5 id="go">Go</h5>
<pre><code class="language-go">func getCowNum(n int) int {
	if n &lt;= 2 {
		return n + 1
	}

	return getCowNum(n-1) + getCowNum(n-3)
}
</code></pre>
<h4 id="斐波那契数列的矩阵求法">斐波那契数列的矩阵求法</h4>
<p>以上算法的时间复杂度为 <code>O(N²)</code> 本题可参考<a href="https://www.geeksforgeeks.org/matrix-exponentiation/">斐波那契数列的矩阵求法</a>, 该算法时间复杂度仅为 <code>O(logN)</code></p>
<p>推导如下:</p>
<pre><code>F(n) = a*F(n-1) + b*F(n-2) + c*F(n-3)
F(n-1) = F(n-1)
F(n-2) = F(n-2)
</code></pre>
<p>本题中 a = 1, b = 0, c = 1</p>
<p>故可得</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200705153235.png" alt="" loading="lazy"></figure>
<h5 id="python-2">Python</h5>
<pre><code class="language-python">import numpy as np

def fib_matrix(n):
    if n &lt;= 2:
        return n + 1
    result = np.mat([
        [1, 0, 1],
        [1, 0, 0],
        [0, 1, 0]
    ], dtype='float64') ** (n - 2) * np.mat([3, 2, 1]).T
    return int(result[0, 0])
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[字符串的全排列]]></title>
        <id>https://zcybupt.github.io/post/PrintPermutations/</id>
        <link href="https://zcybupt.github.io/post/PrintPermutations/">
        </link>
        <updated>2020-07-02T11:54:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="题目">题目</h3>
<p>打印一个字符串的全部排列</p>
<h3 id="题解">题解</h3>
<p>每次递归都将后续字符与当前字符交换, 如第 <code>i</code> 次递归, 将 <code>i</code> 位置之后的字符分别与 <code>i</code> 处字符交换</p>
<p>若要去重使用集合即可</p>
<h4 id="java">Java</h4>
<pre><code class="language-java">public class PrintPermutations {
    public static void printPermutations(String str) {
        char[] chars = str.toCharArray();
        process(chars, 0);
    }

    public static void process(char[] chars, int i) {
        if (i == chars.length) {
            System.out.println(String.valueOf(chars));
        }

        for (int j = i; j &lt; chars.length; j++) {
            swap(chars, i, j);
            process1(chars, i + 1);
        }
    }

    public static void swap(char[] chars, int i, int j) {
        char tmp = chars[i];
        chars[i] = chars[j];
        chars[j] = tmp;
    }
}
</code></pre>
<h4 id="python">Python</h4>
<pre><code class="language-python">def print_permutations(str_input):
    char_arr = [i for i in str_input]
    process(char_arr, 0)


def process(char_arr, i):
    arr_len = len(char_arr)
    if i == arr_len:
        print(''.join(char_arr))

    for j in range(i, arr_len):
        char_arr[i], char_arr[j] = char_arr[j], char_arr[i]
        process(char_arr, i + 1)
</code></pre>
<h4 id="go">Go</h4>
<pre><code class="language-go">func printPermutations(input string) {
	charArr := []byte(input)
	process(charArr, 0)
}

func process(charArr []byte, i int) {
	arrLen := len(charArr)
	if i == arrLen {
		fmt.Println(string(charArr))
	}

	for j := i; j &lt; arrLen; j++ {
		charArr[i], charArr[j] = charArr[j], charArr[i]
		process(charArr, i+1)
	}
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[打印字符串的子序列]]></title>
        <id>https://zcybupt.github.io/post/Subsequence/</id>
        <link href="https://zcybupt.github.io/post/Subsequence/">
        </link>
        <updated>2020-06-28T09:10:02.000Z</updated>
        <content type="html"><![CDATA[<h3 id="题目">题目</h3>
<p>打印一个字符串的全部子序列, 包括空字符串</p>
<h3 id="题解">题解</h3>
<p>迭代过程中, 要么当前字符保留, 要么略过</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200628200644.png" alt="" loading="lazy"></figure>
<h4 id="java">Java</h4>
<pre><code class="language-java">public class Subsequence {
    public static void printAllSub(String str) {
        char[] chs = str.toCharArray();
        process(chs, 0, &quot;&quot;);
        System.out.println(&quot;&quot;);
    }

    public static void process(char[] chs, int i, String pre) {
        if (i == chs.length) {
            if (!pre.equals(&quot;&quot;)) {
                System.out.println(pre);
            }
            return;
        }
        process(chs, i + 1, pre + String.valueOf(chs[i]));
        process(chs, i + 1, pre);
    }

    public static void main(String[] args) {
        String test = &quot;abc&quot;;
        printAllSub(test);
    }
}
</code></pre>
<pre><code>abc
ab
ac
a
bc
b
c

</code></pre>
<h4 id="python">Python</h4>
<pre><code class="language-python">def print_subsequence(string):
    str_arr = [i for i in string]
    process(str_arr, 0, '')
    print()


def process(str_arr, i, pre):
    if i == len(str_arr):
        if pre != '':
            print(pre)
        return
    process(str_arr, i + 1, pre + str_arr[i])
    process(str_arr, i + 1, pre)
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[汉诺塔问题]]></title>
        <id>https://zcybupt.github.io/post/Hannoi/</id>
        <link href="https://zcybupt.github.io/post/Hannoi/">
        </link>
        <updated>2020-06-27T11:46:36.000Z</updated>
        <content type="html"><![CDATA[<h3 id="题目">题目</h3>
<p>打印 <code>n</code> 层汉诺塔从最左边移动到最右边的全部过程</p>
<h3 id="题解">题解</h3>
<p>假定三根柱子分别为 <code>from</code> , <code>to</code> 和 <code>help</code> , 该问题可划分为 3 个步骤</p>
<ol>
<li>将 <code>1~(n-1)</code> 从 <code>from</code> 移动到 <code>help</code> 上</li>
<li>将 <code>n</code> 从 <code>from</code> 移动到 <code>to</code> 上</li>
<li>将 <code>1~(n-1)</code> 从 <code>help</code> 移动到 <code>to</code> 上</li>
</ol>
<p>其中, 将 <code>1~(n-1)</code> 从 <code>help</code> 移动到 <code>to</code> 的过程又可分为多个子问题, 需不断调整三个柱子的角色.</p>
<h4 id="java">Java</h4>
<pre><code class="language-java">public class Hannoi {
    public static void hannoi(int n, String from, String to, String help) {
        if (n == 1) {
            System.out.println(&quot;move &quot; + 1 + &quot; from &quot; + from + &quot; to &quot; + to);
        } else {
            hannoi(n - 1, from, help, to);
            System.out.println(&quot;move &quot; + n + &quot; from &quot; + from + &quot; to &quot; + to);
            hannoi(n - 1, help, to, from);
        }
    }

    public static void main(String[] args) {
        int n = 3;
        hannoi(3, &quot;left&quot;, &quot;right&quot;, &quot;mid&quot;);
    }
}
</code></pre>
<h4 id="python">Python</h4>
<pre><code class="language-python">def hannoi(n, source, target, assist):
    if n == 1:
        print('move 1 from ' + source + ' to ' + target)
    else:
        hannoi(n - 1, source, assist, target)
        print('move ' + str(n) + ' from ' + source + ' to ' + target)
        hannoi(n - 1, assist, target, source)
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[限重最大值问题]]></title>
        <id>https://zcybupt.github.io/post/Knapsack/</id>
        <link href="https://zcybupt.github.io/post/Knapsack/">
        </link>
        <updated>2020-06-26T11:37:58.000Z</updated>
        <content type="html"><![CDATA[<h3 id="题目">题目</h3>
<p>给定两个数组 <code>w</code> 和 <code>v</code> ，两个数组长度相等，<code>w[i]</code> 表示第i件商品的 重量，<code>v[i]</code> 表示第i件商品的价值。 再给定一个整数 <code>bag</code> ，要求你挑选商品的重量加起来一定不能超过 <code>bag</code> ，返回满足这个条件下，你能获得的最大价值。</p>
<h3 id="题解">题解</h3>
<p>解法类似<a href="https://zcybupt.github.io/post/SumToTarget/">上一篇博客</a></p>
<h4 id="暴力递归">暴力递归</h4>
<p>穷举所有可能的结果, 迭代过程中判断累计重量 <code>acc_weight</code> 是否超过 <code>bag</code> , 若超过则返回 <code>Integer.MIN_VALUE</code> , 相当于直接舍弃该分支. 另一个迭代的基础条件为当 <code>i</code> 达到 <code>w.length</code> 时, 返回 <code>0</code> , 因为第 <code>i</code> 次递归的返回值为当前商品的价值 <code>v[i]</code> 加上后续商品的价值与只有后续商品价值二者中的较大者, <code>i</code> 等于 <code>w.length</code> 时, 没有后续商品, 故直接返回 <code>0</code> .</p>
<h5 id="java">Java</h5>
<pre><code class="language-java">public class Knapsack {
    public static int maxValue(int[] w, int[] v, int i, int accWeight, int bag) {
        if (accWeight &gt; bag) {
            return Integer.MIN_VALUE;   // 重量超过 bag, 舍弃
        }

        if (i == w.length) {
            return 0;
        }

        return Math.max(
                maxValue(w, v, i + 1, accWeight, bag),
                v[i] + maxValue(w, v, i + 1, accWeight + w[i], bag)
        );
    }

    public static void main(String[] args) {
        int[] w = {3, 2, 4, 7};
        int[] v = {5, 6, 3, 19};
        int bag = 11;

        System.out.println(maxValue(w, v, 0, 0, bag));
    }
}
</code></pre>
<h5 id="python">Python</h5>
<pre><code class="language-python">def max_value(w, v, i, acc_weight, bag):
    if acc_weight &gt; bag: return float('-inf')

    if i == len(w): return 0

    return max(
        max_value(w, v, i + 1, acc_weight, bag),
        v[i] + max_value(w, v, i + 1, acc_weight + w[i], bag)
    )
</code></pre>
<h4 id="动态规划">动态规划</h4>
<p>此类<strong>无后效性</strong>的递归问题均可转化为动态递归.</p>
<h5 id="步骤">步骤</h5>
<ol>
<li>分析可变参数, 求出表空间</li>
<li>确定最终状态</li>
<li>根据 base case 确定初始状态</li>
<li>分析一个普遍位置的依赖关系</li>
<li>根据依赖顺序, 逆序求出整张表</li>
</ol>
<h5 id="java-2">Java</h5>
<pre><code class="language-python">public class Knapsack {
    public static int maxValue(int[] w, int[] v, int bag) {
        int[][] dp = new int[w.length + 1][bag + 1];

        for (int i = 0; i &lt;= bag; i++) {
            dp[w.length][i] = 0;
        }

        for (int i = w.length - 1; i &gt;= 0; i--) {
            for (int j = bag; j &gt;= 0; j--) {
                dp[i][j] = dp[i + 1][j];
                if (j + w[i] &lt;= bag)
                    dp[i][j] = Math.max(
                            dp[i + 1][j],
                            dp[i + 1][j + w[i]] + v[i]
                    );
            }
        }

        return dp[0][0];
    }
}
</code></pre>
<h5 id="python-2">Python</h5>
<pre><code class="language-python">def max_value(w, v, bag):
    dp = [[0] * (bag + 1) for _ in range(len(w) + 1)]

    for i in range(len(w) - 1, -1, -1):
        for j in range(bag, -1, -1):
            dp[i][j] = dp[i + 1][j]
            if j + w[i] &lt;= bag:
                dp[i][j] = max(
                    dp[i + 1][j],
                    dp[i + 1][j + w[i]] + v[i]
                )
                
    return dp[0][0]
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[数组累加得到目标值]]></title>
        <id>https://zcybupt.github.io/post/SumToTarget/</id>
        <link href="https://zcybupt.github.io/post/SumToTarget/">
        </link>
        <updated>2020-06-24T12:25:25.000Z</updated>
        <content type="html"><![CDATA[<h3 id="题目">题目</h3>
<p>给定一个<strong>正整数</strong>数组 <code>arr</code> 和一个整数 <code>target</code> , 如果可以任意选择 <code>arr</code> 中的数字, 求能否累加得到 <code>target</code> .</p>
<h3 id="题解">题解</h3>
<h4 id="暴力解法">暴力解法</h4>
<p>穷举所有可能的和, 判断是否含有 <code>target</code> , 穷举过程如下, 其中中间变量格式为 <code>(i, sum)</code> , <code>i</code> 为索引, <code>sum</code> 为当前的累加和, 左支表示累加 <code>arr[i]</code> , 右支表示跳过该值.</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200624204631.png" alt="" loading="lazy"></figure>
<h5 id="java">Java</h5>
<pre><code class="language-java">public class SumToTarget {
    public static boolean isSum(int[] arr, int i, int sum, int target) {
        if (i == arr.length)
            return sum == target;

        // 左分支 || 右分支
        return isSum(arr, i + 1, sum + arr[i], target) || isSum(arr, i + 1, sum, target);
    }

    public static void main(String[] args) {
        int[] arr = {3, 2, 7};
        int target = 10;
        System.out.println(isSum(arr, 0, 0, target));
    }
}
</code></pre>
<h5 id="python">Python</h5>
<pre><code class="language-python">def sum_to_target(arr, i, cur_sum, target):
    if i == len(arr):
        return cur_sum == target

    return sum_to_target(arr, i + 1, cur_sum + arr[i], target) or \
		   sum_to_target(arr, i + 1, cur_sum, target)


if __name__ == '__main__':
    arr = [2, 3, 7]
    target = 10
    print(sum_to_target(arr, 0, 0, target))
</code></pre>
<h4 id="动态规划">动态规划</h4>
<p>将暴力递归改进为动态规划时, 无需考虑原题目的含义, 只需找到迭代时各变量之间的依赖关系即可. 递归到动态规划的转化方法:</p>
<ol>
<li>递归函数有 <code>n</code> 个参数, 就定义一个 <code>n</code> 维的数组</li>
<li>数组的下标是递归函数参数的取值范围</li>
<li>数组元素的值是递归函数的返回值, 这样就可以从边界值开始， 逐步填充数组，相当于计算递归函数值的逆过程。</li>
</ol>
<pre><code class="language-java">return isSum(arr, i + 1, sum + arr[i], target) || isSum(arr, i + 1, sum, target);
</code></pre>
<p>由上述代码知递归函数第 <code>i</code> 次递归的结果只依赖于 <code>i+1</code> 和 <code>sum</code> , 故需定义一个 <code>i+1</code> 和 <code>sum</code> 状态关系的数组. 递归函数参数 <code>i</code> 的取值范围为 <code>0~arr.length</code> , <code>sum</code> 的范围为全部元素都不取到全部元素的和, 以 <code>[3, 2, 7]</code> 为例即为 <code>0~12</code> . 构成的数组如下:</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200625180738.png" alt="" loading="lazy"></figure>
<p>任意一格 <code>(i, sum)</code> 的值仅由 <code>(i+1, sum)</code> 和 <code>(i+1, sum+arr[i])</code> 决定. 如上表所示, <code>(1, 3)</code> 的值取决于 <code>(1+1, 3)</code> 和 <code>(1+1, 3+arr[1])</code> , 其中 <code>arr[1]</code> 为 2, 故 <code>(1, 3)</code> 的真值取决于 <code>(2, 3)</code> 和 <code>(2, 5)</code> 的真值.</p>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200625181137.png" alt="" loading="lazy"></figure>
<p>最终结果返回表中 <code>(0, 0)</code> 位置即可.</p>
<h5 id="java-2">Java</h5>
<pre><code class="language-java">public class SumToTarget {
    public static boolean isSum(int[] arr, int target) {
        // 本题只要有一列达到 target 即可, 故 target + 1 列即可
        boolean[][] dp = new boolean[arr.length + 1][target + 1];
        // target 列必为 True
        dp[i][target] = true;

        for (int i = arr.length - 1; i &gt;= 0; i--) {
            for (int j = target; j &gt;= 0; j--) {
                dp[i][j] = dp[i + 1][j];
                if (j + arr[i] &lt;= target) {
                    dp[i][j] = dp[i][j] || dp[i + 1][j + arr[i]];
                }
            }
        }
        return dp[0][0];
    }
}
</code></pre>
<h5 id="python-2">Python</h5>
<pre><code class="language-python">def sum_to_target(arr, target):
    row = len(arr)
    dp = [[False] * (target + 1) for _ in range(row + 1)]

    dp[row][target] = True

    for i in range(row - 1, -1, -1):
        for j in range(target, -1, -1):
            if j + arr[i] &lt;= target:
                dp[i][j] = dp[i + 1][j] or dp[i + 1][j + arr[i]]
            else:
                dp[i][j] = dp[i + 1][j]

    return dp[0][0]
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[二维数组最小路径和]]></title>
        <id>https://zcybupt.github.io/post/MinimalPathSum/</id>
        <link href="https://zcybupt.github.io/post/MinimalPathSum/">
        </link>
        <updated>2020-06-24T00:37:41.000Z</updated>
        <content type="html"><![CDATA[<h3 id="题目-leetcode-64">题目 (<a href="https://leetcode-cn.com/problems/minimum-path-sum">LeetCode #64</a>)</h3>
<p>给定一个包含非负整数的 <code>m x n</code> 网格，请找出一条从左上角到右下角的路径，使得路径上的数字总和为最小。</p>
<p><strong>说明:</strong> 每次只能向下或者向右移动一步。</p>
<p><strong>示例:</strong></p>
<p>输入:</p>
<pre><code class="language-python">[
    [1, 3, 1],
    [1, 5, 1],
    [4, 2, 1]
]
</code></pre>
<p>输出: 7</p>
<h4 id="题解">题解</h4>
<h5 id="暴力递归">暴力递归</h5>
<p>从左上角到右下角, 每次移动只有向右或向下两种可能, 需递归地找出左右两个方向到右下角路径和较小者即可.</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200624102923.png" alt="" loading="lazy"></figure>
<p>暴力递归不记录子状态的结果, <code>F(E)</code> , <code>F(H)</code>, <code>F(F)</code> 进行了多次重复计算, 占用较多空间, 数据较多时会超时, 可改用计划搜索的方法进行优化.</p>
<h6 id="java">Java</h6>
<pre><code class="language-java">public class MinimalPathSum {
	public int minPathSum(int[][] grid) {
        if (grid == null || grid.length &lt; 1 || grid[0] == null || grid[0].length &lt; 1) {
            return 0;
        }
        return process(grid, 0, 0);
    }

    public static int process(int[][] grid, int i, int j) {
        if (i == grid.length - 1 &amp;&amp; j == grid[0].length - 1)
            return grid[i][j];

        if (i == grid.length - 1)
            return grid[i][j] + process(grid, i, j + 1);

        if (j == grid[0].length - 1)
            return grid[i][j] + process(grid, i + 1, j);

        return grid[i][j] + Math.min(process(grid, i, j + 1), process(grid, i + 1, j));
    }
}
</code></pre>
<h6 id="python">Python</h6>
<pre><code class="language-python">def get_minimal_path_sum(grid):
    if not grid or len(grid) &lt; 1 or not grid[0] or len(grid[0]) &lt; 1:
        return 0
    return process(grid, 0, 0)


def process(grid, i, j):
    if i == len(grid) - 1 and j == len(grid[0]) - 1:
        return grid[i][j]

    if i == len(grid) - 1:
        return grid[i][j] + process(grid, i, j + 1)
    if j == len(grid[0]) - 1:
        return grid[i][j] + process(grid, i + 1, j)

    return grid[i][j] + min(process(grid, i + 1, j), process(grid, i, j + 1))

</code></pre>
<h5 id="计划搜索">计划搜索</h5>
<p>将每个子状态的运算结果存入缓存中, 若之前计算过则直接返回计算结果.</p>
<h6 id="java-2">Java</h6>
<pre><code class="language-java">public class MinimalPathSum {
    static HashMap&lt;String, Integer&gt; cache;

    public static int minimalPathSum(int[][] grid) {
        if (grid == null || grid.length &lt; 1 || grid[0] == null || grid[0].length &lt; 1) {
            return 0;
        }

        cache = new HashMap&lt;&gt;();
        return process(grid, 0, 0);
    }

    public static int process(int[][] grid, int i, int j) {
        String key = i + &quot;_&quot; + j;
        if (cache.containsKey(key)) {
            return cache.get(key);	// 存入 Map 时, 已经包含了 grid[i][j] 的值, 故不需重复加
        }

        int result = 0;
        if (i == grid.length - 1 &amp;&amp; j == grid[0].length - 1) {
            result = grid[i][j];
        } else if (i == grid.length - 1) {
            result = grid[i][j] + process(grid, i, j + 1);
        } else if (j == grid[0].length - 1) {
            result = grid[i][j] + process(grid, i + 1, j);
        } else {
            result = grid[i][j] + Math.min(process(grid, i + 1, j), process(grid, i, j + 1));
        }

        cache.put(key, result);
        return result;
    }
}
</code></pre>
<h6 id="python-2">Python</h6>
<pre><code class="language-python">class MinimalPathSum:
    def __init__(self):
        self.result_dict = None

    def get_minimal_path_sum(self, grid):
        if not grid or len(grid) &lt; 1 or not grid[0] or len(grid[0]) &lt; 1:
            return 0

        self.result_dict = dict()
        return self.process(grid, 0, 0)

    def process(self, grid, i, j):
        key = str(i) + &quot;_&quot; + str(j)
        if self.result_dict.get(key):
            return self.result_dict.get(key)

        if i == len(grid) - 1 and j == len(grid[0]) - 1:
            result = grid[i][j]
        elif i == len(grid) - 1:
            result = grid[i][j] + self.process(grid, i, j + 1)
        elif j == len(grid[0]) - 1:
            result = grid[i][j] + self.process(grid, i + 1, j)
        else:
            result = grid[i][j] + min(self.process(grid, i + 1, j), self.process(grid, i, j + 1))

        self.result_dict[key] = result
        return result
</code></pre>
<h5 id="动态规划">动态规划</h5>
<p>左上角到右下角的最短距离与右下角到左上角最短距离相同, 为便于表示, 以下改为求右下角到左上角的最短距离.</p>
<p>由以上计划搜索部分知, 当矩阵中某点的位置即 <code>i</code> 和 <code>j</code> 的值确定时, 该点到左上角的最短路径就确定了, 故可单独求出矩阵中各元素到左上角的最短路径, 每个元素的最短路径值仅由其相邻的左边和上边元素决定, 其对应关系如下例, 故输出矩阵中的右下角即为左上角到右下角的最短距离.</p>
<p><strong>输入</strong></p>
<pre><code class="language-java">3	8	6	0	5	9	9	6	3	4	0	5	7	3	9	3	
0	9	2	5	5	4	9	1	4	6	9	5	6	7	3	2	
8	2	2	3	3	3	1	6	9	1	1	6	6	2	1	9	
1	3	6	9	9	5	0	3	4	9	1	0	9	6	2	7	
8	6	2	2	1	3	0	0	7	2	7	5	4	8	4	8	
4	1	9	5	8	9	9	2	0	2	5	1	8	7	0	9	
6	2	1	7	8	1	8	5	5	7	0	2	5	7	2	1	
8	1	7	6	2	8	1	2	2	6	4	0	5	4	1	3	
9	2	1	7	6	1	4	3	8	6	5	5	3	9	7	3	
0	6	0	2	4	3	7	6	1	3	8	6	9	0	0	8	
4	3	7	2	4	3	6	4	0	3	9	5	3	6	9	3	
2	1	8	8	4	5	6	5	8	7	3	7	7	5	8	3	
0	7	6	6	1	2	0	3	5	0	8	0	8	7	4	3	
0	4	3	4	9	0	1	9	7	7	8	6	4	6	9	5	
6	5	1	9	9	2	2	7	4	2	7	2	2	3	7	2	
7	1	9	6	1	2	7	0	9	6	6	4	4	5	1	0	
3	4	9	2	8	3	1	2	6	9	7	0	2	4	2	0	
5	1	8	8	4	6	8	5	2	4	1	6	2	2	9	7
</code></pre>
<p><strong>输出</strong></p>
<pre><code class="language-java">3	11	17	17	22	31	40	46	49	53	53	58	65	68	77	80	
3	12	14	19	24	28	37	38	42	48	57	62	68	75	78	80	
11	13	15	18	21	24	25	31	40	41	42	48	54	56	57	66	
12	15	21	27	30	29	25	28	32	41	42	42	51	57	59	66	
20	21	23	25	26	29	25	25	32	34	41	46	50	58	62	70	
24	22	31	30	34	38	34	27	27	29	34	35	43	50	50	59	
30	24	25	32	40	39	42	32	32	36	34	36	41	48	50	51	
38	25	32	38	40	47	43	34	34	40	38	36	41	45	46	49	
47	27	28	35	41	42	46	37	42	46	43	41	44	53	53	52	
47	33	28	30	34	37	44	43	43	46	51	47	53	53	53	60	
51	36	35	32	36	39	45	47	43	46	55	52	55	59	62	63	
53	37	43	40	40	44	50	52	51	53	56	59	62	64	70	66	
53	44	49	46	41	43	43	46	51	51	59	59	67	71	74	69	
53	48	51	50	50	43	44	53	58	58	66	65	69	75	83	74	
59	53	52	59	59	45	46	53	57	59	66	67	69	72	79	76	
66	54	61	65	60	47	53	53	62	65	71	71	73	77	78	76	
69	58	67	67	68	50	51	53	59	68	75	71	73	77	79	76	
74	59	67	75	72	56	59	58	60	64	65	71	73	75	84	83
</code></pre>
<h6 id="java-3">Java</h6>
<pre><code class="language-java">public class MinimalPathSum {
	public static int dynamicProgramming(int[][] grid) {
        if (grid == null || grid.length &lt; 1 || grid[0] == null || grid[0].length &lt; 1) {
            return 0;
        }

        int row = grid.length;
        int column = grid[0].length;

        int[][] dp = new int[row][column];

        dp[0][0] = grid[0][0];

        for (int i = 1; i &lt; row; i++) {
            dp[i][0] = dp[i - 1][0] + grid[i][0];
        }

        for (int j = 1; j &lt; column; j++) {
            dp[0][j] = dp[0][j - 1] + grid[0][j];
        }

        for (int i = 1; i &lt; row; i++) {
            for (int j = 1; j &lt; column; j++) {
                dp[i][j] = grid[i][j] + Math.min(dp[i - 1][j], dp[i][j - 1]);
            }
        }

        for (int i = 0; i &lt; row; i++) {
            for (int j = 0; j &lt; column; j++) {
                System.out.print(dp[i][j] + &quot;\t&quot;);
            }
            System.out.println();
        }

        return dp[row - 1][column - 1];
    }
}
</code></pre>
<h6 id="python-3">Python</h6>
<pre><code class="language-python">class MinimalPathSum:
    def dynamic_programming(grid):
        if not grid or len(grid) &lt; 1 or not grid[0] or len(grid[0]) &lt; 1: return 0

        row = len(grid)
        column = len(grid[0])

        dp = [[0] * column for _ in range(row)]

        dp[0][0] = grid[0][0]

        for i in range(1, row):
            dp[i][0] = grid[i][0] + dp[i - 1][0]

        for j in range(1, column):
            dp[0][j] = grid[0][j] + dp[0][j - 1]

        for i in range(1, row):
            for j in range(1, column):
                dp[i][j] = min(dp[i - 1][j], dp[i][j - 1]) + grid[i][j]

        return dp[row - 1][column - 1]
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[最小生成树]]></title>
        <id>https://zcybupt.github.io/post/MinimumSpanningTree/</id>
        <link href="https://zcybupt.github.io/post/MinimumSpanningTree/">
        </link>
        <updated>2020-06-22T00:28:28.000Z</updated>
        <content type="html"><![CDATA[<p>最小生成树是一副连通加权无向图中一棵权值最小的生成树。一个连通图可能有多个生成树。当图中的边具有权值时，总会有一个生成树的边的权值之和小于或者等于其它生成树的边的权值之和 (<a href="%5Bhttps://zh.wikipedia.org/wiki/%E6%9C%80%E5%B0%8F%E7%94%9F%E6%88%90%E6%A0%91%5D(https://zh.wikipedia.org/wiki/%E6%9C%80%E5%B0%8F%E7%94%9F%E6%88%90%E6%A0%91)">Wikipedia</a>)。</p>
<p>常见算法有 Kruskal 算法和 Prim 算法</p>
<h4 id="kruskal-算法">Kruskal 算法</h4>
<p>按照边的权重顺序（从小到大）将边加入生成树中，但是若加入该边会与生成树形成环则不加入该边。直到树中含有顶点数减一条边为止。这些边组成的就是该图的最小生成树。</p>
<p>实现过程中, 可利用小根堆来依次返回权重最小的边, 利用<a href="https://zcybupt.github.io/post/UnionFind/">并查集</a>来避免生成树形成回路.</p>
<h5 id="java">Java</h5>
<p><strong>UnionFind</strong> 定义如下</p>
<pre><code class="language-java">class UnionFind {
    public HashMap&lt;GraphNode, GraphNode&gt; parentMap;
    public HashMap&lt;GraphNode, Integer&gt; sizeMap;

    public UnionFind(Collection&lt;GraphNode&gt; nodes) {
        parentMap = new HashMap&lt;&gt;();
        sizeMap = new HashMap&lt;&gt;();

        // 初始化并查集, 此时每个结点自成一个集合, 指针指向它本身
        for (GraphNode node : nodes) {
            parentMap.put(node, node);
            sizeMap.put(node, 1);
        }
    }

    /**
     * 找到代表结点并整理结点
     *
     * @param node 当前节点
     * @return 代表结点
     */
    private GraphNode findHead(GraphNode node) {
        GraphNode parent = parentMap.get(node);
        if (parent != node) {
            parent = findHead(parent);
        }

        parentMap.put(node, parent);    // 整理结点
        return parent;
    }

    public boolean isSameSet(GraphNode node1, GraphNode node2) {
        return findHead(node1) == findHead(node2);
    }

    public void union(GraphNode a, GraphNode b) {
        if (a == null || b == null)
            return;
        GraphNode aHead = findHead(a);
        GraphNode bHead = findHead(b);

        if (aHead == bHead)
            return;

        int aSetSize = sizeMap.get(aHead);
        int bSetSize = sizeMap.get(bHead);

        if (aSetSize &lt;= bSetSize) {
            parentMap.put(aHead, bHead);
            sizeMap.put(bHead, aSetSize + bSetSize);
        } else {
            parentMap.put(bHead, aHead);
            sizeMap.put(aHead, aSetSize + bSetSize);
        }
    }
}
</code></pre>
<p><strong>Kruskal</strong> 实现</p>
<pre><code class="language-java">public class KruskalMST {
    public static Set&lt;Edge&gt; kruskalMST(Graph graph) {
        UnionFind unionFind = new UnionFind(graph.nodes.values());
        PriorityQueue&lt;Edge&gt; priorityQueue = new PriorityQueue&lt;&gt;((a, b)-&gt;{
            return a.weight - b.weight;
        });

        priorityQueue.addAll(graph.edges);

        Set&lt;Edge&gt; result = new HashSet&lt;&gt;();
        while (!priorityQueue.isEmpty()) {
            Edge edge = priorityQueue.poll();
            if (!unionFind.isSameSet(edge.from, edge.to)) {
                result.add(edge);
                unionFind.union(edge.from, edge.to);
            }
        }

        return result;
    }
}
</code></pre>
<h5 id="python">Python</h5>
<p><code>UnionFindSet</code> 定义见<a href="https://zcybupt.github.io/post/UnionFind/#python">并查集</a></p>
<p>由于 <code>Edge</code> 对象不能够直接比较大小, 需重写 <code>Edge</code> 的比较函数, <code>__lt__</code> 对应 <code>&lt;</code> , <code>__gt__</code> 对应 <code>&gt;</code> , 只重写其中一个, 另一个会自动取反. 比较相等时结果相同.</p>
<pre><code class="language-python">class Edge:
    def __init__(self, weight, from_node, to_node):
        self.weight = weight
        self.from_node = from_node
        self.to_node = to_node

    def __lt__(self, other):
        # 重写 &lt; 比较函数
        return True if self.weight &lt; other.weight else False
</code></pre>
<p><strong>Kruskal 实现</strong></p>
<pre><code class="language-python">from heapq import *

def kruskal_mst(graph):
    union_find_set = UnionFindSet(graph.nodes.values())
    heap = []

    for edge in graph.edges:
        heappush(heap, edge)

    result = set()

    while heap:
        edge = heappop(heap)
        if not union_find_set.is_same_set(edge.from_node, edge.to_node):
            result.add(edge)
            union_find_set.union(edge.from_node, edge.to_node)

    return result
</code></pre>
<h4 id="prim-算法">Prim 算法</h4>
<p>从任意一个节点开始, 将与其相连的几条边全部放入一个小根堆中, 每次弹出小根堆中权重最小的边, 获取该边的另一端节点放入集合中, 并将另一端节点的所有直接相连的边放入小根堆中, 迭代直到小根堆中的边全部取出为止.</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200623110909.png" alt="" loading="lazy"></figure>
<h5 id="java-2">Java</h5>
<pre><code class="language-java">public class PrimMST {
    public static Set&lt;Edge&gt; primMST(Graph graph) {
        PriorityQueue&lt;Edge&gt; priorityQueue = new PriorityQueue&lt;&gt;((a, b) -&gt; {
            return a.weight - b.weight;
        });
        HashSet&lt;GraphNode&gt; set = new HashSet&lt;&gt;();
        Set&lt;Edge&gt; result = new HashSet&lt;&gt;();
		
        // 遍历所有节点, 考虑到可能有多棵生成树不相连的情况
        for (GraphNode node : graph.nodes.values()) {
            if (!set.contains(node)) {
                set.add(node);
                priorityQueue.addAll(node.edges);

                while (!priorityQueue.isEmpty()) {
                    Edge edge = priorityQueue.poll();
                    GraphNode toNode = edge.to;
                    if (!set.contains(toNode)) {
                        set.add(toNode);
                        result.add(edge);
                        priorityQueue.addAll(toNode.edges);
                    }
                }
            }
        }
        return result;
    }

    public static void main(String[] args) {
        // Prim 算法需沿着边迭代加入点, 故建立无向图时需建立双向指针
        Integer[][] nodes = {
                {6, 1, 2}, {6, 2, 1},
                {1, 1, 3}, {1, 3, 1},
                {5, 1, 4}, {5, 4, 1},
                {5, 2, 3}, {5, 3, 2},
                {3, 2, 5}, {3, 5, 2},
                {5, 3, 4}, {5, 4, 3},
                {4, 3, 6}, {4, 6, 3},
                {6, 3, 5}, {6, 5, 3},
                {2, 4, 6}, {2, 6, 4},
                {6, 5, 6}, {6, 6, 5}
        };
        Graph graph = Code_26_GraphBuilder.createGraph(nodes);
        Set&lt;Edge&gt; result = primMST(graph);
        for (Edge edge : result) {
            System.out.println(edge.weight);
        }
    }
}
</code></pre>
<pre><code class="language-java">1 3 2 4 5
</code></pre>
<h5 id="python-2">Python</h5>
<pre><code class="language-python">def prim_mst(graph):
    edge_heap = []
    node_set = set()
    result = set()

    for node in graph.nodes.values():
        if node not in node_set:
            node_set.add(node)
            for edge in node.edges:
                heappush(edge_heap, edge)

            while edge_heap:
                edge = heappop(edge_heap)
                to_node = edge.to_node
                if to_node not in node_set:
                    node_set.add(to_node)
                    result.add(edge)
                    for next_edge in to_node.edges:
                        heappush(edge_heap, next_edge)
    return result
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[拓扑排序算法]]></title>
        <id>https://zcybupt.github.io/post/TopologySort/</id>
        <link href="https://zcybupt.github.io/post/TopologySort/">
        </link>
        <updated>2020-06-21T10:45:38.000Z</updated>
        <content type="html"><![CDATA[<p>在图论中，由一个有向无环图的顶点组成的序列，当且仅当满足下列条件时，才能称为该图的一个<a href="https://zh.wikipedia.org/wiki/%E6%8B%93%E6%92%B2%E6%8E%92%E5%BA%8F">拓扑排序</a>（Topological sorting）：</p>
<ol>
<li>序列中包含每个顶点，且每个顶点只出现一次；</li>
<li>若A在序列中排在B的前面，则在图中不存在从B到A的路径。</li>
</ol>
<p>适用范围:</p>
<ul>
<li>有向图</li>
<li>有入度为 0 的节点</li>
<li>图中没有环</li>
</ul>
<p>拓扑排序常应用于有依赖关系的一组事务之间的排序. 例如有依赖关系的多个模块的编译次序问题等. 假定头图中的 <code>A</code> , <code>B</code> , <code>C</code> , <code>D</code> , <code>K</code> 为待编译的几个模块, 箭头表示依赖关系(如 <code>B</code> , <code>D</code> 依赖于 <code>K</code> ), 经拓扑排序后应输出模块编译顺序, 即 <code>K</code> -&gt; <code>C</code> -&gt; <code>B</code> -&gt; <code>D</code> -&gt; <code>A</code> 或 <code>C</code> -&gt; <code>K</code> -&gt; <code>B</code> -&gt; <code>D</code> -&gt; <code>A</code> ( <code>K</code> 和 <code>C</code> , <code>B</code> 和 <code>D</code> 顺序可互换)</p>
<h4 id="思路">思路</h4>
<p>先找到所有入度为 0 的节点并输出, 删除这些节点. 在下一次迭代中, 找到剩余节点中入度为 0 的节点并输出, 直到图中节点全部输出为止.</p>
<h5 id="java">Java</h5>
<p>图的结构定义及实现见<a href="https://zcybupt.github.io/post/Graph/">另一篇博客</a></p>
<pre><code class="language-java">public class TopologySort {
    public static List&lt;GraphNode&gt; topologySort(Graph graph) {
        HashMap&lt;GraphNode, Integer&gt; inMap = new HashMap&lt;&gt;();
        Queue&lt;GraphNode&gt; zeroInQueue = new LinkedList&lt;&gt;();
        // 取出所有节
        for (GraphNode node : graph.nodes.values()) {
            inMap.put(node, node.in);   // 建立节点和其入度的对应关系
            if (node.in == 0)
                zeroInQueue.add(node);
        }

        List&lt;GraphNode&gt; result = new ArrayList&lt;&gt;();
        while (!zeroInQueue.isEmpty()) {
            GraphNode cur = zeroInQueue.poll();
            result.add(cur);
            // 将入度为 0 的节点的下一层节点的入度减一, 若为 0 则放入 zeroInQueue 中
            for (GraphNode next : cur.nexts) {
                inMap.put(next, inMap.get(next) - 1);
                if (inMap.get(next) == 0) {
                    zeroInQueue.add(next);
                }
            }
        }
        return result;
    }
}
</code></pre>
<h5 id="python">Python</h5>
<pre><code class="language-python">def topology_sort(graph):
    in_dict = dict()
    zero_in_list = []

    for node in graph.nodes.values():
        in_dict[node] = node.in_degree
        if node.in_degree == 0:
            zero_in_list.append(node)

    result = []
    while zero_in_list:
        cur = zero_in_list.pop()
        result.append(cur)

        for next in cur.nexts:
            in_dict[next] = in_dict[next] - 1
            if in_dict[next] == 0:
                zero_in_list.append(next)

    return result
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[深度优先遍历]]></title>
        <id>https://zcybupt.github.io/post/DFS/</id>
        <link href="https://zcybupt.github.io/post/DFS/">
        </link>
        <updated>2020-06-21T08:07:49.000Z</updated>
        <content type="html"><![CDATA[<p>借助栈和集合实现树和图的深度优先遍历, 每次从栈中取出一个节点 <code>A</code> , 遍历与节点 <code>A</code> 相邻的后续节点 <code>Bn</code> , 若集合中没有该后续 <code>Bn</code> 则将节点 <code>A</code> 放回栈中, 并将后续节点 <code>Bn</code> 放入栈和集合中, 输出后续节点 <code>Bn</code> . 每遍历完一个后续节点就跳出当前循环, 避免出现横向遍历 ( 如下图, 可能会产生 2, 3, 4 的错误输出 ).</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200620202932.png" alt="" loading="lazy"></figure>
<p>以上图为例, 图的节点定义及整体结构见<a href="https://zcybupt.github.io/post/Graph/">上一篇博客</a></p>
<h5 id="java">Java</h5>
<pre><code class="language-java">public class DFS {
    public static void dfs(GraphNode node) {
        if (node == null) return;

        Stack&lt;GraphNode&gt; stack = new Stack&lt;&gt;();
        HashSet&lt;GraphNode&gt; set = new HashSet&lt;&gt;();
        stack.push(node);
        set.add(node);
        System.out.print(node.value + &quot; &quot;);
        while (!stack.isEmpty()) {
            GraphNode cur = stack.pop();
            for (GraphNode next : cur.nexts) {
                if (!set.contains(next)) {
                    stack.push(cur);
                    stack.push(next);
                    set.add(next);
                    System.out.print(next.value + &quot; &quot;);
                    break;
                }
            }
        }
    }

    public static void main(String[] args) {
        Integer[][] nodes = {
                {1, 1, 2},
                {1, 1, 3},
                {1, 1, 4},
                {1, 2, 3},
                {1, 2, 7},
                {1, 3, 5},
                {1, 4, 6},
                {1, 7, 3}
        };
        Graph graph = GraphBuilder.createGraph(nodes);
        dfs(graph.nodes.get(1));
    }
}
</code></pre>
<pre><code class="language-java">1 2 3 5 7 4 6
</code></pre>
<h5 id="python">Python</h5>
<pre><code class="language-python">def dfs(node):
    if not node: return

    ele_stack = []
    ele_set = set()

    ele_stack.append(node)
    ele_set.add(node)

    print(node.value, end=' ')
    while ele_stack:
        current_node = ele_stack.pop()
        for next_node in current_node.nexts:
            if next_node not in ele_set:
                ele_stack.append(current_node)
                ele_stack.append(next_node)
                ele_set.add(next_node)
                print(next_node.value, end=' ')
                break
</code></pre>
]]></content>
    </entry>
</feed>