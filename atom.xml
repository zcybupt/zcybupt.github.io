<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://zcybupt.github.io</id>
    <title>CODEFANCIER</title>
    <updated>2020-07-22T00:56:04.259Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://zcybupt.github.io"/>
    <link rel="self" href="https://zcybupt.github.io/atom.xml"/>
    <logo>https://zcybupt.github.io/images/avatar.png</logo>
    <icon>https://zcybupt.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, CODEFANCIER</rights>
    <entry>
        <title type="html"><![CDATA[Manacher算法]]></title>
        <id>https://zcybupt.github.io/post/Manacher/</id>
        <link href="https://zcybupt.github.io/post/Manacher/">
        </link>
        <updated>2020-07-20T02:58:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-5">题目(<a href="https://leetcode-cn.com/problems/longest-palindromic-substring/">LeetCode #5</a>)</h2>
<p>给定一个字符串 <code>s</code>，找到 <code>s</code> 中最长的回文子串。你可以假设 <code>s</code> 的最大长度为 1000。</p>
<p><strong>示例 1：</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;babad&quot;<br>
<strong>输出:</strong> &quot;bab&quot;<br>
<strong>注意:</strong> &quot;aba&quot; 也是一个有效答案。</p>
</blockquote>
<p><strong>示例 2：</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;cbbd&quot;<br>
<strong>输出:</strong> &quot;bb&quot;</p>
</blockquote>
<h2 id="题解">题解</h2>
<h3 id="扩展中心法">扩展中心法</h3>
<p>遍历字符串，由每个字符串向两侧扩展逐一比对。</p>
<p>时间复杂度为 <code>O(n²)</code> 。</p>
<pre><code class="language-java">public class Solution {
    public static String longestPalindrome(String s) {
        if (s == null || s.length() == 0) return &quot;&quot;;
        int start = 0, end = 0;

        for (int i = 0; i &lt; s.length(); i++) {
            int len1 = expandAroundCenter(s, i, i);
            int len2 = expandAroundCenter(s, i, i + 1);
            int len = Math.max(len1, len2);

            if (len &gt; end - start) {
                start = i - (len - 1) / 2;
                end = i + len / 2;
            }
        }

        return s.substring(start, end + 1);
    }

    public static int expandAroundCenter(String s, int left, int right) {
        while (left &gt;= 0 &amp;&amp; right &lt; s.length() &amp;&amp; s.charAt(left) == s.charAt(right)) {
            left--; right++;
        }

        return right - left - 1;
    }
}
</code></pre>
<h3 id="manacher算法">Manacher算法</h3>
<h4 id="预处理">预处理</h4>
<p>在原始字符串的每个字符的左右两边都加上特殊字符，使处理后的字符串长度均为奇数，以此来解决偶回文时用扩展法不能找到回文中心而导致的找不到回文子串的问题。</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721172042.png" alt="处理前" loading="lazy"></figure>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721172128.png" alt="处理后" loading="lazy"></figure>
<h4 id="最长回文子串长度">最长回文子串长度</h4>
<p>观察可知，预处理后的字符串的最长回文子串的回文半径减 <code>1</code> 即为原字符串的最长回文子串长度。以字符串 <code>cabbaf</code> 为例，处理后的最长回文子串的回文半径为 <code>p[6] = 5</code> ，<code>p[6] - 1 = 4</code> 即为原字符串的最长回文子串 <code>abba</code> 的长度。</p>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721172934.png" alt="" loading="lazy"></figure>
<p>所以原字符串的最长回文子串长度 <code>maxLen = p[i] - 1</code></p>
<h4 id="最长回文子串的起始索引">最长回文子串的起始索引</h4>
<p>在 <code>#c#a#b#b#a#f#</code> 中，<code>p[6] = 5</code> ，用索引 <code>i</code> 减去最长回文半径 <code>p[i]</code> 即可得到原字符串的最长回文子串的起始索引，即 <code>i - p[i] = 6 - 5 = 1</code> ；但对奇回文 <code>aba</code> 来说，用这种方法得到的结果为 <code>3 - p[3] = 3 - 4 = -1</code> ，下标越界。</p>
<p>为解决下标越界的问题，可在预处理后的字符串前再添加一个特殊字符，而为了使字符串长度保持为奇数，其后也需添加另一个字符。而原字符串的最长回文子串的起始索引变为 <code>(i - p[i]) / 2</code> 。</p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721180408.png" alt="" loading="lazy"></figure>
<p>对 <code>cabbaf</code> 来说，原字符串的最长回文子串的起始索引为 <code>(7 - p[7]) / 2 = (7 - 5) / 2 = 1</code></p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721180435.png" alt="" loading="lazy"></figure>
<p>对 <code>aba</code> 来说，原字符串的最长回文子串的起始索引为 <code>(3 - p[3]) / 2 = (3 - 3) / 2 = 0</code></p>
<p>故原字符串的最长回文子串的起始索引可表示为 <code>index = (i - p[i]) / 2</code></p>
<h4 id="计算-p-数组">计算 p 数组</h4>
<p>定义两个变量 <code>C</code> 和 <code>R</code> ，分别为某一回文子串的中心字符的索引和右边界。如对 <code>^#c#a#b#b#a#f#$</code> 中的 <code>#a#b#b#a#</code> 来说，<code>C = 7</code> ，<code>p[C] = 5</code> ，以 <code>7</code> 为中心的回文子串的右边界即为 <code>R = C + p[C] = 12</code> ；当 <code>C = 12</code> 时，以 <code>12</code> 为中心的回文子串的右边界即为 <code>R = C + p[C] = 12 + 2 = 14</code> 。</p>
<p>故可得回文子串右边界与其半径之间的关系为：<code>R = C + p[C]</code></p>
<figure data-type="image" tabindex="6"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721192607.png" alt="" loading="lazy"></figure>
<p>由于回文子串关于 <code>C</code> 对称，由该子串上某位置 <code>i</code> 可得其对称位置 <code>j</code> ，由 <code>i + j = 2 * C</code> 知 <code>j = 2 * C - i</code> 。</p>
<p>若以 <code>j</code> 为中心的回文子串的右边界在 <code>R</code> 以内，即 <code>j + p[j] &lt; R</code> 时，由对称性可知，<code>p[j] = p[i]</code> ；若以 <code>j</code> 为中心的回文子串的右边界大于 <code>R</code> ，即 <code>j + p[j] &gt; R</code> 时，图中红色部分为关于 <code>j</code> 对称的回文子串，故此时有 <code>p[j] = R - j</code> ；若 <code>j + p[j] &gt;= R</code> ，则令 <code>p[j] = 1</code> ，此时也需更新 <code>C</code> 和 <code>R</code> ，继续向后用扩展中心法计算 <code>p</code> 各位置的值。</p>
<h5 id="java">Java</h5>
<pre><code class="language-java">public class Manacher {
    public static String preprocess(String s) {
        String result = &quot;^&quot;;
        for (int i = 0; i &lt; s.length(); i++) {
            result += &quot;#&quot; + s.charAt(i);
        }

        return result + &quot;#$&quot;;
    }

    public static String longestPalindrome(String s) {
        if (s.length() == 0) return &quot;&quot;;

        String tmpStr = preprocess(s);
        int len = tmpStr.length();
        int[] p = new int[len];
        int C = 0, R = 0;
        int maxLen = -1;    // 最长回文子串长度
        int index = 0;      // 最长回文子串中心索引

        for (int i = 1; i &lt; len - 1; i++) {
            p[i] = R &gt; i ? Math.min(p[2 * C - i], R - i) : 1;

            while (tmpStr.charAt(i + p[i]) == tmpStr.charAt(i - p[i]))
                p[i]++;

            if (R &lt; p[i] + i) {
                R = p[i] + i;
                C = i;
            }

            if (maxLen &lt; p[i] - 1) {
                maxLen = p[i] - 1;
                index = i;
            }
        }

        int start = (index - maxLen) / 2;   // (i - p[i]) / 2
        return s.substring(start, start + maxLen);
    }
}
</code></pre>
<h5 id="python">Python</h5>
<pre><code class="language-python">class Manacher:
    def longest_palindrome(self, s: str) -&gt; str:
        if s is None or len(s) == 0: return &quot;&quot;

        tmp_str = '^#' + ''.join([x + '#' for x in s]) + &quot;$&quot;
        length = len(tmp_str)
        p = [0] * length
        c, r = 0, 0
        max_len = -1
        index = 0

        for i in range(1, length - 1):
            p[i] = min(p[2 * c - i], r - i) if r &gt; i else 1

            while tmp_str[i + p[i]] == tmp_str[i - p[i]]:
                p[i] += 1

            if r &lt; i + p[i]:
                r = i + p[i]
                c = i

            if max_len &lt; p[i] - 1:
                max_len = p[i] - 1
                index = i

        start = (index - max_len) // 2
        return s[start: start + max_len]
</code></pre>
<h5 id="go">Go</h5>
<pre><code class="language-go">func preprocess(s string) string {
	result := &quot;^#&quot;

	for _, ch := range s {
		result += string(ch) + &quot;#&quot;
	}

	return result + &quot;$&quot;
}

func longestPalindrome(s string) string {
	if len(s) == 0 {
		return &quot;&quot;
	}

	tmpStr := preprocess(s)
	length := len(tmpStr)
	p := make([]int, length)
	C, R := 0, 0
	maxLen := 0
	index := 0

	for i := 1; i &lt; length-1; i++ {
		if R &gt; i {
			p[i] = min(p[2*C-i], R-i)
		} else {
			p[i] = 1
		}

		for tmpStr[i+p[i]] == tmpStr[i-p[i]] {
			p[i]++
		}

		if R &lt; i+p[i] {
			R = i + p[i]
			C = i
		}

		if maxLen &lt; p[i]-1 {
			maxLen = p[i] - 1
			index = i
		}
	}

	start := (index - maxLen) / 2
	return s[start : start+maxLen]
}

func min(a, b int) int {
	if a &lt; b {
		return a
	} else {
		return b
	}
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[判断子树结构是否相同]]></title>
        <id>https://zcybupt.github.io/post/SubtreeOfAnotherTree/</id>
        <link href="https://zcybupt.github.io/post/SubtreeOfAnotherTree/">
        </link>
        <updated>2020-07-19T09:05:27.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-572">题目(<a href="https://leetcode-cn.com/problems/subtree-of-another-tree/">LeetCode #572</a>)</h2>
<p>给定两个非空二叉树 <code>s</code> 和 <code>t</code>，检验 <code>s</code> 中是否包含和 <code>t</code> 具有相同结构和节点值的子树。<code>s</code> 的一个子树包括 <code>s</code> 的一个节点和这个节点的所有子孙。<code>s</code> 也可以看做它自身的一棵子树。</p>
<p>示例 1:</p>
<p>给定的树 s:</p>
<pre><code class="language-python">     3
    / \
   4   5
  / \
 1   2
</code></pre>
<p>给定的树 t：</p>
<pre><code class="language-python">   4 
  / \
 1   2
</code></pre>
<p>返回 <strong>true</strong>，因为 t 与 s 的一个子树拥有相同的结构和节点值。</p>
<p>示例 2:</p>
<p>给定的树 s：</p>
<pre><code class="language-python">     3
    / \
   4   5
  / \
 1   2
    /
   0
</code></pre>
<p>给定的树 t：</p>
<pre><code class="language-python">   4
  / \
 1   2
</code></pre>
<p>返回 <strong>false</strong>。</p>
<h2 id="题解">题解</h2>
<p><strong>仅作为KMP的练习，非最优解</strong></p>
<p>先将两棵树序列化（这里采用先序遍历），再利用 KMP 判断 <code>t</code> 是否为 <code>s</code> 的子串。</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;

    TreeNode(int val) {
        this.val = val;
    }
}

public class Code_45_SubtreeOfAnotherTree {
    public static String getPreorderString(TreeNode treeNode) {
        if (treeNode == null) return &quot;#&quot;;

        String tmpResult = &quot;_&quot; + treeNode.val + &quot;_&quot; + getPreorderString(treeNode.left);
        tmpResult += &quot;_&quot; + getPreorderString(treeNode.right);

        return tmpResult;
    }

    public static int[] getNextTable(String pattern) {
        int len = pattern.length();
        int lp = -1, rp = 0;
        int[] nextTable = new int[len];
        nextTable[0] = -1;

        while (rp &lt; len - 1) {
            if (lp == -1 || pattern.charAt(lp) == pattern.charAt(rp)) {
                nextTable[++rp] = ++lp;
            } else {
                lp = nextTable[lp];
            }
        }

        return nextTable;
    }

    // KMP
    public static boolean isSubtree(TreeNode s, TreeNode t) {
        String serializedS = getPreorderString(s);
        String serializedT = getPreorderString(t);
        int sCur = 0, tCur = 0;
        int sLen = serializedS.length();
        int tLen = serializedT.length();
        int[] nextTable = getNextTable(serializedT);

        while (sCur &lt; sLen &amp;&amp; tCur &lt; tLen) {
            if (serializedS.charAt(sCur) == serializedT.charAt(tCur)) {
                sCur++;
                tCur++;
            } else {
                if (tCur == 0) sCur++;
                else tCur = nextTable[tCur];
            }
        }

        return tCur == tLen;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


class SubTreeOfAnotherTree:
    def get_preorder_string(self, tree_node: TreeNode):
        if not tree_node: return '#'

        tmp_str = '_' + str(tree_node.val) + '_' + self.get_preorder_string(tree_node.left)
        tmp_str += '_' + self.get_preorder_string(tree_node.right)

        return tmp_str

    def get_next_table(self, pattern: str):
        lp, rp = -1, 0
        length = len(pattern)
        next_table = [0] * length
        next_table[0] = -1

        while rp &lt; length - 1:
            if pattern[lp] == pattern[rp] or lp == -1:
                lp += 1
                rp += 1
                next_table[rp] = lp
            else:
                lp = next_table[lp]

        return next_table

    def isSubtree(self, s: TreeNode, t: TreeNode) -&gt; bool:
        serialized_s = self.get_preorder_string(s)
        serialized_t = self.get_preorder_string(t)
        s_len = len(serialized_s)
        t_len = len(serialized_t)

        s_cur, t_cur = 0, 0
        next_table = self.get_next_table(serialized_t)

        while s_cur &lt; s_len and t_cur &lt; t_len:
            if serialized_s[s_cur] == serialized_t[t_cur]:
                s_cur += 1
                t_cur += 1
            else:
                if t_cur == 0:
                    s_cur += 1
                else:
                    t_cur = next_table[t_cur]

        return t_cur == t_len
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">type TreeNode struct {
	Val   int
	Left  *TreeNode
	Right *TreeNode
}

func getPreorderString(treeNode *TreeNode) string {
	if treeNode == nil {
		return &quot;#&quot;
	}

	tmpStr := &quot;_&quot; + strconv.Itoa(treeNode.Val) + &quot;_&quot; + getPreorderString(treeNode.Left)
	tmpStr += &quot;_&quot; + getPreorderString(treeNode.Right)

	return tmpStr
}

func getNextTable(pattern string) []int {
	lp, rp := -1, 0
	length := len(pattern)
	nextTable := make([]int, length)
	nextTable[0] = -1

	for rp &lt; length-1 {
		if lp == -1 || pattern[lp] == pattern[rp] {
			lp++
			rp++
			nextTable[rp] = lp
		} else {
			lp = nextTable[lp]
		}
	}

	return nextTable
}

func isSubtree(s *TreeNode, t *TreeNode) bool {
	serializedS := getPreorderString(s)
	serializedT := getPreorderString(t)
	sCur, tCur := 0, 0
	sLen := len(serializedS)
	tLen := len(serializedT)
	nextTable := getNextTable(serializedT)

	for sCur &lt; sLen &amp;&amp; tCur &lt; tLen {
		if serializedS[sCur] == serializedT[tCur] {
			sCur++
			tCur++
		} else {
			if tCur == 0 {
				sCur++
			} else {
				tCur = nextTable[tCur]
			}
		}
	}

	return tCur == tLen
}
</code></pre>
<h3 id="奇淫巧技">奇淫巧技</h3>
<blockquote>
<p><a href="https://leetcode-cn.com/problems/subtree-of-another-tree/comments/384272">LeetCode @洪世贤</a>：<br>
没有什么是 Python 一行解决不了的</p>
</blockquote>
<p>Python 的诱惑？</p>
<pre><code class="language-python">class Solution:
    def isSubtree(self, s: TreeNode, t: TreeNode) -&gt; bool:
        return str(s).find(str(t)) != -1
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[KMP算法]]></title>
        <id>https://zcybupt.github.io/post/KMP/</id>
        <link href="https://zcybupt.github.io/post/KMP/">
        </link>
        <updated>2020-07-18T13:03:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-28">题目(<a href="https://leetcode-cn.com/problems/implement-strstr/">LeetCode #28</a>)</h2>
<p>实现 <code>strStr()</code> 函数。</p>
<p>给定一个 haystack 字符串和一个 needle 字符串，在 haystack 字符串中找出 needle 字符串出现的第一个位置 (从0开始)。如果不存在，则返回  -1。</p>
<p><strong>示例 1:</strong></p>
<blockquote>
<p><strong>输入:</strong> haystack = &quot;hello&quot;, needle = &quot;ll&quot;<br>
<strong>输出:</strong> 2</p>
</blockquote>
<p><strong>示例 2:</strong></p>
<blockquote>
<p><strong>输入:</strong> haystack = &quot;aaaaa&quot;, needle = &quot;bba&quot;<br>
<strong>输出:</strong> -1</p>
</blockquote>
<h2 id="题解">题解</h2>
<p>参考资料：</p>
<ul>
<li>
<p><a href="https://www.bilibili.com/video/BV1Px411z7Yo">KMP字符串匹配算法1</a></p>
</li>
<li>
<p><a href="https://www.bilibili.com/video/BV1hW411a7ys">KMP字符串匹配算法2</a></p>
</li>
<li>
<p><a href="https://mp.weixin.qq.com/s?__biz=MzI5ODA5NTgyMw==&amp;mid=2247483968&amp;idx=1&amp;sn=3c21f29eb6c5083f85ac7c97d2051303&amp;chksm=ecaa47e0dbddcef6a97a453df68bc6c6ff040bdd84f58caf99b1f3a8cb84d7405062b039c396&amp;token=1263366918&amp;lang=zh_CN#rd">图解KMP</a></p>
</li>
</ul>
<h3 id="java">Java</h3>
<pre><code class="language-java">public class KMP {
    public static int[] getNextTable(String needle) {
        int len = needle.length();
        int[] next = new int[len];
        next[0] = -1;
        int lp = -1, rp = 0;    // 左右指针, 即文章中的 k 和 index

        while (rp &lt; len - 1) {
            if (lp == -1 || needle.charAt(rp) == needle.charAt(lp))
                // k = -1 时, 左右指针均向右移动
                // 当左右指针所指元素相等时, 均向右移动
                next[++rp] = ++lp;
            else
                // 左右指针所指元素不等, 左指针移向其所指字符的对应 next 指向的位置
                lp = next[lp];
        }

        return next;
    }

    public static int getIndex(String haystack, String needle) {
        int haystackLen = haystack.length();
        int needleLen = needle.length();

        if (needleLen == 0) return 0;
        int haystackCur = 0, needleCur = 0;
        int[] next = getNextTable(needle);

        while (haystackCur &lt; haystackLen &amp;&amp; needleCur &lt; needleLen) {
            if (haystack.charAt(haystackCur) == needle.charAt(needleCur)) {
                // 对比两个字符串, 各位相同时两指针右移
                haystackCur++;
                needleCur++;
            } else {
                // 不相同则根据 next 表查找 needleCur 移向的位置
                // needleCur 移动到了开头, 退无可退, 主串指针继续后移
                if (needleCur == 0) haystackCur++;
                else needleCur = next[needleCur];
            }
        }

        return needleCur == needleLen ? haystackCur - needleCur : -1;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class KMP:
    def get_next_table(self, needle):
        length = len(needle)
        lp, rp = -1, 0
        next_table = [0] * length
        next_table[0] = -1

        while rp &lt; length - 1:
            if needle[lp] == needle[rp] or lp == -1:
                rp += 1
                lp += 1
                next_table[rp] = lp
            else:
                lp = next_table[lp]

        return next_table

    def get_index(self, haystack, needle):
        haystack_len = len(haystack)
        needle_len = len(needle)

        if needle_len == 0: return 0
        haystack_cur, needle_cur = 0, 0
        next_table = self.get_next_table(needle)

        while haystack_cur &lt; haystack_len and needle_cur &lt; needle_len:
            if haystack[haystack_cur] == needle[needle_cur]:
                haystack_cur += 1
                needle_cur += 1
            else:
                if needle_cur == 0: haystack_cur += 1
                else: needle_cur = next_table[needle_cur]

        return haystack_cur - needle_cur if needle_cur == needle_len else -1
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">func getNextTable(needle string) []int {
	length := len(needle)
	lp, rp := -1, 0
	next := make([]int, length)
	next[0] = -1

	for rp &lt; length-1 {
		if lp == -1 || needle[lp] == needle[rp] {
			rp++
			lp++
			next[rp] = lp
		} else {
			lp = next[lp]
		}
	}

	return next
}

func getIndex(haystack, needle string) int {
	haystackLen := len(haystack)
	needleLen := len(needle)

	if needleLen == 0 {
		return 0
	}

	haystackCur, needleCur := 0, 0
	next := getNextTable(needle)

	for haystackCur &lt; haystackLen &amp;&amp; needleCur &lt; needleLen {
		if haystack[haystackCur] == needle[needleCur] {
			haystackCur++
			needleCur++
		} else {
			if needleCur == 0 {
				haystackCur++
			} else {
				needleCur = next[needleCur]
			}
		}
	}

	if needleCur == needleLen {
		return haystackCur - needleCur
	} else {
		return -1
	}
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[日程安排问题]]></title>
        <id>https://zcybupt.github.io/post/BestArrange/</id>
        <link href="https://zcybupt.github.io/post/BestArrange/">
        </link>
        <updated>2020-07-15T11:57:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="问题">问题</h2>
<p>一些项目要占用一个会议室宣讲, 会议室不能同时容纳两个项目的宣讲. 给定每个项目的开始和结束时间 (输入为数组, 其中是一个个具体的项目), 合理安排宣讲日程, 使得会议室进行的宣讲场次最多, 返回这个最多的场次</p>
<h2 id="题解">题解</h2>
<p>本题不可按开始时间排序, 若某个宣讲开始时间早但持续时间长, 可能不如多个晚开始的宣讲场次多; 也不可按持续时间长短, 某个短时间的宣讲可能与其它宣讲的起始时间重叠.</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200715202427.png" alt="" loading="lazy"></figure>
<h3 id="java">Java</h3>
<pre><code class="language-java">class Program {
    int start;
    int end;

    public Program(int start, int end) {
        this.start = start;
        this.end = end;
    }
}

public class BestArrange {
    public static int bestArrange(Program[] programs, int start) {
        Arrays.sort(programs, (a, b) -&gt; a.end - b.end);
        int result = 0;

        for (Program program : programs) {
            if (start &lt;= program.start) {
                result++;
                start = program.end;
            }
        }

        return result;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class Program:
    def __init__(self, start, end):
        self.start = start
        self.end = end


def get_best_arrange(programs, start):
    programs.sort(key=lambda x: x.end)
    result = 0

    for program in programs:
        if start &lt;= program.start:
            result += 1
            start = program.end

    return result
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">type Program struct {
	start, end int
}

func getBestArrange(programs []Program, start int) int {
	for i := 0; i &lt; len(programs); i++ {
		for j := 0; j &lt; len(programs)-1; j++ {
			if programs[j].end &gt; programs[j+1].end {
				programs[j], programs[j+1] = programs[j+1], programs[j]
			}
		}
	}
	result := 0

	for _, program := range programs {
		if start &lt;= program.start {
			result++
			start = program.end
		}
	}

	return result
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[最低字典序排列字符串]]></title>
        <id>https://zcybupt.github.io/post/LexicographicalString/</id>
        <link href="https://zcybupt.github.io/post/LexicographicalString/">
        </link>
        <updated>2020-07-13T10:29:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目">题目</h2>
<p>给定一个字符串类型的数组 <code>strs</code> , 找到一种拼接方式, 使得把所有字符串拼接起来之后形成的字符串具有最低的字典序.</p>
<h2 id="题解">题解</h2>
<p>不可通过将各个数组元素按字典序排列再拼接的方式来解, 如对数组 <code>['b', 'ba']</code> 来说, 若按元素字典序来排列将得到 <code>bba</code> , 而实际上该结果并没有 <code>bab</code> 小, 故这种做法不可取.</p>
<p>本题应比较 <code>str1 + str2</code> 和 <code>str2 + str1</code> 的字典序大小, 以此为标准重写比较函数, 排序后拼接即可.</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">public class LowestLexicography {
    public static String lowestString(String[] strs) {
        if (strs == null || strs.length == 0) {
            return null;
        }

        Arrays.sort(strs, (a, b) -&gt; (a + b).compareTo(b + a));

        String result = &quot;&quot;;
        for (String str : strs) {
            result += str;
        }

        return result;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<p>以冒泡排序为例</p>
<pre><code class="language-python">def get_lowest_lexicography(strings):
    for i in range(len(strings)):
        for j in range(len(strings) - 1):
            if strings[j] + strings[j + 1] &gt; strings[j + 1] + strings[j]:
                strings[j], strings[j + 1] = strings[j + 1], strings[j]

    return &quot;&quot;.join(strings)
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-python">func getLowestLexicography(strs []string) string {
	for i := 0; i &lt; len(strs); i++ {
		for j := 0; j &lt; len(strs)-1; j++ {
			if strs[j]+strs[j+1] &gt; strs[j+1]+strs[j] {
				strs[j], strs[j+1] = strs[j+1], strs[j]
			}
		}
	}

	return strings.Join(strs, &quot;&quot;)
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[滑动窗口最大值]]></title>
        <id>https://zcybupt.github.io/post/SlidingWindowMaximum/</id>
        <link href="https://zcybupt.github.io/post/SlidingWindowMaximum/">
        </link>
        <updated>2020-07-12T02:19:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-239">题目(<a href="https://leetcode-cn.com/problems/sliding-window-maximum/">LeetCode #239</a>)</h2>
<p>给定一个数组 nums，有一个大小为 k 的滑动窗口从数组的最左侧移动到数组的最右侧。你只可以看到在滑动窗口内的 k 个数字。滑动窗口每次只向右移动一位。</p>
<p>返回滑动窗口中的最大值。</p>
<p>示例:</p>
<pre><code class="language-java">输入: nums = [1,3,-1,-3,5,3,6,7], 和 k = 3
输出: [3,3,5,5,6,7] 
解释: 
             滑动窗口的位置              最大值
[ 1   3   -1 ] -3   5   3   6   7        3
  1 [ 3   -1   -3 ] 5   3   6   7        3
  1   3 [ -1   -3   5 ] 3   6   7        5
  1   3   -1 [ -3   5   3 ] 6   7        5
  1   3   -1   -3 [ 5   3   6 ] 7        6
  1   3   -1   -3   5 [ 3   6   7 ]      7
</code></pre>
<p>提示:</p>
<ul>
<li>1 &lt;= nums.length &lt;= 10^5</li>
<li>-10^4 &lt;= nums[i] &lt;= 10^4</li>
<li>1 &lt;= k &lt;= nums.length</li>
</ul>
<h2 id="题解">题解</h2>
<h3 id="窗口内最值的更新结构">窗口内最值的更新结构</h3>
<p>借助单调双端队列实现, 队列由头到尾单调递减, 头指针始终指向窗口内的最大值. 定义两个指针 <code>L</code> 与 <code>R</code> , 初始值为 -1, <code>L</code> 与 <code>R</code> 之间即为窗口范围. 在窗口移动的过程中, 需满足:</p>
<ol>
<li><code>L</code> 和 <code>R</code> 不向左移动</li>
<li><code>L</code> 不能移动到 <code>R</code> 的右侧</li>
</ol>
<p>定义一个双端队列, 其中保存输入数组中元素的索引.</p>
<ul>
<li>
<p>当窗口增加元素, 即 <code>R</code> 增加时, 需保持队列的单调性. 若新元素比原有元素都小, 则将其索引追加在最后; 若队列中有小于等于新元素的, 则先将所有小于等于新元素的索引弹出, 再加入新元素的索引. 以此实现队列的头指针始终指向最大元素</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200712162837.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>当窗口减少元素, 即 <code>L</code> 增加时, 依次弹出头结点即可.</p>
</li>
</ul>
<h3 id="java">java</h3>
<p>本题窗口宽度固定, 可用相对位置表示 <code>L</code> 与 <code>R</code></p>
<pre><code class="language-java">public class SlidingWindowMaximum {
    public static int[] maxSlidingWindow(int[] nums, int k) {
        if (nums == null || k &lt; 1 || nums.length &lt; k) {
            return null;
        }

        LinkedList&lt;Integer&gt; maxQueue = new LinkedList&lt;&gt;();
        // 共有 arr.length - k + 1 个窗口, 即共有arr.length - win + 1个最大值
        int[] result = new int[nums.length - k + 1];
        int index = 0;

        // 用 i 表示 R
        for (int i = 0; i &lt; nums.length; i++) {
            while (!maxQueue.isEmpty() &amp;&amp; nums[maxQueue.peekLast()] &lt;= nums[i]) {
                maxQueue.pollLast();
            }
            maxQueue.addLast(i);

            // i - k 为过期的索引, 即窗口的 L 已越过该位置, 需弹出头结点维持窗口大小为 k
            if (maxQueue.peekFirst() == i - k) {
                System.out.println(i + &quot; &quot; + k);
                maxQueue.pollFirst();
            }
            if (i &gt;= k - 1) {
                result[index++] = nums[maxQueue.peekFirst()];
            }
        }

        return result;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class SlidingWindowMaximum:
    def maxSlidingWindow(self, nums, k):
        if not nums or k &lt; 1 or len(nums) &lt; k:
            return

        max_queue = []
        result = []

        for i in range(len(nums)):
            while max_queue and nums[max_queue[-1]] &lt;= nums[i]:
                max_queue.pop()
            max_queue.append(i)

            if max_queue[0] == i - k:
                max_queue = max_queue[1:]

            if i &gt;= k - 1:
                result.append(nums[max_queue[0]])

        return result
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">func maxSlidingWindow(nums []int, k int) []int {
	if nums == nil || k &lt; 0 || len(nums) &lt; k {
		return nil
	}

	maxQueue := make([]int, len(nums))
	result := make([]int, len(nums)-k+1)
	index := 0

	for i := 0; i &lt; len(nums); i++ {
		for len(maxQueue) &gt; 0 &amp;&amp; nums[maxQueue[len(maxQueue)-1]] &lt;= nums[i] {
			maxQueue = maxQueue[:len(maxQueue)-1]
		}
		maxQueue = append(maxQueue, i)

		if maxQueue[0] == i-k {
			maxQueue = maxQueue[1:]
		}

		if i &gt;= k-1 {
			result[index] =nums[maxQueue[0]]
			index++
		}
	}

	return result
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[子数组最大累加和]]></title>
        <id>https://zcybupt.github.io/post/MaxSumOfSubarray/</id>
        <link href="https://zcybupt.github.io/post/MaxSumOfSubarray/">
        </link>
        <updated>2020-07-11T02:37:01.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-53">题目(<a href="https://leetcode-cn.com/problems/maximum-subarray/submissions/">LeetCode #53</a>)</h2>
<p>给定一个整数数组 <code>nums</code> ，找到一个具有最大和的连续子数组（子数组最少包含一个元素），返回其最大和。</p>
<p>示例:</p>
<blockquote>
<p>输入: [-2,1,-3,4,-1,2,1,-5,4],<br>
输出: 6<br>
解释: 连续子数组 [4,-1,2,1] 的和最大，为 6。</p>
</blockquote>
<h2 id="解法">解法</h2>
<p>创建两个变量, <code>current</code> 和 <code>maxSum</code> , 遍历数组, 将每个元素累加到 <code>current</code> 上, 再与 <code>maxSum</code> 比较, 使 <code>maxSum</code> 总保存 <code>current</code> 的最大值, 若累加过程中 <code>current</code> 变为负数, 则手动将其归零, 继续计算.</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">public class MaxSumOfSubarray {
    public static int maxSubArray(int[] nums) {
        if (nums == null || nums.length == 0) {
            return 0;
        }
        int current = 0;
        int maxSum = Integer.MIN_VALUE;

        for (int num : nums) {
            current += num;
            maxSum = Math.max(current, maxSum);
            current = Math.max(current, 0);
        }

        return maxSum;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class MaxSumOfSubarray:
    def maxSubArray(self, nums):
        if not nums or len(nums) == 0:
            return 0

        current = 0
        max_sum = float(&quot;-inf&quot;)

        for num in nums:
            current += num
            max_sum = max(current, max_sum)
            current = max(0, current)

        return max_sum
</code></pre>
<h3 id="go">Go</h3>
<p>Go 中整型常量可定义如下</p>
<pre><code class="language-go">const INT_MAX = int(^uint(0) &gt;&gt; 1) 	// 有符号最大值
const INT_MIN = ^INT_MAX			// 有符号最小值

const UINT_MIN uint = 0				// 无符号最小值, 即为 0
const UINT_MAX = ^uint(0)			// 无符号最大值
</code></pre>
<pre><code class="language-go">func maxSubArray(nums []int) int {
	if nums == nil || len(nums) == 0 {
		return 0
	}

	current := 0
	maxSum := ^int(^uint(0) &gt;&gt; 1)

	for _, num := range nums {
		current += num
		maxSum = max(current, maxSum)
		current = max(0, current)
	}

	return maxSum
}

func max(a, b int) int {
	if a &gt; b {
		return a
	} else {
		return b
	}
}

func min(a, b int) int {
	if a &lt; b {
		return a
	} else {
		return b
	}
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[接雨水问题]]></title>
        <id>https://zcybupt.github.io/post/TrappingRainWater/</id>
        <link href="https://zcybupt.github.io/post/TrappingRainWater/">
        </link>
        <updated>2020-07-10T02:44:54.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-42">题目(<a href="https://leetcode-cn.com/problems/trapping-rain-water/">LeetCode #42</a>)</h2>
<p>给定 n 个非负整数表示每个宽度为 1 的柱子的高度图，计算按此排列的柱子，下雨之后能接多少雨水。</p>
<p>上面是由数组 [0,1,0,2,1,0,1,3,2,1,2,1] 表示的高度图，在这种情况下，可以接 6 个单位的雨水（蓝色部分表示雨水）。</p>
<h2 id="题解">题解</h2>
<p>本题可转化为求每根柱子上可接到的水量的和, 而每根柱子上可留住的水量又取决于其左右两侧最高的柱子的高度, 假设一根柱子高度为 1, 其左右两侧最高的柱子高度为 3 和 5, 则这根柱子上方可留住的水量为 3 - 1 = 2.</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200710111108.png" alt="" loading="lazy"></figure>
<h3 id="预处理数组法">预处理数组法</h3>
<p>若每次迭代均遍历查找左右两侧的最大值将增加时间复杂度. 故可采用预处理数组的方式, 将数组中每个元素左右两侧的最大值保存起来. 本例中使用两个辅助数组, 分别保存每位置上的左侧最大值和右侧最大值. 如下例所示:</p>
<pre><code class="language-python">[3, 2, 4, 5, 4, 3, 1] # 原数组
[3, 3, 4, 5, 5, 5, 5] # 每个元素左侧最大值构成的辅助数组
[5, 5, 5, 5, 4, 3, 1] # 每个元素右侧最大值构成的辅助数组
</code></pre>
<p>该种解法的时间复杂度和空间复杂度均为为 <code>O(n)</code></p>
<h4 id="java">Java</h4>
<pre><code class="language-java">public class TrapRainWater {
    public static int trap(int[] heightArr) {
        if (heightArr == null || heightArr.length &lt; 0)
            return 0;

        int arrLen = heightArr.length;
        int[] help1 = new int[arrLen];
        int[] help2 = new int[arrLen];
        int result = 0;

        help1[0] = heightArr[0];
        help2[arrLen - 1] = heightArr[arrLen - 1];

        for (int i = 1; i &lt; arrLen; i++) {
            help1[i] = Math.max(help1[i - 1], heightArr[i]);
            help2[arrLen - i - 1] = Math.max(help2[arrLen - i], heightArr[arrLen - i - 1]);
        }

        for (int i = 1; i &lt; arrLen - 1; i++) {
            int tmp = Math.min(help1[i - 1], help2[i + 1]) - heightArr[i];
            result += Math.max(tmp, 0);
        }

        return result;
    }

    public static void main(String[] args) {
        int[] heightArr = {0, 1, 0, 2, 1, 0, 1, 3, 2, 1, 2, 1};
        System.out.println(trap(heightArr));
    }
}
</code></pre>
<pre><code class="language-java">6
</code></pre>
<h4 id="python">Python</h4>
<pre><code class="language-python">class TrapRainWater:
    def trap(self, height_arr):
        if not height_arr or len(height_arr) &lt; 0:
            return 0
        arr_len = len(height_arr)
        help1 = [0] * arr_len
        help2 = [0] * arr_len
        result = 0

        help1[0] = height_arr[0]
        help2[arr_len - 1] = height_arr[arr_len - 1]

        for i in range(1, arr_len):
            help1[i] = max(help1[i - 1], height_arr[i])
            help2[arr_len - i - 1] = max(help2[arr_len - i], height_arr[arr_len - i - 1])

        for i in range(1, arr_len - 1):
            tmp = min(help1[i - 1], help2[i + 1]) - height_arr[i]
            result += max(0, tmp)

        return result
</code></pre>
<h4 id="go">Go</h4>
<p>Go 为啥没有 min/max, 也没有三目运算符...</p>
<pre><code class="language-go">func trap(heightArr []int) int {
	if heightArr == nil || len(heightArr) &lt; 0 {
		return 0
	}

	arrLen := len(heightArr)
	help1 := make([]int, arrLen)
	help2 := make([]int, arrLen)
	result := 0

	help1[0] = heightArr[0]
	help2[arrLen-1] = heightArr[arrLen-1]
	for i := 1; i &lt; arrLen; i++ {
		help1[i] = max(help1[i-1], heightArr[i])
		help2[arrLen-i-1] = max(help2[arrLen-i], heightArr[arrLen-i-1])
	}

	for i := 1; i &lt; arrLen-1; i++ {
		tmp := min(help1[i-1], help2[i+1])
		result += max(0, tmp-heightArr[i])
	}

	return result
}

func max(a, b int) int {
	if a &gt; b {
		return a
	} else {
		return b
	}
}

func min(a, b int) int {
	if a &lt; b {
		return a
	} else {
		return b
	}
}
</code></pre>
<h3 id="双指针法">双指针法</h3>
<p>本方法时间复杂度为 <code>O(N)</code> 空间复杂度为 <code>O(1)</code></p>
<h4 id="java-2">Java</h4>
<pre><code class="language-java">public class TrapRainWater {
    public static int trap(int[] heightArr) {
        if (heightArr == null || heightArr.length &lt; 3)
            return 0;

        int arrLen = heightArr.length;
        int leftMax = heightArr[0];
        int rightMax = heightArr[arrLen - 1];
        int L = 1;
        int R = arrLen - 2;
        int result = 0;

        while (L &lt;= R) {
            if (leftMax &lt;= rightMax) {
                result += Math.max(0, leftMax - heightArr[L]);
                leftMax = Math.max(leftMax, heightArr[L++]);
            } else {
                result += Math.max(0, rightMax - heightArr[R]);
                rightMax = Math.max(rightMax, heightArr[R--]);
            }
        }

        return result;
    }
}
</code></pre>
<h4 id="python-2">Python</h4>
<pre><code class="language-python">class TrapRainWater:
    def trap(self, height_arr):
        if not height_arr or len(height_arr) &lt; 3:
            return 0

        arr_len = len(height_arr)
        left_max = height_arr[0]
        right_max = height_arr[arr_len - 1]
        l = 1
        r = arr_len - 2
        result = 0

        while l &lt;= r:
            if left_max &lt;= right_max:
                result += max(0, left_max - height_arr[l])
                left_max = max(left_max, height_arr[l])
                l += 1
            else:
                result += max(0, right_max - height_arr[r])
                right_max = max(right_max, height_arr[r])
                r -= 1

        return result
</code></pre>
<h4 id="go-2">Go</h4>
<pre><code class="language-go">func trap(heightArr []int) int {
	if heightArr == nil || len(heightArr) &lt; 3 {
		return 0
	}

	arrLen := len(heightArr)
	leftMax := heightArr[0]
	rightMax := heightArr[arrLen-1]
	L := 1
	R := arrLen - 2
	result := 0

	for L &lt;= R {
		if leftMax &lt;= rightMax {
			result += max(0, leftMax-heightArr[L])
			leftMax = max(leftMax, heightArr[L])
			L++
		} else {
			result += max(0, rightMax-heightArr[R])
			rightMax = max(rightMax, heightArr[R])
			R--
		}
	}

	return result
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[无 root 配置 CUDA 10 运行 Yolo v4]]></title>
        <id>https://zcybupt.github.io/post/Yolov4Deployment/</id>
        <link href="https://zcybupt.github.io/post/Yolov4Deployment/">
        </link>
        <updated>2020-07-08T11:26:50.000Z</updated>
        <content type="html"><![CDATA[<h2 id="环境部署">环境部署</h2>
<p>在无 root 的情况下可将 cuda 安装到自己的目录中, 配置好环境变量即可使用.</p>
<p>服务器基础信息:</p>
<blockquote>
<p>OS: Ubuntu 16.04<br>
Kernel: x86_64 Linux 4.13.0-38-generic<br>
CPU: Intel Xeon CPU E5-2620 v3 @ 3.2GHz<br>
GPU: 双路 GeForce RTX 2080Ti<br>
GPU驱动版本: 430.50<br>
RAM: 128 GB<br>
GCC: 5.4.1</p>
</blockquote>
<h3 id="cuda-10-安装">CUDA 10 安装</h3>
<p>到英伟达官网下载<a href="https://developer.nvidia.com/cuda-downloads">CUDA toolkit</a>的 runfile, 本文以 10.0 版本为例</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200708201529.png" alt="" loading="lazy"></figure>
<pre><code class="language-shell">$ wget https://developer.nvidia.com/compute/cuda/10.0/Prod/local_installers/cuda_10.0.130_410.48_linux
</code></pre>
<p>赋予可执行权限</p>
<pre><code class="language-shell">$ chmod +x ./cuda_10.0.130_410.48_linux
</code></pre>
<p>执行安装程序</p>
<pre><code class="language-shell">$ ./cuda_10.0.130_410.48_linux
</code></pre>
<p>按空格向下滚动, accept 许可</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200708202500.png" alt="" loading="lazy"></figure>
<p>提示是否安装显卡驱动, 由于已安装驱动, 且无 root 不可覆盖安装, 故输入 n 跳过</p>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200708202851.png" alt="" loading="lazy"></figure>
<p>按 y 安装 CUDA Toolkit, 在此需输入自己的 CUDA 安装路径, <strong>一定要选择自己有写入权限的目录</strong>, 若文件夹不存在将会自动创建, 在此以 <code>/server_space/zhaoym/cuda-10.0</code> 为例.</p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200708203038.png" alt="" loading="lazy"></figure>
<p>软链接无需创建, CUDA Samples 也无需安装</p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200708203218.png" alt="" loading="lazy"></figure>
<p>若 Toolkit 为 Installed 即为安装成功.</p>
<h3 id="cudnn-安装">cuDNN 安装</h3>
<p>首先下载 <a href="https://developer.nvidia.com/cudnn">cuDNN 库压缩文件</a>, 该链接不可直接通过 wget 下载, 可先使用浏览器打开, 再复制浏览器下载时跳转的地址进行下载</p>
<figure data-type="image" tabindex="6"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200708203643.png" alt="" loading="lazy"></figure>
<p>解压压缩文件</p>
<pre><code class="language-shell">$ tar -zxvf cudnn-10.0-linux-x64-v7.6.5.32.tgz
</code></pre>
<p>得到名为 cuda 的文件夹, 其目录结构与 CUDA 安装路径的目录结构相同</p>
<pre><code class="language-shell">cuda
├── NVIDIA_SLA_cuDNN_Support.txt
├── include
│   └── cudnn.h
└── lib64
    ├── libcudnn.so -&gt; libcudnn.so.7
    ├── libcudnn.so.7 -&gt; libcudnn.so.7.6.5
    ├── libcudnn.so.7.6.5
    └── libcudnn_static.a
</code></pre>
<p>将该文件夹下的文件分别复制到之前安装 CUDA 10 的<strong>对应</strong>路径中, 本例中即为 <code>/server_space/zhaoym/cuda-10.0</code></p>
<h3 id="配置环境变量">配置环境变量</h3>
<p>打开 <code>~/.bashrc</code></p>
<pre><code class="language-shell">$ vi ~/.bashrc
</code></pre>
<p>在末尾追加</p>
<pre><code class="language-shell">export PATH=/server_space/zhaoym/cuda-10.0/bin:$PATH
export LD_LIBRARY_PATH=/server_space/zhaoym/cuda-10.0/lib64:$LD_LIBRARY_PATH
</code></pre>
<p>若之前有配置过其它版本的 CUDA, 需先将其注释掉</p>
<p>退出编辑器, 使环境变量生效</p>
<pre><code class="language-shell">$ source ~/.bashrc
</code></pre>
<p>至此 CUDA 和 cuDNN 安装结束</p>
<pre><code class="language-shell">$ nvcc -V
</code></pre>
<p>输出</p>
<pre><code class="language-shell">nvcc: NVIDIA (R) Cuda compiler driver
Copyright (c) 2005-2018 NVIDIA Corporation
Built on Sat_Aug_25_21:08:01_CDT_2018
Cuda compilation tools, release 10.0, V10.0.130
</code></pre>
<h2 id="yolo-v4-编译">Yolo v4 编译</h2>
<p>项目主页: <a href="https://github.com/AlexeyAB/darknet">https://github.com/AlexeyAB/darknet</a></p>
<p>预训练权重: <a href="https://drive.google.com/open?id=1cewMfusmPjYWbrnuJRuKhPMwRe_b9PaT">yolov4.weights</a></p>
<p>克隆项目</p>
<pre><code class="language-shell">$ git clone https://github.com/AlexeyAB/darknet.git
</code></pre>
<p>修改 Makefile</p>
<pre><code class="language-shell">$ vi Makefile
</code></pre>
<p>将前三行修改为1, RTX 2080 Ti 为图灵架构, 将 CUDNN_HALF 设为 1 可进一步加速计算</p>
<pre><code>GPU=1
CUDNN=1
CUDNN_HALF=1
</code></pre>
<p>服务器默认 GCC/G++ 版本为 4.9.4, 这里改为使用更高的 5.4.1, 修改 60 和 62 行</p>
<pre><code class="language-shell">ifeq ($(USE_CPP), 1)
CC=g++-5
else
CC=gcc-5
endif
</code></pre>
<p>全局查找并替换 <code>/usr/local/cuda</code> 为 <code>/server_space/zhaoym/cuda-10.0</code></p>
<p>按 ESC 进入命令模式, 输入以下内容 , 回车完成替换</p>
<pre><code class="language-shell">:%s/\/usr\/local\/cuda/\/server_space\/zhaoym\/cuda-10.0/g
</code></pre>
<p>编译</p>
<pre><code class="language-shell">$ make -j8
</code></pre>
<p>若编译出错, 修改后需 <code>make clean</code> 再重新 <code>make</code></p>
<p>运行 demo</p>
<pre><code class="language-shell">$ ./darknet detector test ./cfg/coco.data ./cfg/yolov4.cfg ./yolov4.weights
</code></pre>
<figure data-type="image" tabindex="7"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200708211444.png" alt="" loading="lazy"></figure>
<p>Yolo v4 的训练配置方式与 v3 相同, 可参考之前的文章: <a href="https://zcybupt.github.io/post/YoloDeployment">YOLO v3 环境搭建及训练入门指南</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[递归逆序栈]]></title>
        <id>https://zcybupt.github.io/post/ReverseStackUsingRecursive/</id>
        <link href="https://zcybupt.github.io/post/ReverseStackUsingRecursive/">
        </link>
        <updated>2020-07-07T09:05:33.000Z</updated>
        <content type="html"><![CDATA[<h3 id="题目">题目</h3>
<p>逆序一个栈, 且不能使用额外的数据结构, 只可使用递归函数</p>
<h3 id="题解">题解</h3>
<p>可将问题分为两个步骤:</p>
<ol>
<li>弹出栈底元素并返回</li>
<li>获取栈底元素并将其放回栈顶</li>
</ol>
<h4 id="java">Java</h4>
<pre><code class="language-java">public class ReverseStackUsingRecursive {
    public static void reverse(Stack&lt;Integer&gt; stack) {
        if (stack.isEmpty()) return;

        int ele = getAndRemoveLastElement(stack);
        reverse(stack);
        stack.push(ele);
    }

    public static int getAndRemoveLastElement(Stack&lt;Integer&gt; stack) {
        int result = stack.pop();
        if (stack.isEmpty()) {
            return result;
        } else {
            int last = getAndRemoveLastElement(stack);
            stack.push(result);
            return last;
        }
    }
}
</code></pre>
<h4 id="python">Python</h4>
<pre><code class="language-python">def reverse(stack):
    if not stack or len(stack) == 0:
        return
    ele = get_and_remove_last_element(stack)
    reverse(stack)
    stack.append(ele)


def get_and_remove_last_element(stack):
    result = stack.pop()
    if len(stack) == 0:
        return result
    else:
        last = get_and_remove_last_element(stack)
        stack.append(result)
        return last
</code></pre>
<h4 id="go">Go</h4>
<p>若不使用指针的指针则需注意引用传递, 否则会由于 stack 数组不更新进入死循环</p>
<pre><code class="language-go">func reverse(stack []int) {
	if stack == nil || len(stack) == 0 {
		return
	}

	ele, stack := getAndRemoveLastElement(stack)	// 需将 stack 重新赋值
	reverse(stack)
	stack = append(stack, ele)
}

func getAndRemoveLastElement(stack []int) (int, []int) {
	result := stack[len(stack)-1]
	stack = stack[:len(stack)-1]
	if len(stack) == 0 {
		return result, stack
	} else {
		last, stack := getAndRemoveLastElement(stack)
		stack = append(stack, result)
		return last, stack
	}
}
</code></pre>
]]></content>
    </entry>
</feed>