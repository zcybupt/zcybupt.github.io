<?xml version="1.0" encoding="utf-8"?>
<feed xmlns="http://www.w3.org/2005/Atom">
    <id>https://zcybupt.github.io</id>
    <title>CODEFANCIER</title>
    <updated>2020-08-02T11:04:32.608Z</updated>
    <generator>https://github.com/jpmonette/feed</generator>
    <link rel="alternate" href="https://zcybupt.github.io"/>
    <link rel="self" href="https://zcybupt.github.io/atom.xml"/>
    <logo>https://zcybupt.github.io/images/avatar.png</logo>
    <icon>https://zcybupt.github.io/favicon.ico</icon>
    <rights>All rights reserved 2020, CODEFANCIER</rights>
    <entry>
        <title type="html"><![CDATA[无重复字符的最长子串]]></title>
        <id>https://zcybupt.github.io/post/LengthOfLongestSubstring/</id>
        <link href="https://zcybupt.github.io/post/LengthOfLongestSubstring/">
        </link>
        <updated>2020-08-02T09:09:19.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-3">题目(<a href="https://leetcode-cn.com/problems/longest-substring-without-repeating-characters/">LeetCode #3</a>)</h2>
<p>给定一个字符串，请你找出其中不含有重复字符的<strong>最长子串</strong>的长度。</p>
<p><strong>示例 1:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;abcabcbb&quot;<br>
<strong>输出:</strong> 3<br>
<strong>解释:</strong> 因为无重复字符的最长子串是 &quot;abc&quot;，所以其长度为 3。</p>
</blockquote>
<p><strong>示例 2:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;bbbbb&quot;<br>
<strong>输出:</strong> 1<br>
<strong>解释:</strong> 因为无重复字符的最长子串是 &quot;b&quot;，所以其长度为 1。</p>
</blockquote>
<p><strong>示例 3:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;pwwkew&quot;<br>
<strong>输出:</strong> 3<br>
<strong>解释:</strong> 因为无重复字符的最长子串是 &quot;wke&quot;，所以其长度为 3。答案必须是<strong>子串</strong>的长度，&quot;pwke&quot; 是一个子序列，不是子串。</p>
</blockquote>
<h2 id="题解">题解</h2>
<p>可采用滑动窗口法，将窗口内的字符保存到集合中，窗口的左边界向右移动时，弹出已不在窗口内的元素；窗口的右边界向右移动时判断字符是否已在集合中，若不在则加入，若已有则终止循环，记录当前最长长度。</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">import java.util.HashSet;

public class Solution {
    public int lengthOfLongestSubstring(String s) {
        HashSet&lt;Character&gt; charSet = new HashSet&lt;&gt;();
        int length = s.length();
        int rk = -1, ans = 0;

        for (int i = 0; i &lt; length; i++) {
            if (i != 0) {
                charSet.remove(s.charAt(i - 1));
            }
            while (rk + 1 &lt; length &amp;&amp; !charSet.contains(s.charAt(rk + 1))) {
                charSet.add(s.charAt(rk + 1));
                rk++;
            }
            ans = Math.max(ans, rk - i + 1);
        }

        return ans;
    }

    public static void main(String[] args) {
        String s = &quot;abcabcbb&quot;;
        Solution solution = new Solution();
        System.out.println(solution.lengthOfLongestSubstring(s));
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class Solution:
    def lengthOfLongestSubstring(self, s: str) -&gt; int:
        char_set = set()
        rk, ans = -1, 0
        length = len(s)

        for i in range(length):
            if i != 0: char_set.remove(s[i - 1])

            while rk + 1 &lt; length and s[rk + 1] not in char_set:
                char_set.add(s[rk + 1])
                rk += 1
            ans = max(ans, rk - i + 1)

        return ans
</code></pre>
<h3 id="go">Go</h3>
<p>Golang 中没有集合，使用 map 实现过于繁琐，可采用 int 数组来模拟集合操作。</p>
<p>将每个字符的 <code>ASCII</code> 值作为数组的索引，数组内存储的为该字符的出现与否（1 或 0）。</p>
<pre><code class="language-go">package main

import (
	&quot;fmt&quot;
	&quot;math&quot;
)

func lengthOfLongestSubstring(s string) int {
	var window [128]int         // ASCII 总共定义了 128 个字符
	length := len(s)
	rk, ans := -1, 0

	for i, _ := range s {
		if i != 0 {
			window[s[i-1]] = 0  // 左边界移出窗口
		}
		for rk+1 &lt; length &amp;&amp; window[s[rk+1]] == 0 {
			window[s[rk+1]] = 1 // 右边界移入窗口
			rk++
		}
		ans = int(math.Max(float64(ans), float64(rk-i+1)))
	}
	return ans
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[二叉查找树]]></title>
        <id>https://zcybupt.github.io/post/BinarySearchTree/</id>
        <link href="https://zcybupt.github.io/post/BinarySearchTree/">
        </link>
        <updated>2020-07-28T07:38:11.000Z</updated>
        <content type="html"><![CDATA[<h2 id="定义">定义</h2>
<p><strong><a href="https://zh.wikipedia.org/wiki/%E4%BA%8C%E5%85%83%E6%90%9C%E5%B0%8B%E6%A8%B9">二叉查找树</a></strong>，也称为<strong>二叉搜索树</strong>、<strong>有序二叉树</strong>（ordered binary tree）或<strong>排序二叉树</strong>（sorted binary tree），是指一棵空树或者具有下列性质的二叉树：</p>
<ol>
<li>若任意节点的左子树不空，则左子树上所有节点的值均小于它的根节点的值；</li>
<li>若任意节点的右子树不空，则右子树上所有节点的值均大于或等于它的根节点的值；</li>
<li>任意节点的左、右子树也分别为二叉查找树；</li>
</ol>
<p>其优势在于查找、插入的时间复杂度较低，理想情况下为 <code>O(logn)</code> 。</p>
<h2 id="实现">实现</h2>
<h3 id="节点定义">节点定义</h3>
<pre><code class="language-java">class Node {
    int value;
    Node parent;
    Node left;
    Node right;

    public Node(int value, Node parent, Node left, Node right) {
        this.value = value;
        this.parent = parent;
        this.left = left;
        this.right = right;
    }
}
</code></pre>
<h3 id="查找节点">查找节点</h3>
<pre><code class="language-java">public static Node search(int ele) {
    Node tmp = root;

    while (tmp != null &amp;&amp; tmp.value != ele) {
        if (ele &lt; tmp.value) {
            tmp = tmp.left;
        } else {
            tmp = tmp.right;
        }
    }

    return tmp;
}
</code></pre>
<h3 id="插入节点">插入节点</h3>
<p>先找到待插入节点的父节点，再插入新节点并返回。</p>
<pre><code class="language-java">public static Node insert(int ele) {
    if (root == null) {
        root = new Node(ele, null, null, null);
        return root;
    }

    // 找到即将插入元素的父节点
    Node parent = null;
    Node tmp = root;
    while (tmp != null) {
        parent = tmp;
        if (ele &lt; tmp.value) {
            tmp = tmp.left;
        } else {
            tmp = tmp.right;
        }
    }

    Node newNode = new Node(ele, parent, null, null);
    if (parent.value &gt; ele) {
        parent.left = newNode;
    } else {
        parent.right = newNode;
    }

    return newNode;
}
</code></pre>
<h3 id="删除节点">删除节点</h3>
<h4 id="替换-移植节点">替换 / 移植节点</h4>
<p>使用 <code>newNode</code> 替换 <code>nodeToReplace</code> ，并返回被替换的节点。</p>
<pre><code class="language-java">public static Node transplant(Node nodeToReplace, Node newNode) {
    if (nodeToReplace.parent == null) {
        root = newNode;
    } else if (nodeToReplace == nodeToReplace.parent.left) {
        nodeToReplace.parent.left = newNode;
    } else {
        nodeToReplace.parent.right = newNode;
    }

    if (newNode != null) {
        newNode.parent = nodeToReplace.parent;
    }

    return newNode;
}
</code></pre>
<h4 id="找到后继节点">找到后继节点</h4>
<p>找到节点的中序后继，即该节点右子树上的最小值。</p>
<pre><code class="language-java">public static Node getMinimum(Node node) {
    while (node.left != null) {
        node = node.left;
    }

    return node;
}
</code></pre>
<h4 id="删除">删除</h4>
<p>将待删除节点的中序后继移动到待删除节点位置，并调整树的结构</p>
<ul>
<li>
<p>若待删除节点的父节点无左子树或右子树，直接将该节点的右子树或左子树上移即可；</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730102431.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>若待删除节点的左右子树均不为空，以下图所示树为例，其中 9 为待删除节点，此时其中序后继 10 与 9 不直接相连</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730153412.png" alt="" loading="lazy"></figure>
<ul>
<li>先找到待删除节点的中序后继，在此为 10，即为待删除节点的替换节点，此时该后继不与待删除节点直接相连，先将其子树 11 移至 10 位置。①使 <code>successorNode.right</code> 指向待删除节点 9 的 <code>right</code> ，②使 <code>successorNode.right.parent</code> 指向 10，得到：</li>
</ul>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730154437.png" alt="" loading="lazy"></figure>
<ul>
<li>再将 <code>successorNode</code> 移到 <code>deleteNode</code> 的位置，并修改其左子树的指向。</li>
</ul>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730154935.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>若中序后继与待删除节点直接相连，如下图所示：</p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730160504.png" alt="" loading="lazy"></figure>
<ul>
<li>9 与 12 直接相连，则直接将 12 上移至 9 的位置，并修改 12 的左子树指向即可。</li>
</ul>
<figure data-type="image" tabindex="6"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200730160537.png" alt="" loading="lazy"></figure>
</li>
</ul>
<pre><code class="language-java">public static Node delete(Node deleteNode) {
    if (deleteNode != null) {
        Node nodeToReturn = null;
        if (deleteNode.left == null) {
            nodeToReturn = transplant(deleteNode, deleteNode.right);
        } else if (deleteNode.right == null) {
            nodeToReturn = transplant(deleteNode, deleteNode.left);
        } else {
            Node successorNode = getMinimum(deleteNode.right);
            if (successorNode.parent != deleteNode) {
                transplant(successorNode, successorNode.right);
                successorNode.right = deleteNode.right;
                successorNode.right.parent = successorNode;
            }
            transplant(deleteNode, successorNode);
            successorNode.left = deleteNode.left;
            successorNode.left.parent = successorNode;
            nodeToReturn = successorNode;
        }
        return nodeToReturn;
    }
    return null;
}
</code></pre>
<h2 id="缺点">缺点</h2>
<p>插入或删除节点过程中如果不及时调整，将使查找效率降低，如将数组 <code>[2, 3, 4, 5, 6]</code> 插入树的过程中，不加调整将得到如下结构，此时查找和插入效率退化至 <code>O(n)</code> 。</p>
<figure data-type="image" tabindex="7"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200729204357.png" alt="" loading="lazy"></figure>
<p>为解决查找效率退化的问题，又提出了 AVL 树和红黑树等，可将最坏效率降至 <code>O(logn)</code>。</p>
<p><a href="https://github.com/zcybupt/rawfiles/blob/master/codes/Java/Code_50_BinarySearchTree.java">完整代码</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Morris遍历]]></title>
        <id>https://zcybupt.github.io/post/MorrisTraversal/</id>
        <link href="https://zcybupt.github.io/post/MorrisTraversal/">
        </link>
        <updated>2020-07-26T10:20:43.000Z</updated>
        <content type="html"><![CDATA[<h2 id="原理">原理</h2>
<p>无论是手动创建栈遍历树还是递归（即调用系统栈）遍历树，其空间复杂度均为 <code>O(N)</code> ，而 Morris 实现了 <code>O(1)</code> 常数空间复杂度的树的遍历。</p>
<p>递归遍历树的过程中，每个节点均被遍历了三次，前中后序取决于打印的位置。</p>
<pre><code class="language-java">public class RecursiveTraversal {
    public static void recursiveTraverse(TreeNode head) {
        if (head == null) return;

        // 先序遍历
        // System.out.println(head.val);

        recursiveTraverse(head.left);

        // 中序遍历
        // System.out.println(head.val);
        
        recursiveTraverse(head.right);
        
        // 后序遍历
        // System.out.println(head.val);
    }
}
</code></pre>
<p>Morris 遍历实质上是人为地追踪递归过程中遍历位置的变化。其过程如下：</p>
<p>设 <code>cur</code> 为当前节点。</p>
<ol>
<li>若 <code>cur</code> 无左子树，<code>cur</code> 向右移动</li>
<li>若 <code>cur</code> 有左子树，找到左子树上最右节点，记为 <code>rightmost</code>
<ol>
<li>若 <code>rightmost</code> 的右孩子为 <code>null</code> ，<code>rightmost.right = cur</code> ，<code>cur</code> 向左移动</li>
<li>若 <code>rightmost</code> 的右孩子为 <code>cur</code> ，<code>rightmost.right = null</code> ，<code>cur</code> 向右移动</li>
</ol>
</li>
</ol>
<ul>
<li>
<p>若某节点有左子树，则该节点将被遍历两次（即 <code>2.2</code> ），若没有左子树则将被遍历一次；</p>
</li>
<li>
<p>在第二遍历某节点时，当前节点总是该节点左子树的 <code>rightmost</code> 的后继（节点的后继指中序遍历输出结果的后续节点）</p>
</li>
<li>
<p>通过 <code>rightmost</code> 的右孩子的指向来判断是第一次还是第二次遍历到当前节点。</p>
</li>
</ul>
<p>下图为每一步迭代的结果（从左至右，从上到下），cur代表当前节点，深色节点表示该节点已输出。（图源：<a href="https://www.cnblogs.com/anniekim/archive/2013/06/15/morristraversal.html">AnnieKim</a>）</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200727191413.png" alt="" loading="lazy"></figure>
<h2 id="实现">实现</h2>
<h3 id="先序遍历">先序遍历</h3>
<p>第一次到达一个节点时就输出。</p>
<h4 id="java">Java</h4>
<pre><code class="language-java">class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;

    TreeNode(int val) {
        this.val = val;
    }
}

public class MorrisTraversal {
    public static void morrisPreorderTraverse(TreeNode head) {
        if (head == null) return;

        TreeNode cur1 = head;
        TreeNode cur2 = null;

        while (cur1 != null) {
            cur2 = cur1.left;
            if (cur2 != null) {                       // cur1 有左子树
                while (cur2.right != null &amp;&amp; cur2.right != cur1) {
                    cur2 = cur2.right;                // 左子树上最右节点, 此时 cur2 即为 rightmost
                }
                if (cur2.right == null) {             // rightmost 的右孩子为空, 此时为第一次遍历当前节点
                    cur2.right = cur1;
                    System.out.print(cur1.val + &quot; &quot;); // 第一次遍历一个节点, 打印该节点
                    cur1 = cur1.left;
                    continue;
                } else {
                    cur2.right = null;                // rightmost 的右孩子指向 cur1, 此时为第二次遍历当前节点
                }
            } else {
                System.out.print(cur1.val + &quot; &quot;);     // 节点无左子树, 只会遍历一遍, 故直接输出
            }
            cur1 = cur1.right;                        // cur1 无左子树或 rightmost 的右孩子指向 cur1 时
        }
    }
}
</code></pre>
<h4 id="python">Python</h4>
<pre><code class="language-python">class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


def morris_preorder_traverse(head: TreeNode) -&gt; None:
    if not head: return

    cur1 = head
    while cur1:
        cur2 = cur1.left
        if cur2:
            while cur2.right and cur2.right != cur1:
                cur2 = cur2.right
            if cur2.right is None:
                cur2.right = cur1
                print(cur1.val, end=&quot; &quot;)
                cur1 = cur1.left
                continue
            else:
                cur2.right = None
        else:
            print(cur1.val, end=&quot; &quot;)
        cur1 = cur1.right
</code></pre>
<h4 id="go">Go</h4>
<pre><code class="language-go">type TreeNode struct {
	val   int
	left  *TreeNode
	right *TreeNode
}

func morrisPreorderTraverse(head *TreeNode) {
	if head == nil {
		return
	}

	cur1 := head
	var cur2 *TreeNode

	for cur1 != nil {
		cur2 = cur1.left
		if cur2 != nil {
			for cur2.right != nil &amp;&amp; cur2.right != cur1 {
				cur2 = cur2.right
			}
			if cur2.right == nil {
				cur2.right = cur1
				fmt.Printf(&quot;%d &quot;, cur1.val)
				cur1 = cur1.left
				continue
			} else {
				cur2.right = nil
			}
		} else {
			fmt.Printf(&quot;%d &quot;, cur1.val)
		}
		cur1 = cur1.right
	}
}
</code></pre>
<h3 id="中序遍历">中序遍历</h3>
<p>有左子树的节点第二次回到该节点时再打印；无左子树的节点只会遍历一次，故直接输出。如下图输出顺序为 <code>2 1 3</code> 。</p>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200727220729.png" alt="" loading="lazy"></figure>
<p>当前节点无左子树或第二次遍历当前节点（即 <code>rightmost</code> 的右孩子指向 <code>cur1</code>）时均需输出当前节点值，而只有这两种情况需将指针右移，故可理解为只要向右移动就输出。</p>
<h4 id="java-2">Java</h4>
<pre><code class="language-java">public class MorrisTraversal {
    public static void morrisInOrderTraverse(TreeNode head) {
        if (head == null) return;

        TreeNode cur1 = head;
        TreeNode cur2 = null;

        while (cur1 != null) {
            cur2 = cur1.left;
            if (cur2 != null) {               // cur1 有左子树
                while (cur2.right != null &amp;&amp; cur2.right != cur1) {
                    cur2 = cur2.right;        // 左子树上最右节点, 此时 cur2 即为 rightmost
                }
                if (cur2.right == null) {     // rightmost 的右孩子为空, 此时为第一次遍历当前节点
                    cur2.right = cur1;
                    cur1 = cur1.left;
                    continue;
                } else {
                    cur2.right = null;        // rightmost 的右孩子指向 cur1, 此时为第二次遍历当前节点
                }
            }
            System.out.print(cur1.val + &quot; &quot;); // 当前节点无左子树或第二次遍历当前节点
            cur1 = cur1.right;                // cur1 无左子树或 rightmost 的右孩子指向 cur1 时
        }
    }
}
</code></pre>
<h4 id="python-2">Python</h4>
<pre><code class="language-python">def morris_in_order_traverse(head: TreeNode) -&gt; None:
    if not head: return

    cur1 = head
    while cur1:
        cur2 = cur1.left
        if cur2:
            while cur2.right and cur2.right != cur1:
                cur2 = cur2.right
            if cur2.right is None:
                cur2.right = cur1
                cur1 = cur1.left
                continue
            else:
                cur2.right = None
        print(cur1.val, end=&quot; &quot;)
        cur1 = cur1.right
</code></pre>
<h4 id="go-2">Go</h4>
<pre><code class="language-go">func morrisInOrderTraverse(head *TreeNode) {
	if head == nil {
		return
	}

	cur1 := head
	var cur2 *TreeNode

	for cur1 != nil {
		cur2 = cur1.left
		if cur2 != nil {
			for cur2.right != nil &amp;&amp; cur2.right != cur1 {
				cur2 = cur2.right
			}
			if cur2.right == nil {
				cur2.right = cur1
				cur1 = cur1.left
				continue
			} else {
				cur2.right = nil
			}
		}
		fmt.Printf(&quot;%d &quot;, cur1.val)
		cur1 = cur1.right
	}
}
</code></pre>
<h3 id="后序遍历">后序遍历</h3>
<p>第二次遍历到某节点时，逆序打印其左子树的右边界。遍历完后再逆序打印整棵树的右边界即为后序遍历。</p>
<h4 id="java-3">Java</h4>
<pre><code class="language-java">public class MorrisTraversal {
    public static void morrisPostOrderTraverse(TreeNode head) {
        if (head == null) return;

        TreeNode cur1 = head;
        TreeNode cur2 = null;

        while (cur1 != null) {
            cur2 = cur1.left;
            if (cur2 != null) {             // cur1 有左子树
                while (cur2.right != null &amp;&amp; cur2.right != cur1) {
                    cur2 = cur2.right;      // 左子树上最右节点, 此时 cur2 即为 rightmost
                }
                if (cur2.right == null) {   // rightmost 的右孩子为空, 此时为第一次遍历当前节点
                    cur2.right = cur1;
                    cur1 = cur1.left;
                    continue;
                } else {
                    cur2.right = null;      // rightmost 的右孩子指向 cur1, 此时为第二次遍历当前节点
                    printEdge(cur1.left);   // 第二次遍历到该节点, 打印左子树的右边界
                }
            }
            cur1 = cur1.right;              // cur1 无左子树
        }
        printEdge(head);                    // 打印整棵树的右边界
    }

    public static void printEdge(TreeNode head) {
        TreeNode tail = reverseEdge(head);
        TreeNode cur = tail;

        while (cur != null) {
            System.out.print(cur.val + &quot; &quot;);
            cur = cur.right;
        }
        recursiveTraverse(tail);
    }

    public static TreeNode reverseEdge(TreeNode from) {
        TreeNode pre = null;
        TreeNode next = null;

        while (from != null) {
            next = from.right;
            from.right = pre;
            pre = from;
            from = next;
        }
        return pre;
    }
}
</code></pre>
<h4 id="python-3">Python</h4>
<pre><code class="language-python">def morris_post_order_traverse(head: TreeNode) -&gt; None:
    if not head: return

    cur1 = head
    while cur1:
        cur2 = cur1.left
        if cur2:
            while cur2.right and cur2.right != cur1:
                cur2 = cur2.right
            if cur2.right is None:
                cur2.right = cur1
                cur1 = cur1.left
                continue
            else:
                cur2.right = None
                print_edge(cur1.left)
        cur1 = cur1.right
    print_edge(head)


def print_edge(head: TreeNode) -&gt; None:
    tail = reverse_edge(head)
    cur = tail

    while cur:
        print(cur.val, end=&quot; &quot;)
        cur = cur.right

    reverse_edge(tail)


def reverse_edge(head: TreeNode) -&gt; TreeNode:
    pre = None
    while head:
        rear = head.right
        head.right = pre
        pre = head
        head = rear
    return pre
</code></pre>
<h4 id="go-3">Go</h4>
<pre><code class="language-go">func morrisPostOrderTraverse(head *TreeNode) {
	if head == nil {
		return
	}

	cur1 := head
	var cur2 *TreeNode

	for cur1 != nil {
		cur2 = cur1.left
		if cur2 != nil {
			for cur2.right != nil &amp;&amp; cur2.right != cur1 {
				cur2 = cur2.right
			}
			if cur2.right == nil {
				cur2.right = cur1
				cur1 = cur1.left
				continue
			} else {
				cur2.right = nil
				printEdge(cur1.left)
			}
		}
		cur1 = cur1.right
	}
	printEdge(head)
}

func printEdge(head *TreeNode) {
	tail := reverseEdge(head)
	cur := tail

	for cur != nil {
		fmt.Printf(&quot;%d &quot;, cur.val)
		cur = cur.right
	}

	reverseEdge(tail)
}

func reverseEdge(head *TreeNode) *TreeNode {
	var pre *TreeNode
	for head != nil {
		next := head.right
		head.right = pre
		pre = head
		head = next
	}

	return pre
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[最短回文串]]></title>
        <id>https://zcybupt.github.io/post/ShortestPalindrome/</id>
        <link href="https://zcybupt.github.io/post/ShortestPalindrome/">
        </link>
        <updated>2020-07-22T10:28:47.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-214">题目(<a href="https://leetcode-cn.com/problems/shortest-palindrome">LeetCode #214</a>)</h2>
<p>给定一个字符串 s，你可以通过在字符串前面添加字符将其转换为回文串。找到并返回可以用这种方式转换的最短回文串。</p>
<p><strong>示例 1:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;aacecaaa&quot;<br>
<strong>输出:</strong> &quot;aaacecaaa&quot;</p>
</blockquote>
<p><strong>示例 2:</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;abcd&quot;<br>
<strong>输出:</strong> &quot;dcbabcd&quot;</p>
</blockquote>
<h2 id="题解">题解</h2>
<p>可利用<a href="https://zcybupt.github.io/post/Manacher/">Manacher 算法</a>找到原字符串包含第一个字符的最长回文子串，将该回文子串后的字符逆序加到开头即可。</p>
<p>示例 1 中，对 <code>aacecaaa</code> 来说，包含第一个字符的最长回文子串为 <code>aacecaa</code> ，需把剩余的 <code>a</code> 逆序加到开头，即得到 <code>aaacecaaa</code> ；示例 2 中，<code>abcd</code> 包含第一个字符的最长回文子串为 <code>a</code> ，<code>bcd</code> 逆序得到 <code>dcb</code> ，放到开头即得到 <code>dcbabcd</code> 。</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">public class ShortestPalindrome {
    public static String preprocess(String s) {
        String result = &quot;^&quot;;
        for (int i = 0; i &lt; s.length(); i++) {
            result += &quot;#&quot; + s.charAt(i);
        }

        return result + &quot;#$&quot;;
    }

    public static String shortestPalindrome(String s) {
        if (s == null || s.length() == 0) return &quot;&quot;;

        String tmpStr = preprocess(s);
        int strLen = tmpStr.length();
        int C = 0, R = 0;
        int[] p = new int[strLen];
        int maxLen = 0;

        for (int i = 1; i &lt; strLen - 1; i++) {
            p[i] = R &gt; i ? Math.min(p[2 * C - i], R - i) : 1;

            while (tmpStr.charAt(i - p[i]) == tmpStr.charAt(i + p[i])) {
                p[i]++;
            }

            if (R &lt; p[i] + i) {
                R = p[i] + i;
                C = i;
            }

            // 仅保存以第一个字符开头的回文子串的最大长度
            // (C - p[C]) / 2 即为当前回文串的起始索引
            if (maxLen &lt; p[i] - 1 &amp;&amp; (C - p[C]) / 2 == 0) {
                maxLen = p[i] - 1;
            }
        }

        // 逆序剩余部分字符串
        String result = &quot;&quot;;
        for (int i = s.length() - 1; i &gt;= maxLen; i--) {
            result += s.charAt(i);
        }

        return result + s;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class ShortestPalindrome:
    def shortest_palindrome(self, s: str) -&gt; str:
        tmp_str = &quot;^#&quot; + &quot;&quot;.join([x + &quot;#&quot; for x in s]) + &quot;$&quot;
        str_len = len(tmp_str)
        p = [0] * str_len
        c, r = 0, 0
        max_len = 0

        for i in range(1, str_len - 1):
            p[i] = min(p[2 * c - i], r - i) if r &gt; i else 1
            while tmp_str[i - p[i]] == tmp_str[i + p[i]]:
                p[i] += 1

            if r &lt; p[i] + i:
                r = p[i] + i
                c = i

            if max_len &lt; p[i] - 1 and (c - p[c]) // 2 == 0:
                max_len = p[i] - 1

        return &quot;&quot;.join([x for x in s[:max_len - 1:-1]]) + s
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">func preprocess(s string) string {
	result := &quot;^#&quot;

	for _, ch := range s {
		result += string(ch) + &quot;#&quot;
	}

	return result + &quot;$&quot;
}

func shortestPalindrome(s string) string {
	if len(s) == 0 {
		return &quot;&quot;
	}

	tmpStr := preprocess(s)
	strLen := len(tmpStr)
	p := make([]int, strLen)
	C, R := 0, 0
	maxLen := 0

	for i := 1; i &lt; strLen-1; i++ {
		if R &gt; i {
			p[i] = min(p[2*C-i], R-i)
		} else {
			p[i] = 1
		}

		for tmpStr[i-p[i]] == tmpStr[i+p[i]] {
			p[i]++
		}

		if R &lt; p[i]+i {
			R = p[i] + i
			C = i
		}

		if maxLen &lt; p[i]-1 &amp;&amp; (C-p[C])/2 == 0 {
			maxLen = p[i] - 1
		}
	}

	result := &quot;&quot;
	for i := len(s) - 1; i &gt;= maxLen; i-- {
		result += string(s[i])
	}

	return result + s
}

func min(a, b int) int {
	if a &lt; b {
		return a
	}
	return b
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[Manacher算法]]></title>
        <id>https://zcybupt.github.io/post/Manacher/</id>
        <link href="https://zcybupt.github.io/post/Manacher/">
        </link>
        <updated>2020-07-20T02:58:51.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-5">题目(<a href="https://leetcode-cn.com/problems/longest-palindromic-substring/">LeetCode #5</a>)</h2>
<p>给定一个字符串 <code>s</code>，找到 <code>s</code> 中最长的回文子串。你可以假设 <code>s</code> 的最大长度为 1000。</p>
<p><strong>示例 1：</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;babad&quot;<br>
<strong>输出:</strong> &quot;bab&quot;<br>
<strong>注意:</strong> &quot;aba&quot; 也是一个有效答案。</p>
</blockquote>
<p><strong>示例 2：</strong></p>
<blockquote>
<p><strong>输入:</strong> &quot;cbbd&quot;<br>
<strong>输出:</strong> &quot;bb&quot;</p>
</blockquote>
<h2 id="题解">题解</h2>
<h3 id="扩展中心法">扩展中心法</h3>
<p>遍历字符串，由每个字符串向两侧扩展逐一比对。</p>
<p>时间复杂度为 <code>O(n²)</code> 。</p>
<pre><code class="language-java">public class Solution {
    public static String longestPalindrome(String s) {
        if (s == null || s.length() == 0) return &quot;&quot;;
        int start = 0, end = 0;

        for (int i = 0; i &lt; s.length(); i++) {
            int len1 = expandAroundCenter(s, i, i);
            int len2 = expandAroundCenter(s, i, i + 1);
            int len = Math.max(len1, len2);

            if (len &gt; end - start) {
                start = i - (len - 1) / 2;
                end = i + len / 2;
            }
        }

        return s.substring(start, end + 1);
    }

    public static int expandAroundCenter(String s, int left, int right) {
        while (left &gt;= 0 &amp;&amp; right &lt; s.length() &amp;&amp; s.charAt(left) == s.charAt(right)) {
            left--; right++;
        }

        return right - left - 1;
    }
}
</code></pre>
<h3 id="manacher算法">Manacher算法</h3>
<h4 id="预处理">预处理</h4>
<p>在原始字符串的每个字符的左右两边都加上特殊字符，使处理后的字符串长度均为奇数，以此来解决偶回文时用扩展法不能找到回文中心而导致的找不到回文子串的问题。</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721172042.png" alt="处理前" loading="lazy"></figure>
<figure data-type="image" tabindex="2"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721172128.png" alt="处理后" loading="lazy"></figure>
<h4 id="最长回文子串长度">最长回文子串长度</h4>
<p>下表中 <code>p</code> 数组存储的是以各字符为中心的回文子串的最大长度。观察可知，预处理后的字符串的最长回文子串的回文半径减 <code>1</code> 即为原字符串的最长回文子串长度。以字符串 <code>cabbaf</code> 为例，处理后的最长回文子串的回文半径为 <code>p[6] = 5</code> ，<code>p[6] - 1 = 4</code> 即为原字符串的最长回文子串 <code>abba</code> 的长度。</p>
<figure data-type="image" tabindex="3"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721172934.png" alt="" loading="lazy"></figure>
<p>所以原字符串的最长回文子串长度 <code>maxLen = p[i] - 1</code></p>
<h4 id="最长回文子串的起始索引">最长回文子串的起始索引</h4>
<p>在 <code>#c#a#b#b#a#f#</code> 中，<code>p[6] = 5</code> ，用索引 <code>i</code> 减去最长回文半径 <code>p[i]</code> 即可得到原字符串的最长回文子串的起始索引，即 <code>i - p[i] = 6 - 5 = 1</code> ；但对奇回文 <code>aba</code> 来说，用这种方法得到的结果为 <code>3 - p[3] = 3 - 4 = -1</code> ，下标越界。</p>
<p>为解决下标越界的问题，可在预处理后的字符串前再添加一个特殊字符，而为了使字符串长度保持为奇数，其后也需添加另一个字符。而原字符串的最长回文子串的起始索引变为 <code>(i - p[i]) / 2</code> 。</p>
<figure data-type="image" tabindex="4"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721180408.png" alt="" loading="lazy"></figure>
<p>对 <code>cabbaf</code> 来说，原字符串的最长回文子串的起始索引为 <code>(7 - p[7]) / 2 = (7 - 5) / 2 = 1</code></p>
<figure data-type="image" tabindex="5"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721180435.png" alt="" loading="lazy"></figure>
<p>对 <code>aba</code> 来说，原字符串的最长回文子串的起始索引为 <code>(3 - p[3]) / 2 = (3 - 3) / 2 = 0</code></p>
<p>故原字符串的最长回文子串的起始索引可表示为 <code>index = (i - p[i]) / 2</code></p>
<h4 id="计算-p-数组">计算 p 数组</h4>
<p>定义两个变量 <code>C</code> 和 <code>R</code> ，分别为某一回文子串的中心字符的索引和右边界。如对 <code>^#c#a#b#b#a#f#$</code> 中的 <code>#a#b#b#a#</code> 来说，<code>C = 7</code> ，<code>p[C] = 5</code> ，以 <code>7</code> 为中心的回文子串的右边界即为 <code>R = C + p[C] = 12</code> ；当 <code>C = 12</code> 时，以 <code>12</code> 为中心的回文子串的右边界即为 <code>R = C + p[C] = 12 + 2 = 14</code> 。</p>
<p>故可得回文子串右边界与其半径之间的关系为：<code>R = C + p[C]</code></p>
<figure data-type="image" tabindex="6"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200721192607.png" alt="" loading="lazy"></figure>
<p>由于回文子串关于 <code>C</code> 对称，由该子串上某位置 <code>i</code> 可得其对称位置 <code>j</code> ，由 <code>i + j = 2 * C</code> 知 <code>j = 2 * C - i</code> 。</p>
<ul>
<li>若以 <code>j</code> 为中心的回文子串的右边界在 <code>R</code> 以内，即 <code>j + p[j] &lt; R</code> 时，由对称性可知，<code>p[j] = p[i]</code> ；</li>
<li>若以 <code>j</code> 为中心的回文子串的右边界大于 <code>R</code> ，即 <code>j + p[j] &gt; R</code> 时，图中红色部分为关于 <code>j</code> 对称的回文子串，故此时有 <code>p[j] = R - j</code> ；</li>
<li>若 <code>j + p[j] &gt;= R</code> ，则令 <code>p[j] = 1</code> ，此时也需更新 <code>C</code> 和 <code>R</code> ，继续向后用扩展中心法计算 <code>p</code> 各位置的值。</li>
</ul>
<h5 id="java">Java</h5>
<pre><code class="language-java">public class Manacher {
    public static String preprocess(String s) {
        String result = &quot;^&quot;;
        for (int i = 0; i &lt; s.length(); i++) {
            result += &quot;#&quot; + s.charAt(i);
        }

        return result + &quot;#$&quot;;
    }

    public static String longestPalindrome(String s) {
        if (s.length() == 0) return &quot;&quot;;

        String tmpStr = preprocess(s);
        int len = tmpStr.length();
        int[] p = new int[len];
        int C = 0, R = 0;
        int maxLen = -1;    // 最长回文子串长度
        int index = 0;      // 最长回文子串中心索引

        for (int i = 1; i &lt; len - 1; i++) {
            p[i] = R &gt; i ? Math.min(p[2 * C - i], R - i) : 1;

            while (tmpStr.charAt(i + p[i]) == tmpStr.charAt(i - p[i]))
                p[i]++;

            if (R &lt; p[i] + i) {
                R = p[i] + i;
                C = i;
            }

            if (maxLen &lt; p[i] - 1) {
                maxLen = p[i] - 1;
                index = i;
            }
        }

        int start = (index - maxLen) / 2;   // (i - p[i]) / 2
        return s.substring(start, start + maxLen);
    }
}
</code></pre>
<h5 id="python">Python</h5>
<pre><code class="language-python">class Manacher:
    def longest_palindrome(self, s: str) -&gt; str:
        if s is None or len(s) == 0: return &quot;&quot;

        tmp_str = '^#' + ''.join([x + '#' for x in s]) + &quot;$&quot;
        length = len(tmp_str)
        p = [0] * length
        c, r = 0, 0
        max_len = -1
        index = 0

        for i in range(1, length - 1):
            p[i] = min(p[2 * c - i], r - i) if r &gt; i else 1

            while tmp_str[i + p[i]] == tmp_str[i - p[i]]:
                p[i] += 1

            if r &lt; i + p[i]:
                r = i + p[i]
                c = i

            if max_len &lt; p[i] - 1:
                max_len = p[i] - 1
                index = i

        start = (index - max_len) // 2
        return s[start: start + max_len]
</code></pre>
<h5 id="go">Go</h5>
<pre><code class="language-go">func preprocess(s string) string {
	result := &quot;^#&quot;

	for _, ch := range s {
		result += string(ch) + &quot;#&quot;
	}

	return result + &quot;$&quot;
}

func longestPalindrome(s string) string {
	if len(s) == 0 {
		return &quot;&quot;
	}

	tmpStr := preprocess(s)
	length := len(tmpStr)
	p := make([]int, length)
	C, R := 0, 0
	maxLen := 0
	index := 0

	for i := 1; i &lt; length-1; i++ {
		if R &gt; i {
			p[i] = int(math.Min(float64(p[2*C-i]), float64(R-i)))
		} else {
			p[i] = 1
		}

		for tmpStr[i+p[i]] == tmpStr[i-p[i]] {
			p[i]++
		}

		if R &lt; i+p[i] {
			R = i + p[i]
			C = i
		}

		if maxLen &lt; p[i]-1 {
			maxLen = p[i] - 1
			index = i
		}
	}

	start := (index - maxLen) / 2
	return s[start : start+maxLen]
}
</code></pre>
<h3 id="参考资料">参考资料</h3>
<p><a href="https://www.jianshu.com/p/392172762e55">马拉车算法</a></p>
<p><a href="https://leetcode.wang/leetCode-5-Longest-Palindromic-Substring.html">leetcode.wang</a></p>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[判断子树结构是否相同]]></title>
        <id>https://zcybupt.github.io/post/SubtreeOfAnotherTree/</id>
        <link href="https://zcybupt.github.io/post/SubtreeOfAnotherTree/">
        </link>
        <updated>2020-07-19T09:05:27.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-572">题目(<a href="https://leetcode-cn.com/problems/subtree-of-another-tree/">LeetCode #572</a>)</h2>
<p>给定两个非空二叉树 <code>s</code> 和 <code>t</code>，检验 <code>s</code> 中是否包含和 <code>t</code> 具有相同结构和节点值的子树。<code>s</code> 的一个子树包括 <code>s</code> 的一个节点和这个节点的所有子孙。<code>s</code> 也可以看做它自身的一棵子树。</p>
<p>示例 1:</p>
<p>给定的树 s:</p>
<pre><code class="language-python">     3
    / \
   4   5
  / \
 1   2
</code></pre>
<p>给定的树 t：</p>
<pre><code class="language-python">   4 
  / \
 1   2
</code></pre>
<p>返回 <strong>true</strong>，因为 t 与 s 的一个子树拥有相同的结构和节点值。</p>
<p>示例 2:</p>
<p>给定的树 s：</p>
<pre><code class="language-python">     3
    / \
   4   5
  / \
 1   2
    /
   0
</code></pre>
<p>给定的树 t：</p>
<pre><code class="language-python">   4
  / \
 1   2
</code></pre>
<p>返回 <strong>false</strong>。</p>
<h2 id="题解">题解</h2>
<p><strong>仅作为KMP的练习，非最优解</strong></p>
<p>先将两棵树序列化（这里采用先序遍历），再利用 KMP 判断 <code>t</code> 是否为 <code>s</code> 的子串。</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">class TreeNode {
    int val;
    TreeNode left;
    TreeNode right;

    TreeNode(int val) {
        this.val = val;
    }
}

public class Code_45_SubtreeOfAnotherTree {
    public static String getPreorderString(TreeNode treeNode) {
        if (treeNode == null) return &quot;#&quot;;

        String tmpResult = &quot;_&quot; + treeNode.val + &quot;_&quot; + getPreorderString(treeNode.left);
        tmpResult += &quot;_&quot; + getPreorderString(treeNode.right);

        return tmpResult;
    }

    public static int[] getNextTable(String pattern) {
        int len = pattern.length();
        int lp = -1, rp = 0;
        int[] nextTable = new int[len];
        nextTable[0] = -1;

        while (rp &lt; len - 1) {
            if (lp == -1 || pattern.charAt(lp) == pattern.charAt(rp)) {
                nextTable[++rp] = ++lp;
            } else {
                lp = nextTable[lp];
            }
        }

        return nextTable;
    }

    // KMP
    public static boolean isSubtree(TreeNode s, TreeNode t) {
        String serializedS = getPreorderString(s);
        String serializedT = getPreorderString(t);
        int sCur = 0, tCur = 0;
        int sLen = serializedS.length();
        int tLen = serializedT.length();
        int[] nextTable = getNextTable(serializedT);

        while (sCur &lt; sLen &amp;&amp; tCur &lt; tLen) {
            if (serializedS.charAt(sCur) == serializedT.charAt(tCur)) {
                sCur++;
                tCur++;
            } else {
                if (tCur == 0) sCur++;
                else tCur = nextTable[tCur];
            }
        }

        return tCur == tLen;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class TreeNode:
    def __init__(self, val=0, left=None, right=None):
        self.val = val
        self.left = left
        self.right = right


class SubTreeOfAnotherTree:
    def get_preorder_string(self, tree_node: TreeNode):
        if not tree_node: return '#'

        tmp_str = '_' + str(tree_node.val) + '_' + self.get_preorder_string(tree_node.left)
        tmp_str += '_' + self.get_preorder_string(tree_node.right)

        return tmp_str

    def get_next_table(self, pattern: str):
        lp, rp = -1, 0
        length = len(pattern)
        next_table = [0] * length
        next_table[0] = -1

        while rp &lt; length - 1:
            if pattern[lp] == pattern[rp] or lp == -1:
                lp += 1
                rp += 1
                next_table[rp] = lp
            else:
                lp = next_table[lp]

        return next_table

    def isSubtree(self, s: TreeNode, t: TreeNode) -&gt; bool:
        serialized_s = self.get_preorder_string(s)
        serialized_t = self.get_preorder_string(t)
        s_len = len(serialized_s)
        t_len = len(serialized_t)

        s_cur, t_cur = 0, 0
        next_table = self.get_next_table(serialized_t)

        while s_cur &lt; s_len and t_cur &lt; t_len:
            if serialized_s[s_cur] == serialized_t[t_cur]:
                s_cur += 1
                t_cur += 1
            else:
                if t_cur == 0:
                    s_cur += 1
                else:
                    t_cur = next_table[t_cur]

        return t_cur == t_len
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">type TreeNode struct {
	Val   int
	Left  *TreeNode
	Right *TreeNode
}

func getPreorderString(treeNode *TreeNode) string {
	if treeNode == nil {
		return &quot;#&quot;
	}

	tmpStr := &quot;_&quot; + strconv.Itoa(treeNode.Val) + &quot;_&quot; + getPreorderString(treeNode.Left)
	tmpStr += &quot;_&quot; + getPreorderString(treeNode.Right)

	return tmpStr
}

func getNextTable(pattern string) []int {
	lp, rp := -1, 0
	length := len(pattern)
	nextTable := make([]int, length)
	nextTable[0] = -1

	for rp &lt; length-1 {
		if lp == -1 || pattern[lp] == pattern[rp] {
			lp++
			rp++
			nextTable[rp] = lp
		} else {
			lp = nextTable[lp]
		}
	}

	return nextTable
}

func isSubtree(s *TreeNode, t *TreeNode) bool {
	serializedS := getPreorderString(s)
	serializedT := getPreorderString(t)
	sCur, tCur := 0, 0
	sLen := len(serializedS)
	tLen := len(serializedT)
	nextTable := getNextTable(serializedT)

	for sCur &lt; sLen &amp;&amp; tCur &lt; tLen {
		if serializedS[sCur] == serializedT[tCur] {
			sCur++
			tCur++
		} else {
			if tCur == 0 {
				sCur++
			} else {
				tCur = nextTable[tCur]
			}
		}
	}

	return tCur == tLen
}
</code></pre>
<h3 id="奇淫巧技">奇淫巧技</h3>
<blockquote>
<p><a href="https://leetcode-cn.com/problems/subtree-of-another-tree/comments/384272">LeetCode @洪世贤</a>：<br>
没有什么是 Python 一行解决不了的</p>
</blockquote>
<p>Python 的诱惑？</p>
<pre><code class="language-python">class Solution:
    def isSubtree(self, s: TreeNode, t: TreeNode) -&gt; bool:
        return str(s).find(str(t)) != -1
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[KMP算法]]></title>
        <id>https://zcybupt.github.io/post/KMP/</id>
        <link href="https://zcybupt.github.io/post/KMP/">
        </link>
        <updated>2020-07-18T13:03:03.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-28">题目(<a href="https://leetcode-cn.com/problems/implement-strstr/">LeetCode #28</a>)</h2>
<p>实现 <code>strStr()</code> 函数。</p>
<p>给定一个 haystack 字符串和一个 needle 字符串，在 haystack 字符串中找出 needle 字符串出现的第一个位置 (从0开始)。如果不存在，则返回  -1。</p>
<p><strong>示例 1:</strong></p>
<blockquote>
<p><strong>输入:</strong> haystack = &quot;hello&quot;, needle = &quot;ll&quot;<br>
<strong>输出:</strong> 2</p>
</blockquote>
<p><strong>示例 2:</strong></p>
<blockquote>
<p><strong>输入:</strong> haystack = &quot;aaaaa&quot;, needle = &quot;bba&quot;<br>
<strong>输出:</strong> -1</p>
</blockquote>
<h2 id="题解">题解</h2>
<p>参考资料：</p>
<ul>
<li>
<p><a href="https://www.bilibili.com/video/BV1Px411z7Yo">KMP字符串匹配算法1</a></p>
</li>
<li>
<p><a href="https://www.bilibili.com/video/BV1hW411a7ys">KMP字符串匹配算法2</a></p>
</li>
<li>
<p><a href="https://mp.weixin.qq.com/s?__biz=MzI5ODA5NTgyMw==&amp;mid=2247483968&amp;idx=1&amp;sn=3c21f29eb6c5083f85ac7c97d2051303&amp;chksm=ecaa47e0dbddcef6a97a453df68bc6c6ff040bdd84f58caf99b1f3a8cb84d7405062b039c396&amp;token=1263366918&amp;lang=zh_CN#rd">图解KMP</a></p>
</li>
</ul>
<h3 id="java">Java</h3>
<pre><code class="language-java">public class KMP {
    public static int[] getNextTable(String needle) {
        int len = needle.length();
        int[] next = new int[len];
        next[0] = -1;
        int lp = -1, rp = 0;    // 左右指针, 即文章中的 k 和 index

        while (rp &lt; len - 1) {
            if (lp == -1 || needle.charAt(rp) == needle.charAt(lp))
                // k = -1 时, 左右指针均向右移动
                // 当左右指针所指元素相等时, 均向右移动
                next[++rp] = ++lp;
            else
                // 左右指针所指元素不等, 左指针移向其所指字符的对应 next 指向的位置
                lp = next[lp];
        }

        return next;
    }

    public static int getIndex(String haystack, String needle) {
        int haystackLen = haystack.length();
        int needleLen = needle.length();

        if (needleLen == 0) return 0;
        int haystackCur = 0, needleCur = 0;
        int[] next = getNextTable(needle);

        while (haystackCur &lt; haystackLen &amp;&amp; needleCur &lt; needleLen) {
            if (haystack.charAt(haystackCur) == needle.charAt(needleCur)) {
                // 对比两个字符串, 各位相同时两指针右移
                haystackCur++;
                needleCur++;
            } else {
                // 不相同则根据 next 表查找 needleCur 移向的位置
                // needleCur 移动到了开头, 退无可退, 主串指针继续后移
                if (needleCur == 0) haystackCur++;
                else needleCur = next[needleCur];
            }
        }

        return needleCur == needleLen ? haystackCur - needleCur : -1;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class KMP:
    def get_next_table(self, needle):
        length = len(needle)
        lp, rp = -1, 0
        next_table = [0] * length
        next_table[0] = -1

        while rp &lt; length - 1:
            if needle[lp] == needle[rp] or lp == -1:
                rp += 1
                lp += 1
                next_table[rp] = lp
            else:
                lp = next_table[lp]

        return next_table

    def get_index(self, haystack, needle):
        haystack_len = len(haystack)
        needle_len = len(needle)

        if needle_len == 0: return 0
        haystack_cur, needle_cur = 0, 0
        next_table = self.get_next_table(needle)

        while haystack_cur &lt; haystack_len and needle_cur &lt; needle_len:
            if haystack[haystack_cur] == needle[needle_cur]:
                haystack_cur += 1
                needle_cur += 1
            else:
                if needle_cur == 0: haystack_cur += 1
                else: needle_cur = next_table[needle_cur]

        return haystack_cur - needle_cur if needle_cur == needle_len else -1
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">func getNextTable(needle string) []int {
	length := len(needle)
	lp, rp := -1, 0
	next := make([]int, length)
	next[0] = -1

	for rp &lt; length-1 {
		if lp == -1 || needle[lp] == needle[rp] {
			rp++
			lp++
			next[rp] = lp
		} else {
			lp = next[lp]
		}
	}

	return next
}

func getIndex(haystack, needle string) int {
	haystackLen := len(haystack)
	needleLen := len(needle)

	if needleLen == 0 {
		return 0
	}

	haystackCur, needleCur := 0, 0
	next := getNextTable(needle)

	for haystackCur &lt; haystackLen &amp;&amp; needleCur &lt; needleLen {
		if haystack[haystackCur] == needle[needleCur] {
			haystackCur++
			needleCur++
		} else {
			if needleCur == 0 {
				haystackCur++
			} else {
				needleCur = next[needleCur]
			}
		}
	}

	if needleCur == needleLen {
		return haystackCur - needleCur
	} else {
		return -1
	}
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[日程安排问题]]></title>
        <id>https://zcybupt.github.io/post/BestArrange/</id>
        <link href="https://zcybupt.github.io/post/BestArrange/">
        </link>
        <updated>2020-07-15T11:57:58.000Z</updated>
        <content type="html"><![CDATA[<h2 id="问题">问题</h2>
<p>一些项目要占用一个会议室宣讲, 会议室不能同时容纳两个项目的宣讲. 给定每个项目的开始和结束时间 (输入为数组, 其中是一个个具体的项目), 合理安排宣讲日程, 使得会议室进行的宣讲场次最多, 返回这个最多的场次</p>
<h2 id="题解">题解</h2>
<p>本题不可按开始时间排序, 若某个宣讲开始时间早但持续时间长, 可能不如多个晚开始的宣讲场次多; 也不可按持续时间长短, 某个短时间的宣讲可能与其它宣讲的起始时间重叠.</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200715202427.png" alt="" loading="lazy"></figure>
<h3 id="java">Java</h3>
<pre><code class="language-java">class Program {
    int start;
    int end;

    public Program(int start, int end) {
        this.start = start;
        this.end = end;
    }
}

public class BestArrange {
    public static int bestArrange(Program[] programs, int start) {
        Arrays.sort(programs, (a, b) -&gt; a.end - b.end);
        int result = 0;

        for (Program program : programs) {
            if (start &lt;= program.start) {
                result++;
                start = program.end;
            }
        }

        return result;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class Program:
    def __init__(self, start, end):
        self.start = start
        self.end = end


def get_best_arrange(programs, start):
    programs.sort(key=lambda x: x.end)
    result = 0

    for program in programs:
        if start &lt;= program.start:
            result += 1
            start = program.end

    return result
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">type Program struct {
	start, end int
}

func getBestArrange(programs []Program, start int) int {
	for i := 0; i &lt; len(programs); i++ {
		for j := 0; j &lt; len(programs)-1; j++ {
			if programs[j].end &gt; programs[j+1].end {
				programs[j], programs[j+1] = programs[j+1], programs[j]
			}
		}
	}
	result := 0

	for _, program := range programs {
		if start &lt;= program.start {
			result++
			start = program.end
		}
	}

	return result
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[最低字典序排列字符串]]></title>
        <id>https://zcybupt.github.io/post/LexicographicalString/</id>
        <link href="https://zcybupt.github.io/post/LexicographicalString/">
        </link>
        <updated>2020-07-13T10:29:49.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目">题目</h2>
<p>给定一个字符串类型的数组 <code>strs</code> , 找到一种拼接方式, 使得把所有字符串拼接起来之后形成的字符串具有最低的字典序.</p>
<h2 id="题解">题解</h2>
<p>不可通过将各个数组元素按字典序排列再拼接的方式来解, 如对数组 <code>['b', 'ba']</code> 来说, 若按元素字典序来排列将得到 <code>bba</code> , 而实际上该结果并没有 <code>bab</code> 小, 故这种做法不可取.</p>
<p>本题应比较 <code>str1 + str2</code> 和 <code>str2 + str1</code> 的字典序大小, 以此为标准重写比较函数, 排序后拼接即可.</p>
<h3 id="java">Java</h3>
<pre><code class="language-java">public class LowestLexicography {
    public static String lowestString(String[] strs) {
        if (strs == null || strs.length == 0) {
            return null;
        }

        Arrays.sort(strs, (a, b) -&gt; (a + b).compareTo(b + a));

        String result = &quot;&quot;;
        for (String str : strs) {
            result += str;
        }

        return result;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<p>以冒泡排序为例</p>
<pre><code class="language-python">def get_lowest_lexicography(strings):
    for i in range(len(strings)):
        for j in range(len(strings) - 1):
            if strings[j] + strings[j + 1] &gt; strings[j + 1] + strings[j]:
                strings[j], strings[j + 1] = strings[j + 1], strings[j]

    return &quot;&quot;.join(strings)
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-python">func getLowestLexicography(strs []string) string {
	for i := 0; i &lt; len(strs); i++ {
		for j := 0; j &lt; len(strs)-1; j++ {
			if strs[j]+strs[j+1] &gt; strs[j+1]+strs[j] {
				strs[j], strs[j+1] = strs[j+1], strs[j]
			}
		}
	}

	return strings.Join(strs, &quot;&quot;)
}
</code></pre>
]]></content>
    </entry>
    <entry>
        <title type="html"><![CDATA[滑动窗口最大值]]></title>
        <id>https://zcybupt.github.io/post/SlidingWindowMaximum/</id>
        <link href="https://zcybupt.github.io/post/SlidingWindowMaximum/">
        </link>
        <updated>2020-07-12T02:19:20.000Z</updated>
        <content type="html"><![CDATA[<h2 id="题目leetcode-239">题目(<a href="https://leetcode-cn.com/problems/sliding-window-maximum/">LeetCode #239</a>)</h2>
<p>给定一个数组 nums，有一个大小为 k 的滑动窗口从数组的最左侧移动到数组的最右侧。你只可以看到在滑动窗口内的 k 个数字。滑动窗口每次只向右移动一位。</p>
<p>返回滑动窗口中的最大值。</p>
<p>示例:</p>
<pre><code class="language-java">输入: nums = [1,3,-1,-3,5,3,6,7], 和 k = 3
输出: [3,3,5,5,6,7] 
解释: 
             滑动窗口的位置              最大值
[ 1   3   -1 ] -3   5   3   6   7        3
  1 [ 3   -1   -3 ] 5   3   6   7        3
  1   3 [ -1   -3   5 ] 3   6   7        5
  1   3   -1 [ -3   5   3 ] 6   7        5
  1   3   -1   -3 [ 5   3   6 ] 7        6
  1   3   -1   -3   5 [ 3   6   7 ]      7
</code></pre>
<p>提示:</p>
<ul>
<li>1 &lt;= nums.length &lt;= 10^5</li>
<li>-10^4 &lt;= nums[i] &lt;= 10^4</li>
<li>1 &lt;= k &lt;= nums.length</li>
</ul>
<h2 id="题解">题解</h2>
<h3 id="窗口内最值的更新结构">窗口内最值的更新结构</h3>
<p>借助单调双端队列实现, 队列由头到尾单调递减, 头指针始终指向窗口内的最大值. 定义两个指针 <code>L</code> 与 <code>R</code> , 初始值为 -1, <code>L</code> 与 <code>R</code> 之间即为窗口范围. 在窗口移动的过程中, 需满足:</p>
<ol>
<li><code>L</code> 和 <code>R</code> 不向左移动</li>
<li><code>L</code> 不能移动到 <code>R</code> 的右侧</li>
</ol>
<p>定义一个双端队列, 其中保存输入数组中元素的索引.</p>
<ul>
<li>
<p>当窗口增加元素, 即 <code>R</code> 增加时, 需保持队列的单调性. 若新元素比原有元素都小, 则将其索引追加在最后; 若队列中有小于等于新元素的, 则先将所有小于等于新元素的索引弹出, 再加入新元素的索引. 以此实现队列的头指针始终指向最大元素</p>
<figure data-type="image" tabindex="1"><img src="https://raw.githubusercontent.com/zcybupt/images/master/PicGo/20200712162837.png" alt="" loading="lazy"></figure>
</li>
<li>
<p>当窗口减少元素, 即 <code>L</code> 增加时, 依次弹出头结点即可.</p>
</li>
</ul>
<h3 id="java">java</h3>
<p>本题窗口宽度固定, 可用相对位置表示 <code>L</code> 与 <code>R</code></p>
<pre><code class="language-java">public class SlidingWindowMaximum {
    public static int[] maxSlidingWindow(int[] nums, int k) {
        if (nums == null || k &lt; 1 || nums.length &lt; k) {
            return null;
        }

        LinkedList&lt;Integer&gt; maxQueue = new LinkedList&lt;&gt;();
        // 共有 arr.length - k + 1 个窗口, 即共有arr.length - win + 1个最大值
        int[] result = new int[nums.length - k + 1];
        int index = 0;

        // 用 i 表示 R
        for (int i = 0; i &lt; nums.length; i++) {
            while (!maxQueue.isEmpty() &amp;&amp; nums[maxQueue.peekLast()] &lt;= nums[i]) {
                maxQueue.pollLast();
            }
            maxQueue.addLast(i);

            // i - k 为过期的索引, 即窗口的 L 已越过该位置, 需弹出头结点维持窗口大小为 k
            if (maxQueue.peekFirst() == i - k) {
                System.out.println(i + &quot; &quot; + k);
                maxQueue.pollFirst();
            }
            if (i &gt;= k - 1) {
                result[index++] = nums[maxQueue.peekFirst()];
            }
        }

        return result;
    }
}
</code></pre>
<h3 id="python">Python</h3>
<pre><code class="language-python">class SlidingWindowMaximum:
    def maxSlidingWindow(self, nums, k):
        if not nums or k &lt; 1 or len(nums) &lt; k:
            return

        max_queue = []
        result = []

        for i in range(len(nums)):
            while max_queue and nums[max_queue[-1]] &lt;= nums[i]:
                max_queue.pop()
            max_queue.append(i)

            if max_queue[0] == i - k:
                max_queue = max_queue[1:]

            if i &gt;= k - 1:
                result.append(nums[max_queue[0]])

        return result
</code></pre>
<h3 id="go">Go</h3>
<pre><code class="language-go">func maxSlidingWindow(nums []int, k int) []int {
	if nums == nil || k &lt; 0 || len(nums) &lt; k {
		return nil
	}

	maxQueue := make([]int, len(nums))
	result := make([]int, len(nums)-k+1)
	index := 0

	for i := 0; i &lt; len(nums); i++ {
		for len(maxQueue) &gt; 0 &amp;&amp; nums[maxQueue[len(maxQueue)-1]] &lt;= nums[i] {
			maxQueue = maxQueue[:len(maxQueue)-1]
		}
		maxQueue = append(maxQueue, i)

		if maxQueue[0] == i-k {
			maxQueue = maxQueue[1:]
		}

		if i &gt;= k-1 {
			result[index] =nums[maxQueue[0]]
			index++
		}
	}

	return result
}
</code></pre>
]]></content>
    </entry>
</feed>